# Scope the code-review-graph to the application code (backend + engine + frontend).
# Frontend is an active React/TS app (rebuild started) — indexed; only its build
# artifacts are excluded below.
frontend/dist/**
frontend/node_modules/**
frontend/storybook-static/**

# Non-code / non-target areas.
docker/**
docs/**
specs/**
.github/**
.idea/**
.playwright-mcp/**
.claude/**
