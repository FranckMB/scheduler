# openapi-typescript still declares a TypeScript ^5 peer while the project is on
# TS 6 (it is a build-time codegen tool, compatible in practice). Accept it here
# so `npm ci` works identically in local, CI and the Docker image build.
legacy-peer-deps=true
