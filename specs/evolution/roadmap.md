# Roadmap — évolutions extraites des specs initiales

> **Règle du dossier `specs/evolution/`** : ce fichier est l'**index unique** — toute évolution, gap ou idée future y laisse une trace (une ligne de tableau au minimum). Un fichier à côté n'existe que pour **préciser un besoin** trop gros pour une ligne, et doit être **référencé depuis cette roadmap**. Quand un sujet est livré ou tranché, sa ligne est mise à jour ici et le fichier de détail devenu sans objet est supprimé (l'historique vit dans git). **La ligne mise à jour reste une ligne** (statut + pointeur) : le comportement livré se documente dans `specs/courantes/` (graduation, skill `documentation-update`), jamais dans la note roadmap.
> **Fichiers de détail actifs** : [`accueil-cockpit-temporel.md`](../courantes/accueil-cockpit-temporel.md) (§2 — modèle temporel, cockpit, périodes) · [`bridage-freemium-decouverte.md`](bridage-freemium-decouverte.md) (§6 — bridage du plan gratuit) · [`gestion-matchs-ffbb.md`](gestion-matchs-ffbb.md) (§8 — placement des matchs, radar de conflits, dérogations — FF#21/FF#19) · [`enregistrement-ffbb.md`](enregistrement-ffbb.md) (§3 — refonte enregistrement : vérification FFBB + approbation) · [`console-superadmin.md`](console-superadmin.md) (§9 — monitoring & exploitation cross-tenant) · [`compte-demo.md`](compte-demo.md) (§3 — mode/compte démo, levier de vente) · [`infrastructure-hebergement.md`](infrastructure-hebergement.md) (étude d'hébergement, prérequis P0-2) · [`reprise-perimetre-engage.md`](reprise-perimetre-engage.md) (§3 — cadrage P2-7) · [`p2-9-verrous-et-alertes.md`](p2-9-verrous-et-alertes.md) (§1/P2-9 — ce qui reste des verrous et alertes après le volet 1 : PR A/B/C). *(Supprimés une fois livrés : `transition-de-saison.md` (2026-07-08) ; `planning-versions.md` (2026-07-25, D1→D3quater livrés → `docs/architecture/adr-0002-pattern-plan.md` + `courantes/planning-lifecycle-validated.md`) ; `import-ffbb-autofill.md` (2026-07-25, lot C livré → `courantes/backend-inventory.md` + `backend/docs/ffbb-api.md`) ; `plan-vacances-collecte-coach.md` (2026-07-29, P1 structure de période éditable + P2 collecte coach C1/C2/C3 livrés → `courantes/types-de-planning.md` types 2/3 + E4/E5 et décisions figées · `courantes/backend-inventory.md` §2 · `courantes/accueil-cockpit-temporel.md` §6bis · `docs/architecture/adr-0002-pattern-plan.md` inv. 5 amendé).)*
> **But** : base de réflexion écrite pour les prochaines features. Extraction des capacités décrites dans `specs/initiales/` (ClubScheduler v3 + Spécification des contraintes v2), confrontées à l'état **livré**.
> **Statut** : ✅ livré · 🟡 partiel · ⬜ à faire. Les réf `FF#n` et `BG G#n` sont les identifiants **historiques** des anciens fichiers `features-futures.md` / `backend-gaps.md`, absorbés dans cette roadmap le 2026-07-05 (fichiers supprimés) — conservés pour la traçabilité.
> **Effort** (annoté sur les items actionnables, pour arbitrer sans re-poser la question) : **🟢 léger** (≈ ≤2 j, ciblé, peu de risque) · **🟡 moyen** (quelques jours, plusieurs fichiers/zones) · **🔴 lourd** (structurant / semaine+ / nouvelle archi ou dépendance de zone). L'effort estime le **coût de mise en œuvre**, pas la valeur produit.
> **Sources** : `initiales/ClubScheduler_v3.md` (réf `v3 §x`), `initiales/ClubScheduler_Specification_des_contraintes_v2.md` (réf `contraintes-v2`). Ces specs sont **figées** — ne pas les modifier.

Ce fichier est le **document de suivi unique** (fusion du 2026-07-11 : `backlog-priorise.md` + `docs/technical-debt.md` absorbés — même fonction, un seul endroit) : la **carte** de la vision (§1-§10), la **coupe priorisée** effort × impact (§Backlog, cap commercialisation mi-2027) et la **dette technique ouverte** (§Dette). Un item livré quitte le backlog et laisse une **trace datée** dans la carte ou §Livrés ; les ids P*x-y* sont stables, jamais réutilisés.

---

## 1. Contraintes & solveur (cœur métier)

Le modèle cible de contraintes (scopes, types de règles, familles, liste fermée de types) est **entièrement tranché** (série ENGINE, 2026-07-03) — l'ancien doc de détail `contraintes-modele-cible.md` est absorbé ici (décisions dans le tableau ; historique dans git). Restent ouverts uniquement les items ⬜/🟡 en fin de tableau (ajoutés après la clôture).

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Redesign contraintes : 5 scopes (CLUB/CATEGORY/TEAM/COACH/FACILITY) + types HARD/PREFERRED/BONUS/LOCK + 7 familles + liste fermée de types | ✅ | contraintes-v2 | **Tranché (liste fermée actée dans le code)** : scopes CLUB/TEAM/COACH/FACILITY (`ConstraintScope`), types HARD/PREFERRED/BONUS/LOCK (`ConstraintRuleType`), familles TIME/DAY/FACILITY/COACH_AVAILABILITY/FACILITY_CAPACITY (`ConstraintFamily`). Scope CATEGORY + familles ALLOCATION_PRIORITY/DISTRIBUTION **non retenus** |
| Salles **divisibles** + `max_parallel_trainings` (FACILITY_MAX_PARALLEL_TRAININGS) | ✅ | contraintes-v2 | **Tranché** : « Terrain divisible » (`Venue.canSplit`) + capacité par créneau (`VenueTrainingSlot.capacity`, forcée à 1 si non divisible — garde-fou builder S4) ; le parallélisme borné passe par la famille **`FACILITY_CAPACITY`** (`maxTeams` par créneau) plutôt qu'un type `max_parallel_trainings` dédié |
| `allow_shared_court` par équipe (jeunes partagent, seniors non) | ✅ | contraintes-v2 | **Tranché : abandonné** — non retenu au modèle (le partage de terrain passe par `canSplit` + capacité créneau, pas par une règle par équipe) |
| `CLUB_YOUNG_MAX_START_TIME` / `TEAM_MAX_START_TIME` (« jeunes pas après 19h30 ») | ✅ | contraintes-v2 | **Tranché** : couvert par la famille **TIME** (`maxStartTime`/`minStartTime`, appliqué dans `constraints.py`) ciblée via le tag `JEUNE` (`TeamTagService`). Décision : pas de type dédié 1ère classe |
| `CLUB_TRAINING_DURATION_BY_CATEGORY` (durée par défaut par catégorie) | ✅ | contraintes-v2 | **Tranché : abandonné** — la durée reste saisie par créneau, pas de défaut par catégorie |
| `CLUB_SLOT_GRANULARITY` (granularité 30 min) — **discordance** : v3 grille 15 min vs contraintes-v2 30 min | ✅ | contraintes-v2 / v3 §11.1 | **Tranché : granularité fixée à 15 min** (v3 l'emporte sur les 30 min de contraintes-v2) |
| Règle implicite : **coach principal présent à toutes les séances de son équipe** | ✅ | contraintes-v2 | Livré (S2) : seul le coach `MAIN` alimente le no-overlap dur ; l'assistant est optionnel |
| Objectif mou : **repos après jour de match** (+3, `teams.match_day`) | ✅ | v3 §4.3 | Livré (série ENGINE E-feat) : règle implicite `add_match_day_rest_bonus` — pour `matchDay=m`, le jour `m%7+1` reste libre (réifié, poids `rest`=3, non bloquant). Cas dimanche→lundi évité |
| Objectif mou : **regroupement même-coach-même-salle** (+50) | ✅ | v3 §4.3 | Livré (PR #10) sous forme de **bonus de chaînage borné** : sessions back-to-back du même coach récompensées via `CHAINING_TIER_WEIGHTS` (poids < 21 ⇒ ne déplace jamais un placement) ; optimisé en **phase 2** plafonnée à 10 s après verrouillage du placement |
| Niveau 0 — extraction des locks HARD hors solveur | ✅ | v3 §4.1 | **Tranché** : `_extract_hard_locks` (`model.py`) pré-place les slots HARD (`fixed_slots`, forcés hors solveur), `is_team_satisfied_by_hard_locks` retire les équipes déjà couvertes ; slots préservés dans la sortie quel que soit le statut |
| INFEASIBLE D1–D4 : rapport dégradé, diagnostics de conflit, suggestions texte, **relaxation partielle (D4 = P2)** | ✅ | v3 §7 | **Tranché** : D1–D3 livrés (diagnostics précis S5 — équipes/salle/jour+heure nommés, raison du non-placement, indice de sous-capacité INFEASIBLE). **D4 (relaxation auto) écartée par décision** : passe unique sans fallback silencieux (`fallback_used=False`, ADR-0001) → INFEASIBLE renvoie des diagnostics, jamais un plan aux contraintes relâchées en douce |
| Timeout **adaptatif** (60/180/600s selon complexité) | ✅ | v3 §5.1 | Livré (S3) : complexité = équipes × salles → paliers 60/180/600s, plafonné par le budget du payload (650s) |
| Déterminisme : `score_formula_version`, `constraint_version` exposés | ✅ | v3 §4.5 | Livré (PR #11) : `score_formula_version` = `SCORE_FORMULA_VERSION` (**`T24_LEVEL_2_FIXED_WEIGHTS_V7`** au 2026-07-29 — V6 = `avoided_venue`, audit P0.1 ; V7 = poids `spacing`, ALIGN-06. `objective.py` fait foi) + `constraint_version` = version du contrat (`CONTRACT_VERSION`) dans la sortie. ⚠️ Écart spec : livré comme **version de contrat**, pas un SHA1 du payload comme évoqué en v3 §4.5 |
| `COACH_FORBIDDEN_TIME_RANGE` (plage horaire interdite coach, au-delà du jour entier) | ⬜ | contraintes-v2 | COACH_AVAILABILITY ne gère que les **jours** (`unavailableDays`), pas les plages horaires par jour. Config + application engine · **🟡** — même grille que le questionnaire coach (§2, collecte dispos) si celui-ci retient jours×plages |
| Matrice **temps de trajet** entre salles + **passerelles** équipes (U15→U18) — `venue_travel_times`, `team_links`, `category_passway_rules` | ⬜ | v3 §3.4, §4.1 · FF#5 · besoin fondateur 2026-07-17 | Tables absentes ; contraintes engine en stub (`travel_feasibility`, `required_bridge`). Lié au bridage « travel off » (§6) · **🔴** (modèle + solveur) — V2. **Précision 2026-07-17 — comment obtenir la matrice** : le gymnase porte déjà une **adresse** ; en dériver **lat/long** par géocodage, puis calculer les distances. Deux paliers : (a) **vol d'oiseau** (haversine sur lat/long) — zéro dépendance, suffisant pour « ces deux gymnases sont-ils enchaînables ? » ; (b) **temps de trajet réel** (API d'itinéraires) — plus juste mais dépendance externe + quota. **(a) d'abord** : le trajet réel n'est utile que si le vol d'oiseau se révèle trop grossier. Géocodage : l'**API Adresse (BAN, api-adresse.data.gouv.fr)** est gratuite, publique et 🇫🇷 (pas de transfert hors UE, cohérent RGPD) — à confiner comme `FfbbApiClient` (host en dur, jamais dérivé de l'input, redirections coupées, best-effort). Stocker lat/long **sur le gymnase** (géocoder à la saisie d'adresse, pas au solve) : le payload engine reste des nombres, l'engine ne parle à personne (invariant §2 CLAUDE.md) |
| **Matrice contrainte UI↔engine** (famille × ruleType × config → honorée/warning/non-proposée) | ✅ | audit P0.1 ENG-10..13 | **Livré (2026-07-07, audit P0.1).** Fixes ENG-10 (PREFERRED DAY placebo → soft), ENG-11 (préférence gymnase escaladée dure → soft, plus d'INFEASIBLE), ENG-12 (BONUS retiré UI + alias engine ; LOCK+salle = figé), ENG-13 (union indispos coach), + case morte « Toutes les équipes » (expansion backend CLUB→TEAM). **Verrou anti-récidive** : matrice machine `engine/tests/semantic/constraint_matrix.py` → **test paramétré généré** (44 tests) + gel de l'offre UI (Vitest) + `docs/architecture/constraint-matrix.md`. Formule de score V6 (`avoided_venue`) · **✅** |
| **Typage cœur engine** — dataclass `AssignmentVariable` remplaçant `AssignmentLike = Any` dans `constraints.py` | ✅ | audit ENG-05 | **Livré (2026-07-07, Scope A).** `_normalise_assignments` convertit tout en `AssignmentVariable` (helper `_as_assignment_variable`, mêmes alias qu'avant → comportement identique) ; les 14 `add_*` typés `Sequence[AssignmentVariable]`, ~65 accès duck-typing → attribut direct, 9 accessors morts + `AssignmentLike` retirés. **mypy a attrapé 2 vrais bugs de narrowing** (str vs str|None) — la valeur du typage. Scores golden **inchangés** (byte-identiques), smoke COMPLETED. `BoolVarLike`/`RuleCollection` **laissés en alias documentés** (types ortools instables / forme variable — valeur faible, Scope B/C non retenus). `objective.py` garde son propre `AssignmentLike` (module distinct, hors scope) · **🟡→✅ (A)** |
| **PERF solveur — prove-stall sur problèmes denses riches en soft** : BCCL (49 équipes, 55 préférences gymnase soft) solvait en **612 s** — le worker unique **trouvait** l'optimum (score 50258) en ~2 s mais ne le **prouvait** jamais (borne à 1394 % qui ne se resserre pas). **PAS** une régression P0.1 (les préférences gymnase étaient déjà soft avant ; c'était la donnée du seed complet, jamais exercée car masquée par un cache Redis de payload allégé) | ✅ | audit / constaté 2026-07-07 | **Livré (2026-07-07).** `_adaptive_workers` : `num_search_workers` = 1 (≤200 complexité, déterministe) / 8 (au-delà) → **612 s → ~2 s**, optimum **prouvé**, score identique (50258). Diagnostic : profilage bound-vs-value (find-stall écarté, `relative_gap_limit` inefficace car borne trop lâche, forcer les préférences→HARD rejeté : ~24 équipes non plaçables). Assignation non-déterministe à 8 workers mais **valeur d'objectif stable** → goldens (score + nb créneaux) inchangés. Gate perf resserré (dense + **bccl** < 60 s, ratchet anti-régression). Smoke end-to-end : **11 min → 14 s** · **🔴→✅** |
| **Déterminisme exact du plan sur gros clubs** (option, si besoin produit) — restaurer « même entrée+seed → plan exactement identique » au-dessus du seuil 200 | ⬜ | réconciliation initiales §2 (2026-07-07) | Les workers adaptatifs (8 au-delà de 200) rendent l'assignation non-déterministe (valeur d'objectif stable). `interleave_search` seul ne suffit pas (déterministe uniquement avec `max_deterministic_time`, unités déterministes ≠ secondes → refonte du budget timeout adaptatif). À ne faire **que si** un club demande la repro exacte du plan — aujourd'hui jugé non nécessaire (le gestionnaire ajuste). Alternative : exposer un mode « repro exacte » optionnel (1 worker + budget élargi) · **🟡** |
| **Reaper génération bloquée** — cron : schedule coincé `PENDING`/`GENERATING` > N min → `FAILED` + diagnostic explicite | ✅ | constaté 2026-07-07 (worker mort = spinner UI infini) | **Livré** (statut corrigé le 2026-07-29) : `app:schedules:reconcile-stuck` (`ReconcileStuckSchedulesCommand`) bascule le schedule en `FAILED` + diagnostic `stuck_timeout` + publication Mercure — l'UI cesse de poller. Planifié **toutes les 10 min** par `AdminJobCatalog`, seuil `--older-than` **60 min** (et non les 5 min évoqués ici), `--dry-run` pour inspecter. `GenerateScheduleHandler` et `RegenerateController` documentent explicitement ce watchdog comme leur filet quand la transaction est déjà close |
| **Un LOCK HARD court-circuite TOUTES les contraintes — silencieusement** : un créneau de réservation posé sur le jour de congé d'un coach est accepté sans un mot (cas réel : SM2 réservée le jeudi, jour de congé de Nico → la contrainte `COACH_AVAILABILITY` de Nico est **de facto annulée**, aucun diagnostic) | 🟡 | constaté 2026-07-17 (usage club) · axe **constraint semantics** | **Mécanisme** : `_extract_hard_locks` (`engine/app/solver/model.py`) pré-place les slots HARD **hors solveur**, et `build_model` **ne crée jamais la variable booléenne** des `hard_slot_keys` → aucune contrainte du modèle ne s'applique à ces créneaux. Ce n'est pas propre au coach : **un LOCK viole n'importe quelle contrainte** (indispo, plage horaire, capacité…). **Volet 1 LIVRÉ (2026-07-28, PR #317)** — l'option (a) a été tranchée : le verrou **reste souverain** (ALIGN-07 non rouvert), mais il ne se tait plus. `diagnose_locked_slot_violations` croise les verrous avec les contraintes **saisies** (indispo coach, fenêtres horaires, règles de jours unies par équipe, gymnase interdit) et émet un diagnostic **`constraint_not_honored`** INFO nommant la vraie contrainte, l'équipe, le coach, le gymnase, le jour et l'heure ; NR `engine/tests/semantic/test_hard_lock_announces_violations.py`. **Reste trois PR (A/B/C)** — volet capacité, erreur bloquante sur impossibilité physique, prévention au clic — cadrées pour reprise à froid dans [`p2-9-verrous-et-alertes.md`](p2-9-verrous-et-alertes.md), suivi sous **P2-9** au backlog · **🟡** |
| **Binômes de partage définis par le gestionnaire** — aujourd'hui, sur un créneau à capacité > 1, le solveur associe **n'importe quelles** équipes ; le gestionnaire veut **pré-définir les paires autorisées** (ex. deux catégories jeunes compatibles oui, SM1 + U9 non) | ⬜ | besoin fondateur 2026-07-17 · axe **constraint semantics** | ⚠️ **Rouvre une décision tranchée** : la ligne `allow_shared_court` (ci-dessus) avait été **abandonnée** en actant que « le partage de terrain passe par `canSplit` + capacité créneau, pas par une règle par équipe ». Le terrain montre la conséquence de ce choix : la capacité dit *combien*, jamais *avec qui*. La forme demandée ici est **plus forte** qu'un drapeau par équipe (une relation **paire**, pas un booléen) → à cadrer comme un nouveau type de la famille `FACILITY_CAPACITY` (liste de paires autorisées, ou tag de compatibilité par équipe → paires dérivées ; trancher la forme AVANT de coder, c'est ce qui décide du coût). Attention combinatoire : N² paires à saisir pour 40 équipes = inutilisable → **le tag de compatibilité est probablement la bonne forme** (le gestionnaire tague, le moteur dérive). Besoin à valider (§7 CLAUDE.md) avant tout plan · **🔴** (modèle + UI + solveur + NR) |
| **Reverse-engineering des contraintes** — dériver du planning principal des contraintes **PREFERRED** suggérées (agrégées + scorées) pour rendre le calendrier de base reproductible (setup saison N+1 accéléré) | ⬜ | idée club (2026-07-07) | Le gestionnaire garde la main (accepter/rejeter) — réutilise le rail `pendingConstraintSuggestion` (déjà câblé backend↔engine↔front). **Décisions actées** : (1) suggestions **PREFERRED uniquement** — des contraintes HARD dérivées figeraient le plan et neutraliseraient le solveur (régénérer = no-op) ; (2) **agrégation obligatoire** (« équipe X : 4/4 séances mardi → 1 PREFERRED mardi », pas 4 contraintes ponctuelles) + score de confiance ; (3) analyse de slots stockés = **service backend pur, engine intouché**. Familles cibles : DAY (jour récurrent), TIME (créneau habituel), FACILITY (salle d'affinité), COACH (paire coach-équipe). Axe *constraint semantics* → NR requis · **🔴** |
| **Contraintes implicites du solveur — visibilité lecture seule pour le gestionnaire** (à titre informatif, pas configurable) | ⬜ | besoin fondateur 2026-07-20 | Le solveur applique déjà des règles structurelles jamais saisies par le gestionnaire (`engine/app/solver/constraints.py` : `add_room_at_most_one`, `add_coach_at_most_one`, `add_coach_player_non_overlap`, `add_coach_rest_day_constraints`, `add_one_session_per_day_constraints`, etc.), en plus des règles implicites déjà **✅ documentées** plus haut (coach principal, repos après jour de match, regroupement même-coach-même-salle). Aujourd'hui aucune n'est visible côté UI — le gestionnaire ne sait pas ce qui s'applique « gratuitement ». Demande : un écran/panneau **lecture seule** qui les liste, sans les rendre éditables (ce n'est pas une nouvelle famille de contrainte, juste de la transparence). Non cadré : où l'afficher (page contraintes ? wizard ? tooltip par diagnostic ?), et surtout **quelles règles méritent d'être montrées** — certaines sont des détails d'implémentation sans valeur pour un non-technicien (ex. `add_age_ascending_constraints`), d'autres directement utiles (non-chevauchement coach, repos après match) — trier avant tout cadrage · **🟡** |

---

## 2. Modèle temporel & périodes d'exception

Grosse zone quasi entièrement à faire — l'appli ne gère aujourd'hui qu'un plan de base hebdomadaire.

> **📖 Référence produit des 3 types de planning (socle / overlay / reprise), validée 2026-07-12 :
> [`types-de-planning.md`](../courantes/types-de-planning.md)** — déclenchement, manipulation, règle
> « la semaine est l'unité hors socle ». Écarts E1-E4/E6 **livrés** (cf. §Livrés) ; reste E5
> (modale « Demandes des coachs », suivi sous P2-1).
>
> **⭐ Approche tranchée — voir [`accueil-cockpit-temporel.md`](../courantes/accueil-cockpit-temporel.md).** Cette
> spec **challenge et remplace** la stratégie « matérialiser d'abord » de la vision d'origine : l'accueil
> devient un **cockpit** (bandeau socle · calendrier d'exceptions · radar), le calendrier est une
> **projection** et non une matérialisation J+14, une **occurrence n'existe qu'en delta/override**, et
> les 4 tables `period_*` se réduisent à **une entité `CalendarEntry`** (+ réutilisation de `Constraint`
> et `Schedule`). Résultat : cette §2 **cesse d'être un monolithe 🔴** et se livre en **paliers A/B/C**
> (cockpit projeté → overlays de période → collecte coach). Les 🔴 ci-dessous sont donc à relire à
> travers cette approche.

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Architecture **templates → occurrences** (dates réelles, matérialisation glissante J+14) | ⬜ | v3 §3.5, §8.1 · FF#8 | **♻ Repensé (cockpit §3)** : matérialisation J+14 **abandonnée** au profit d'une **projection + occurrences éparses (deltas)**. `schedule_slot_occurrences` ne stocke, si conservée, **que les overrides**. **Cesse d'être le prérequis 🔴** de toute la §2 |
| Occurrences : exceptions, annulations, déplacements, remplacements coach/salle, fusions | ⬜ | v3 §3.5 · FF#8 | **♻** Grain fin déjà couvert par `ManualEditController` (`/manual-edit/one-time` + `temporaryLock`) ; une table d'occurrences éparses ne s'ajoute **que si** le besoin fin le justifie (palier B/C) · **🟡** |
| **Périodes d'exception** (`period_templates` + `is_cutoff`, `period_template_slots` avec `team_ids[]`, `period_assignments`, `period_coach_responses`) | 🟡 | v3 §3.6, §8 · FF#2 | **♻ = `CalendarEntry` `kind=period`** (cockpit §4, §9ter) : `periodType` + contraintes datées (`Constraint.calendarEntryId`) + overlay (`Schedule.calendarEntryId`). **Livré palier B (PR-4/5) pour closure + holiday** : overlay généré via wizard mode période, fermeture = additive + expansion `forbiddenVenueId`, vacances = remplaçante partielle. **Différés** : `cutoff` (fenêtre vide, pas d'overlay) et `mutualisation` (`team_ids[]` fusionnés) — voir lignes dédiées ci-dessous |
| Scheduler quotidien @8h : détecte période sous 14j → alerte (J-7 rappel, J-3 rouge), jamais d'auto-action | ✅ | v3 §8.2 · FF#2 | **Livré (PR-6 palier C)** : commande `app:periods:remind` (cron quotidien prod) — période sans overlay démarrant à J-14/J-7/J-3 → email aux gestionnaires (owner+admin), sujet 🔴 à J-3, lien cockpit (`FRONTEND_BASE_URL`). Jamais d'auto-action (le radar in-app reste la vue). `--dry-run`/`--date` pour rejeu |
| **Plans secondaires / alternatifs** (vacances, coupure, mutualisation) | 🟡 | v3 §8.1 · FF#2 | **♻ = overlays de période bornés** (cockpit §6). **Livré palier B (PR-4/5)** : `Schedule.calendarEntryId` = overlay (version d'un plan de période, jamais du plan SEASON), généré via wizard mode période (structure R/O, contraintes de période actives), suppression centralisée `OverlayManager`, reopen destructeur du calendrier de saison (409 + confirm). closure + holiday seulement. **+ versions d'overlay (2026-07-11)** : plusieurs versions par période (V1, V2…) + validation/reopen d'overlay + purge `app:overlays:purge` — voir [`adr-0002-pattern-plan.md`](../../docs/architecture/adr-0002-pattern-plan.md) + [`planning-lifecycle-validated.md`](../courantes/planning-lifecycle-validated.md) |
| **Overlay `periodType=cutoff`** (coupure = pas d'entraînement) | ✅ | v3 §8.1 · FF#2 | **Livré (reliquats cockpit, 2026-07-06)** : création via DayDialog (« Coupure (pas d'entraînement) »), icône 🛑 sur le calendrier, rappel radar **sans CTA** (fenêtre vide = rien à générer, jamais de wizard). Entrée `CalendarEntry` nue : ni contrainte datée, ni overlay. Purement informatif tant qu'aucune projection ne consomme les dates (PDF daté, etc.) |
| **Overlay `periodType=mutualisation`** (`team_ids[]` fusionnés) | ⬜ | v3 §3.6 · FF#2 | **Différé palier C.** Ex. : « pendant la coupure de janvier, SM1 + SM2 s'entraînent ensemble le mercredi 20h sur un seul créneau » → nécessite la fusion `team_ids[]` côté solveur (créneau partagé). Gros morceau moteur + UI · **🟡** |
| **DayDialog « Créer une période… »** (période générique `custom`) | ⬜ | — | **Différé palier B/C.** Ex. : « poser une période custom du 10 au 20 mars, ni fermeture ni vacances » → `periodType=custom` non générant aujourd'hui → 422 à la création d'overlay = impasse UX. **Mitigation livrée** (bouton désactivé + tooltip, gardé par test DayDialog). Reste : activer quand `custom` devient générant · **🟢** |
| **Plan de vacances éditable + collecte des demandes coach** (structure de période éditable · lien coach sans login · écran demandes) | ✅ | v3 §8.2 · FF#2 | **Livré en deux temps ; fichier de détail supprimé le 2026-07-29 (gradué, cf. §Livrés).** Le terrain avait **corrigé** la lecture initiale « dispos coach → contrainte dure » : c'est une **négociation** — le coach émet un **souhait**, le gestionnaire **arbitre et tranche**, le lien n'écrit jamais de contrainte (invariant tenu : zéro effet solveur). **P1 structure de période éditable** (#8, 2026-07-24) — ⚠ le modèle retenu **n'est pas** l'override `calendarEntryId` en copie-sur-édition qui avait été spécifié : la période **possède sa grille** (créneaux copiés à la naissance du plan, clé `schedulePlanId`, **jamais d'union** avec le saisonnier — décision fondateur), et seuls les **jumeaux sparse** `TeamPeriodOverride` (activation + séances) et `ConstraintPeriodOverride` (toggle) sont des diffs. **P2 collecte coach C1/C2/C3** (#10, 2026-07-25/26) : todo-list, campagne tokenisée sans login, emails + digest + relance. **Mutualisation** par réservation sur créneau à capacité 2 — **zéro changement engine**, tenu de bout en bout. Reste optionnel, non demandé : flux guidé « reprise progressive » (P2-1), gymnases dispo-seulement-vacances (patron extensible à `Venue` sans rework). → [`types-de-planning.md`](../courantes/types-de-planning.md) types 2/3 + E4/E5 · [`backend-inventory.md`](../courantes/backend-inventory.md) §2 · [`adr-0002-pattern-plan.md`](../../docs/architecture/adr-0002-pattern-plan.md) inv. 5 amendé |
| Mutualisation de créneau (SM1+SM2 même créneau) | ✅ | v3 §3.6 · FF#2 | **Tranché : via réservation, pas d'engine** — salle divisible (`canSplit`) + capacité 2 sur le créneau + réservation des 2 équipes → le solveur place les deux (couvre le partiel : 1 des 2 créneaux partagé, l'autre solo). Le `periodType=mutualisation` dédié + la fusion `team_ids[]` moteur sont **abandonnés** (inutiles). Détail : [`backend-inventory.md`](../courantes/backend-inventory.md) §2 (`Reservation`) |
| `school_holiday_periods` (API Éducation Nationale, zones A/B/C) | ✅ | v3 §3.3 | **Livré (PR-2 cockpit palier A) — gradué dans [`../courantes/vacances-scolaires-jours-feries.md`](../courantes/vacances-scolaires-jours-feries.md)** (table globale, seed, `GET /api/school-holidays`, dérivation zone FFBB) |
| **MAJ automatique des vacances via l'API calendrier scolaire** (1×/saison) | ✅ | [API data.gouv](https://www.data.gouv.fr/dataservices/api-calendrier-scolaire) | Import livré (`app:school-holidays:import`, PR #62), **planification trimestrielle SA3-C** puis **relance superadmin SA3-D** ; vérité courante dans [`../courantes/vacances-scolaires-jours-feries.md`](../courantes/vacances-scolaires-jours-feries.md) et [`../courantes/superadmin-auth.md`](../courantes/superadmin-auth.md). · **🟢** |
| **Calendriers vacances DOM/TOM** (hors zones A/B/C métropole) | ✅ | — | **Livré avec l'import ci-dessus — gradué dans [`../courantes/vacances-scolaires-jours-feries.md`](../courantes/vacances-scolaires-jours-feries.md)** (13 codes de zone, calendriers territoriaux spécifiques). Territoires non publiés côté API certaines années → saisie manuelle en attendant |
| **Jours fériés au calendrier** (afficher les fériés dans le cockpit) | ✅ | [API etalab jours-feries](https://calendrier.api.gouv.fr) | **Livré — backend (`app:public-holidays:import` + `GET /api/public-holidays`, PR #63) + rendu cockpit (pastille jour exact + rappel radar ≤ 30 j sans CTA, reliquats cockpit 2026-07-06) — gradué dans [`../courantes/vacances-scolaires-jours-feries.md`](../courantes/vacances-scolaires-jours-feries.md)** (y compris hors périmètre assumé : Alsace-Moselle, 977/978, fallback offline). Le pont reste une décision gestionnaire (`periodType=closure`) |
| **Vue calendrier annuel** (menu) — voir tous les plannings prévus sur l'année pour repérer les soucis + **vacances scolaires en cours** | 🟡 | — | **♻ = le cockpit lui-même** (cockpit §5), **livré et devenu la page d'accueil** (statut corrigé le 2026-07-29) : `MonthCalendar` rend la couche d'exceptions, les **vacances de la zone** (fond ambre) et les fériés, `RadarPanel` porte les rappels. **Seule réserve** : la vue est **mensuelle navigable**, pas un panorama **12 mois d'un coup** — à rouvrir seulement si « voir l'année entière » est réellement demandé · **🟢** |
| **Régénération partielle guidée** (`PartialRegenService` — salle fermée / coach indispo → slots affectés identifiés + regen ciblée) | ⬜ | v3 §6.2, §14.2 · FF#1 | **♻ Partiellement couvert par les overlays** : une période (fermeture/vacances) génère un overlay borné (palier B livré) sans toucher le plan de base ; reste la regen **ciblée** du plan de base lui-même (hors période) — à requalifier quand un besoin réel se présente · **🔴** |

---

## 3. Onboarding & saisons

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Wizard initial de saisie | ✅ | v3 §9.1 | Livré (6 étapes, reconstruit) — dépasse le draft 4 étapes |
| Auto-save serveur du brouillon (`GET/PUT /api/clubs/{id}/draft` + `clubs.draft_data`) | ✅ | v3 §9.1 · BG G1/G2 | **Tranché : abandonné** — la persistance **par entité** (chaque salle/équipe/coach POST/PUT à la saisie, le store wizard ne tient aucune donnée) couvre déjà le besoin ; un draft-blob serait une 2e source de vérité. Ferme BG G1/G2 |
| **Mode / compte démo** (club fictif pré-rempli, génération 30s avant saisie) | ⬜ | v3 §9.1, §12.3 | **Besoin à spécifier → [`compte-demo.md`](compte-demo.md)** (vendeur vs prospect self-service, options A/B/C, anonymisation RGPD, flag `isDemo`). **🟡** — **fort levier de vente** |
| **Refonte enregistrement** (vérification gestionnaire via code/document FFBB + approbation par email si le club existe déjà) | ⬜ | v3 §9.1 · FF#19 | **Besoin à spécifier → [`enregistrement-ffbb.md`](enregistrement-ffbb.md)** (code vs doc FFBB, preuve de rôle, 1er gestionnaire, anti-squatting). **Croise A5/A6** (gate `/api/club_users`) et l'import FFBB §8. **🟡/🔴** |
| **Transition de saison** (copie éditable des entrées N→N+1 · multi-saison simultané · bascule mi-juillet non destructive · revue guidée) | ✅ | v3 §9.2 · FF#3 | **P1 livré intégralement (PR-1/2/3, 2026-07-06)** (fichier de détail supprimé le 2026-07-08, livré) : (PR-1) résolution multi-saison (`SeasonResolver` pivot 15 juillet, `SeasonFilter`, `X-Season-Id` validé, `/api/me` `seasons[]`). (PR-2) `SeasonTransitionService` (copie entrées N→N+1, remaps, `parentConstraintId`, permanentes seules) + `POST /api/seasons/{id}/transition` + sélecteur frontend. (PR-3) **readonly serveur** (`SeasonAccessGuard` 409 : `AbstractStateProcessor` + `SeasonReadonlyGuardListener`/`SeasonScopedWriteInterface`) + bannière lecture seule + **purge CLI `app:seasons:purge`** (rétention courante+N-1+futures, purge N-2). **Reste = P2 (⬜)** : revue guidée par type + re-datation des événements club + alertes d'anticipation mi-mai→mi-juillet. **🟡** (P2) |
| Capitalisation des contraintes entre saisons (copie éditable, `parent_*_id` self-FK) | ✅ | v3 §3.1, contraintes-v2 §3 · FF#3 | **Livré (transition PR-2)** : `Constraint.parentConstraintId` ajouté + les contraintes **permanentes** sont copiées N→N+1 par `SeasonTransitionService` (`scopeTargetId` + config remappés sur les entités copiées, filiation `parentConstraintId`) |
| **Transition de saison — P2 (revue guidée + confort)** | 🟡 | v3 §9.2 · FF#3 | **Suite du P1 livré — quasi bouclé, seul (4) reste.** Contenu (P2) : (1) **wizard de revue guidée** — **ABANDONNÉ (décision utilisateur 2026-07-07)** : le gestionnaire connaît son club (départs coachs, équipes dissoutes) avant l'outil ; l'édition libre via l'assistant existant + récap suffit. À rouvrir seulement sur signal pilote. (2) **re-datation des événements club** ✅ **livré (P2-PR1, 2026-07-07)** : dialog « Reconduire les événements » après « Préparer la saison suivante » — garder (coché) + date suggérée +364 j (même jour de semaine), création dans le brouillon via l'API calendar_entries standard (X-Season-Id explicite, zéro endpoint/migration) ; sautable, ré-ouvrable en relançant « Préparer » tant que N+1 est sans event. + **fix du 409→bascule** (ky 2.x : body d'erreur consommé → error.data/serverBody stashés ; existingSeasonId était illisible). (3) **alertes d'anticipation** ✅ **livré (P2-PR2, 2026-07-07)** : cron `app:seasons:remind-transition` (J-61/J-30/J-14 avant le pivot 15 juillet, dédup `transition_reminder_log`, patron `app:periods:remind`) + bannière permanente `SeasonTransitionBanner` (CTA → ConfirmDialog partagé via `transitionUiStore`). Silence dès que N+1 existe. (4) à décider : report des **tags custom** entre saisons (bloqué par la dette `TeamTagService`, cf. ligne team_tags). **Dépend du P1 (livré).** · **🟡** |
| Politique de rétention (2 saisons max, saison N-1 read-only, purge RGPD, `solver_metrics` 6 mois) | 🟡 | v3 §3.2, §9.3 | **Livré (transition PR-3)** : fenêtre glissante courante + N-1 readonly + futures, **purge N-2** via `app:seasons:purge` (manuelle, `--dry-run`/`--date`/`--club`), readonly serveur (`SeasonAccessGuard` 409). **Réglé (SA2-stats 2026-07-18)** : `solver_metrics` est **append-only**, rétention **≥ 13 mois** sans cron de purge (décision fondateur — la purge 6 mois contredisait les tendances d'usage sur une saison) · **🟢** |
| **Réinitialiser son club** (RAZ des données pour repartir de zéro) | ✅ | — | **Livré** (statut corrigé le 2026-07-29 : la modale manquante existe) — section « danger » de `ClubPage` : bouton « Réinitialiser le club » + `ConfirmDialog` qui **nomme ce qui part** (équipes, gymnases, coachs, contraintes, plannings) et ce qui reste (le club et les comptes membres), puis retour au wizard. Back gaté **rôle management** *et* saison archivée (409, `SeasonAccessGuard`), tracé à l'audit. **Reliquat cosmétique** : la route s'appelle encore `DELETE /api/reset-season` alors que le geste rendu est « reset club » |

---

## 4. Édition manuelle (boucle de travail)

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| **Cycle de vie planning** : choisir la version qui fait foi (lecture seule), rouvrir, planning principal vs secondaires | ✅ | — | Livré (PR #15), **rebâti par la bascule ADR-0002 (2026-07-16)** : `/validate` (= le plan **pointe** la version + **supprime** les sœurs) · `/reopen` (= **dépointe**), verrou serveur sur les 4 chemins d'édition (clé sur le pointeur, plus sur un statut), modale de responsabilité. `/set-baseline` **supprimé**. Cascade principal→secondaires reportée (dépend des occurrences). → [`planning-lifecycle-validated.md`](../courantes/planning-lifecycle-validated.md) |
| Niveaux de lock NONE/SOFT/HARD (enum, ≤30 min = SOFT silencieux) | ✅ | v3 §3.5, §11.4 | **Tranché : SOFT abandonné (ENG-21).** L'enum `LockLevel` garde ses 3 cas, mais `ManualEditController` **refuse SOFT par un 400** (« SOFT is a placebo — the engine never reads the soft-lock penalty ») : seuls NONE et HARD sont posables. La règle « ≤30 min = SOFT auto » **tombe avec lui** — poser automatiquement un verrou sans effet solveur serait exactement le silence qu'a soldé la série ENGINE. Statut corrigé le 2026-07-29 (la ligne demandait de câbler ce que le code rejette) |
| **Dialogue post-modification** (déplacement significatif → créer contrainte permanente / lock HARD / occurrence one-time « convertir en contrainte ? ») | 🟡 | v3 §11.4 · FF#4 | **Le manque est l'UI, pas le back** (revérifié 2026-07-29, la note disait l'inverse) : `ManualEditService::applyPermanentConstraint` existe et `POST /api/schedule-slots/{id}/manual-edit/constraint` répond « Permanent constraint created. » — mais **aucun appelant frontend** (`features/planning/api.ts` n'appelle que `/manual-edit/lock` et `/one-time`), donc l'endpoint est **mort côté produit**. Reste le **dialogue lui-même** + `source_occurrence_id`. Mention « lock SOFT » retirée : SOFT est refusé (cf. ligne niveaux de lock) · **🟡** (le `source_occurrence_id` dépend des occurrences 🔴) |
| **Alerte diagnostic cliquable → focus dans la grille** (boucle d'ajustement) | 🟡 | | **« Naviguer » est livré (#180) ; « corriger sur place » reste** (suivi P2-2) — statut corrigé le 2026-07-29. Des **3 gaps** recensés le 2026-07-06 : (1) `unused_slot` pointait sur du néant → **résolu**, `DiagnosticsPanel` surligne les créneaux vides du gymnase concerné et `PlanningPage.focusVenue` bascule en vue gymnase filtrée sur lui ; (3) la grille ne rendait pas les créneaux vides → **résolu** (`lib/emptySlots.ts`, cellules « vide »). **Reste** (2) le scroll-into-view, et surtout le fork tranché du côté **« corriger sur place »** : glisser une équipe dans un créneau vide. **⭐ Insight clé inchangé** : c'est **la même brique** que la Grille de réservation ci-dessous — une grille interactive à créneaux vides, **avant** génération (LOCK HARD) et **après** (réparer). · **🟡** — **transforme le rapport en outil de travail** |
| `ScheduleDiffService` (diff lisible entre 2 snapshots) | ⬜ | v3 §6.2 · FF#11 | **🟡** (les snapshots existent déjà — `snapshot_data`) |
| **Grille de réservation** (étape Contraintes, pré-génération) : grille type disponibilités, **clic = affecter une équipe** à un créneau ; affiche les **créneaux déjà placés** en base (`schedule_slot_templates`) | ⬜ | — | Remplace la liste déroulante actuelle (`ReservationPanel`). Réutiliser le style `VenueAvailabilityGrid`. Idée gestionnaire (recette) — les réservations posées ⇒ HARD locks à la génération · **🔴** (nouvelle grille interactive). **⭐ Partage son primitive avec la « boucle d'ajustement » §4** (grille + créneaux vides + clic=affecter) — à spécifier ensemble le jour venu : même brique, deux moments (pré vs post génération) |
| Routes manual-edit documentées (OpenAPI) + naming `schedule-slots` vs `schedule_slot_templates` | ✅ | BG G5/G6 | **Livré (PR #43, BCK-F)** : `CustomRoutesOpenApiFactory` documente les 3 routes `manual-edit` ; G6 tranché **snake_case canonique** (routes custom gardent leur kebab-case) |

---

## 5. Multi-tenant, sécurité, concurrence

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Isolation 3 couches (ClubVoter + TenantFilterListener + RLS FORCE) | ✅ | v3 §10.1 | Livré (listener priorité 7) |
| Verrou de génération (1/club, Messenger + asyncio.Lock engine) | ✅ | v3 §5.1 | `ConcurrentGenerationTest` |
| Snapshot gelé (`snapshot_data` + `snapshot_hash`) | ✅ | v3 §10.2 | |
| **Optimistic locking** (`version` + 409 sur conflit) | 🟡 | v3 §10.2 | `version` exposé ; gestion 409 à généraliser/vérifier · **🟡** |
| **Rate limiting** (api 200/min, generate 5/h, pdf 10/h par club) | 🟡 | v3 §10.2 | **Le gros est livré (SEC-11)** — la ligne disait « register limité seulement », faux (corrigé le 2026-07-29) : `config/packages/rate_limiter.yaml` porte le throttle `api` **par UTILISATEUR** (clé JWT) appliqué sur `^/api` par `ApiRateLimitSubscriber`, en plus des limiteurs **par IP** des portes publiques (register, verify, password-forgot, admin-auth, collecte coach) et de `rgpd_export`. Gardé par `ApiRateLimitTest`, **dans le gate bloquant de la CI**. **Restent** les deux caps métier — `POST /generate` et l'export PDF par heure — **et la clé par CLUB**, là où l'existant borne par utilisateur · **🟢** |
| **Deux users DB** (app_user sans DDL + migration_user) | ✅ | v3 §10.1 | **Livré** (vérifié le 2026-07-29) : `docker/postgres/init/02-users.sh` crée `app_user` (NOSUPERUSER, DML seul, **aucun DDL**) et `migration_user` (DDL), mots de passe **fail-closed** — l'init avorte si les variables manquent, plutôt que de figer à vie un mot de passe de dev (initdb.d ne rejoue jamais). ⚠ **Réserve de vérité** : les migrations passent en fait par la connexion `admin` = `clubscheduler` (`DATABASE_ADMIN_URL`), donc **`migration_user` est créé et jamais utilisé** — soit l'y câbler, soit le retirer de l'init. Pas un trou de sécurité (`clubscheduler` est la porte superadmin documentée), une incohérence entre le script et l'usage |
| CacheInvalidationListener ciblé (Venue/Coach/Team/Schedule) | 🟡 | v3 §6.2 · FF#9 | Stub actuel · **🟡** |
| `ClubTimeService` (UTC ↔ fuseau club) | ⬜ | v3 §6.2 · FF#10 | **🟡** (transverse — toucher tout affichage horaire) |

### Rôles & membres (non couvert par les initiales — issu du hors-scope wizard)

| Évolution | Statut | Note |
|-----------|--------|------|
| **Rôles non-admin** (coach / lecteur au-delà d'`admin`) + modèle de permissions | ⬜ | Aujourd'hui `ClubUser.role` est **hardcodé `'admin'`** au register ; pas de différenciation de droits · **🔴** (modèle de permissions + voters à câbler partout ; `isManagementRole` existe déjà comme amorce) |
| **Gestion des membres** (inviter / changer rôle / désactiver) — écran admin club | ⬜ | `ClubUser` (membership) existe ; pas d'UI · **🟡** (CRUD membership + écran ; dépend en partie des rôles) |
| **Approbation des demandes d'adhésion** (rejoindre un club → membre `pending`) | ✅ | Flux register → membre pending + `WaitingApprovalPage` côté demandeur ; approbation admin livrée (`PendingMembersSection`, approuver/refuser), désormais section **Demandes** du hub `/club` (PR #36) |
| **Modifier ses informations personnelles** (nom, email, mot de passe) | ✅ | **Livré** (statut corrigé le 2026-07-29) : `PATCH /api/me` (prénom/nom/email) + `POST /api/me/password` (mot de passe courant exigé) dans `AuthController`, écran `features/profile/ProfilePage.tsx` — changer l'email **déconnecte** (l'identité *est* l'email, il faut se reconnecter avec la nouvelle). NR `ProfileEndpointsTest` |

---

## 6. Pricing & bridage (business-critique)

Zone entièrement à faire — aucun bridage plan aujourd'hui.

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| 4 plans tarifaires (Découverte/Petit/Club/Grand) : max_teams/venues/generations, prix en DB | 🟡 | v3 §12.1 | **Modèle livré** (ADR-0002 lot A, 2026-07-12 ; statut corrigé le 2026-07-29) : l'entité **`SubscriptionPlan`** — ex-table `plan`, renommée pour libérer le nom au profit de `SchedulePlan` — porte `maxTeams`/`maxVenues`/`maxGenerations`/`monthlyPrice`/`annualPrice`/`features`, avec repository, ressource API et provider ; `Club.planId` la pointe. **Restent** le **seed des 4 offres** et tout **enforcement** (lignes ci-dessous) · **🟡** |
| Bridage **Découverte** (freemium) : cap sur le **nombre de générations**, read-only à l'épuisement | ⬜ | v3 §12.2 | **⭐ Besoin spécifié — voir [`bridage-freemium-decouverte.md`](bridage-freemium-decouverte.md).** Cadré au terrain : **club complet, aucun cap d'entité** (sinon le solveur paraît nul, tue le wow) ; gate = **`POST /generate` plafonné (~4)** ; **générer décompte, ajuster gratuit** (la work-loop sépare déjà solve/édition) ; **compteur total non rechargeable** (reset superadmin) ; **pas de limite de temps** ; **PDF export off** ; **read-only** à l'épuisement (réutilise le verrou serveur des 4 chemins d'édition), pas lockout ; **default freemium**, choix d'offre à la conversion. **Enforcement = 3 gardes, PAS transversal** (le modèle génération dissout le 🔴 d'origine). **🟡** (au lieu de 🔴) |
| Enforcement `generation_count` — compteur **total non rechargeable** (freemium) | ⬜ | v3 §3.2 | 1ʳᵉ brique du bridage : garde dans `GenerateScheduleController`. ⚠ `generation_count_season` existe mais se **remet à zéro par saison** → le freemium a besoin d'un compteur **total** qui ne recharge jamais (nouveau champ ou variante) · **🟢** — détail : [`bridage-freemium-decouverte.md`](bridage-freemium-decouverte.md) §4 |
| `billing_cycle` / `plan_expires_at` | ⬜ | v3 §3.2 | Modèle · **🟢** (champs déjà sur `Club` — reste la logique d'expiration) |

---

## 7. Tests

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| 4 tests bloquants CI (TenantIsolation, TenantCache, ConcurrentGeneration, ContractSchema) | ✅ | v3 §13.2 | Livrés + CI verte. Le gate bloquant a **beaucoup grossi** depuis les 4 d'origine (isolation saison, saison archivée, RLS, durcissement Mercure, rôle management, throttle API, superadmin, périmètre engagé, naissance des plans de période…) — **liste canonique : CLAUDE.md §4**, jamais recopiée ici (elle bouge à chaque axe structurant gardé) |
| Golden datasets (simple/medium/dense/impossible/**vacation_week**/**partial_regen**) | 🟡 | v3 §13.5 | Base présente ; vacation_week/partial_regen à vérifier · **🟡** (vacation_week dépend du modèle occurrences 🔴) |
| Invariants Hypothesis (no double-booking, coach unique, coach-joueur cohérent, HARD préservé, tier-S jamais sacrifié si D placé) | 🟡 | v3 §13.5 | Partiel · **🟡** |
| Tests unitaires frontend (Vitest + RTL) | 🟡 | v3 §13.3 · FF#12 | Livrés depuis (wizard/planning testés) — FF à requalifier · **🟢** (compléter la couverture existante) |
| E2E Playwright (onboarding→génération→cockpit) | ✅ | v3 §13.3 · FF#13 · audit P0.2 | **Livré (2026-07-07, audit P0.2 FRT-05/07/11).** Suite 9/9 verte, câblée en **CI** (job `e2e`, needs blocking-tests, stack docker complète + Vite). `journey.spec` = LE parcours produit réel : register → wizard complet (équipe/gymnase+créneaux/coach/contraintes/récap) → **génération CP-SAT réelle** → planning placé → validation (le plan pointe la version) → cockpit. auth.spec réparé (assertions périmées). Flakiness rate-limiter réglée (limite dev bornée). Reste optionnel : PDF dans le parcours · **✅** |
| Gate perf (dense_club) | ✅ | v3 §13.6 | **Livré (série ENGINE)** : gate perf CI `engine-perf` (main only), budget **60 s** (`test_perf_dense.py`, resserré vs les 180 s de la spec initiale pour attraper la régression prove-stall à 612 s) |
| **Simulateur d'horloge (dev)** — piloter la date/heure de l'app pour rejouer les bascules annuelles (pivot saison 15/7, rappels cron, readonly N-1) | ✅ | outillage (2026-07-08) | **Livré (PR #114)** : `ClockInterface` (Symfony Clock) aliasé sur `SimulatedClock` **en dev seulement** (prod = horloge native, inchangée) ; instant épinglé en Redis (partagé web ↔ cron-runner) ; endpoint `/api/dev/clock` gardé `%kernel.debug%` ; widget dev à côté du nom du club. Temps métier routé : `SeasonResolver` + crons rappels (`reconcile-stuck` reste sur l'heure réelle). **Couverture partielle — cf. ligne suivante** |
| **Horloge simulée — le « jour courant » de l'UI ne la suit pas** : changer la date du simulateur ne déplace **ni le repère « aujourd'hui » du planning, ni les CTA/événements du cockpit** qui en dépendent. La bascule de saison et les crons de rappel sont routés (PR #114), mais tout ce qui dérive « aujourd'hui » **côté front** (et le code serveur qui construit les cartes du radar) lit vraisemblablement l'heure réelle | ⬜ | constaté 2026-07-17 (usage démo) | **Symptôme** : impossible de rejouer une situation datée pour une démo ou une recette — on voit la bonne saison mais le mauvais jour. **À faire** : recenser les lectures d'heure hors `ClockInterface` (front `new Date()`, backend hors service Clock), router le repère « aujourd'hui » du planning + le calcul des CTA/radar sur l'horloge simulée en dev. **Impact démo direct** (P4-16) : c'est ce qui permet de montrer un cockpit vivant à une date choisie · **🟢/🟡** (recensement puis câblage) |

---

## 8. Imports & intégrations

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Import Excel équipes (template basket) | 🟡 | v3 §9.1, §12.3 | Backend livré (`POST /clubs/{id}/import-teams`, `FfbbExcelImporter`) ; **UI wizard différée** — l'API FFBB à venir remplacera l'import manuel · **🟢** (brancher l'UI sur l'endpoint existant) |
| Import CSV salles & coachs | ✅ | v3 §9.1 | **Tranché : abandonné** — peu de lignes (1-5 salles, ~10 coachs), pas de format standard ; la saisie manuelle suffit |
| **Fermetures de salle** (`VenueClosure` date+raison → regen partielle) | ✅ | v3 §3.4 · BG G3 | **Livré** (palier B, puis granularité JOUR le 2026-07-18 — cf. §Livrés P2-5 5b ; statut corrigé le 2026-07-29). **Pas d'entité `VenueClosure`** : une fermeture est un `CalendarEntry` `kind=period`, `periodType=closure` posé au `DayDialog` du cockpit (titre structuré `{gymnase} — {raison}`), qui porte sa contrainte datée `FACILITY` et son overlay borné. `VenueClosureDays` ne retire les créneaux du gymnase que ses **jours réellement fermés** (incident ∩ fenêtre) |
| Connecteurs **API municipales** (`venues.source` manual/municipal/external_api + `external_ref`) | ⬜ | v3 §1.4 · FF#20 | V2 · **🔴** |
| Import équipes **FFBB** (code club, `ffbb_team_id`) | ⬜ | v3 §1.4 · FF#19 | V2 · **🟡** (infra `ffbb_team_id`/`ffbbClubCode` déjà anticipée) — **supprime la moitié de la saisie** |
| Import **calendrier de matchs FFBB** (`competitions`, `ffbb_club_code`) | 🟡 | v3 §1.4 · FF#19 | **Livré PR-4 module matchs (2026-07-07) — SUR FORMAT SUPPOSÉ.** `FbiFixtureImporter` (patron `FfbbExcelImporter`) : un export FBI par équipe, équipe choisie à l'upload, `POST /api/teams/{id}/fixtures/import` + `ImportFbiDialog` dans `/matchs`. HOME/AWAY par nom de club (derby = erreur explicite), idempotence par `Fixture.externalRef` (n° FBI), Division → Competition find-or-create, statut UNPLACED (l'heure FBI préremplit `kickoffTime`), Salle ignorée (annuaire = palier B). ⚠ **Reste 🟡 tant que le format n'est pas validé contre un vrai export FBI** (aucun fichier réel disponible — colonnes actées : Division/Numéro/Équipe 1/Équipe 2/Date/Heure/Salle). Update de re-programmation au re-import = évolution. Distinct de l'annuaire adverse (heures/positions, effet réseau) · **🟡** |
| **Planification des matchs** (placement domicile + radar de conflits + dérogations) | 🟡 | v3 §1.4 · FF#21 | **⭐ Voir [`gestion-matchs-ffbb.md`](gestion-matchs-ffbb.md). Palier A en cours.** **PR-1 socle backend livré (2026-07-06)** : entités `Competition`/`Fixture` (season-scoped, API Platform, amical = competition null, statut UNPLACED→…→VALIDATED), service **`MatchFootprint`** (empreinte-temps : domicile 2h15 = 30 échauffement + 1h45 ; extérieur + 30 douche + 15 battement + trajet paramétré — §4bis), **catalogue-ligue `LeagueMatchWindow`** (table globale hors tenant, seed AURA §6bis, `app:league-windows:seed`, `GET /api/league-match-windows`), dérivation ligue via **`LeagueResolver`** (préfixe `ffbbClubCode`) + `Club.league`. `Team.matchDay` conservé (superseded plus tard). **PR-2 moteur de conflits livré (2026-07-07)** : service pur **`MatchConflictDetector`** + `GET /api/fixtures/conflicts` (à la volée, rien persisté), **périmètre coach seul via `TeamCoach`** — `MATCH_MATCH` (deux matchs d'un coach qui se chevauchent) + `MATCH_TRAINING` (match↔entraînement lu dans le planning **effectif à la date** : overlay période ACTIVE sinon la version choisie du plan SEASON, créneau projeté sur le jour du match). **PR-3 grille week-end UI livrée (2026-07-07)** : feature frontend `features/matches/` (route `/matchs`) — grille datée week-end-centrique (bloc = empreinte 2h15), pose domicile clic→panneau (`PUT` statut PLACED) avec **envelope-ligue** (garde HARD si l'équipe mappe une fenêtre, sinon repère indicatif), saisie manuelle (`POST /api/fixtures`, amical = competition null), radar de conflits affiché ; Vitest + e2e Playwright. **PR-4 import FBI livrée (2026-07-07, format supposé — cf. ligne FF#19)**. **Reste palier A** : volet **joueur** (`CoachPlayerMembership`) + `Team.preferredMatchWindow`. **Paliers B** (dérogation + trajet + annuaire adverse global) **/ C** (effet réseau) plus tard · **🟡** |
| Dérivation fuseau/zone depuis l'adresse (API Géo + timezonedb → `school_zone`) | 🟡 | v3 §3.2 | **Zone livrée (PR-2)** : `school_zone` dérivé du `ffbbClubCode` par `SchoolZoneResolver` (dép.→zone, table fixe), **pas d'API Géo** · reste le **fuseau** (`ClubTimeService`, ligne §6.2). ⚠ heuristique FFBB→dép. best-effort, fallback zone manuelle |

---

## 9. Transverse

| Évolution | Statut | Réf | Note |
|-----------|--------|-----|------|
| Export planning (worker Puppeteer mono-thread) | ✅ | v3 §2.1 | Livré + **étendu (PR #112, 2026-07-08)** : **PDF / PNG / Excel**, chaque rendu tenant sur **1 page A4 paysage** (grille jours×gymnases au pas 15 min, fit auto), périmètre **tous les gymnases** ou **un gymnase** ; XLSX = tableau plat (PhpSpreadsheet). Menu « Exporter » sur un planning terminé. **Reste** : **rétention** (base/period/manual/is_pinned) ⬜ ; logo club dans l'en-tête PDF ⬜ |
| **Audit trail** (`audit_logs` append-only, BRIN, purge RGPD, async) | ✅ | v3 §3.2 · FF#6 | **Livré avec P0-1 (2026-07-11)** — la ligne était restée ⬜ et **contredisait sa propre trace** en §Livrés (corrigé le 2026-07-29). `AuditLog` + service `AuditTrail` ; l'append-only est tenu **par la DB** (la migration ne crée aucune policy UPDATE/DELETE pour `app_user` : le rôle runtime ne peut pas réécrire l'histoire), purge `app:audit:purge` **rétention 12 mois** au cron quotidien, NR `AuditTrailTest`. **Écarts assumés vs le libellé v3** : table `audit_log` (singulier), index **B-tree** et non BRIN, écriture **synchrone** — au volume cible aucun des trois ne se justifie. Registre : [`../../docs/security/rgpd.md`](../../docs/security/rgpd.md) |
| **Console super-admin** (monitoring santé/usage/conversion + métriques par club & globales + data ops auto + actions d'exploitation) | 🟡 | idée produit (2026-07-09) | **SA0 à SA3 livrés (2026-07-16)** + **SA2-stats livré (2026-07-18)** : stats d'usage fondateur — télémétrie **append-only** (plus purgée avec les versions ni au reset ; dimensions `plan_type`/`nb_teams`/`nb_venues` dénormalisées à la capture), `schedule_plan.first_chosen_at` (temps de clôture = création → 1re validation), overview + section React « Usage produit » (plans par type dont validés, p50/p95 clôture et wallTime par type, distribution tailles de clubs). **SA4 v1 livré (2026-07-18)** : catalogue FERMÉ d'actions support par club (`AdminActionCatalog` — reset quota, reset saison courante, purge anciennes saisons), plomberie SA3 réutilisée (verrou + historique `admin_job_run.arguments` + CSRF + audit), confirmation nominative pour le destructif, NR `AdminClubActionTest` (phase1) ; **suspension club + approbation fallback différées au premier cas réel** (décision fondateur — métier non tranché). Vérité courante [`../courantes/superadmin-auth.md`](../courantes/superadmin-auth.md), suite dans [`console-superadmin.md`](console-superadmin.md). Reste **SA4 v2 (au signal) → SA5**. **Alerting + data-freshness livrés (2026-07-18)** ; **prod-readiness livré (2026-07-18)** : Sentry 3 zones (DSN-vide-inactif) + backups `pg_dump` pilotés par l'activité (skip sans activité, rétention 14, `restore-check` prouvé, board+alerte, runbook `docs/ops/backup-restore.md` avec checklist snapshots hébergeur) : job `health-alerts` 10 min → email superadmins sur transition (anti-spam `admin_alert_state`) + board fraîcheur des référentiels (`/api/admin/freshness`) — la console devient une surveillance ACTIVE. **🔴** |
| Table `solver_metrics` (perf par génération) | ✅ | v3 §3.5 · FF#7 | **Capture livrée SA1**, **append-only + dimensions livrés SA2-stats (2026-07-18)**. ~~Partition mensuelle + purge 6 mois~~ **abandonnées (décision fondateur 2026-07-18)** : rétention **≥ 13 mois** (tendances d'une saison entière), pas de mécanique de purge au volume cible, l'index `(club_id, created_at)` suffit |
| Diagnostics avec **suggestions actionnables** (jsonb = actions + liens entités) | 🟡 | v3 §7 | Diagnostics texte livrés ; actions cliquables ⬜ · **🟡** (jumeau de l'« alerte cliquable » §4) |
| Validation temps réel à la saisie (coach-joueur = seule erreur bloquante ; le reste = warnings) | 🟡 | v3 §11.3 | Wizard valide par étape ; **warnings de réservation livrés** (PR #14 : créneau surchargé vs capacité, quota séances/semaine, 2 séances le même jour — non bloquants, le solveur reste l'autorité) ; **warning équipe compétitive classée rang D · Bonus** (PR #35, dérivé du label du tier, non bloquant). Liste complète de warnings à compléter · **🟢** (compléter la liste au fil de l'eau) |
| Push temps réel (Mercure statut/score) | ✅ | v3 §2, §6 | Livré |
| **Identité visuelle par club** (couleur d'accent + logo) | ✅ | Livré 2026-07-02 : accent + logo + extraction 3 couleurs + écran /club (reste : stockage prod S3) → [`identite-visuelle-club.md`](../courantes/identite-visuelle-club.md) |
| **Accent par thème + densité d'accent** — accent distinct clair/sombre + présence d'accent renforcée dans l'UI | ✅ | idée club (2026-07-07) | **Livré (PR #109, 2026-07-08)** : accent distinct clair/sombre (`Club.accentColorDark` + `accentPalette`) et présence d'accent renforcée dans l'UI, contraste AA préservé par thème |
| `team_tags` / `team_tag_assignments` + règles facility par tag | 🟡 | contraintes-v2 | Tags système + ciblage tag livrés ; FACILITY_FORBIDDEN/PREFERRED_TEAM_TAG dédiés ⬜ · **🟡** · ⚠ **dette constatée (transition PR-2)** : `TeamTagService::syncTeamTags` **efface tous** les tags (custom inclus) et ne re-dérive que les tags **système** à chaque édition d'équipe → les tags **custom** sont **éphémères** (ne survivent pas à une édition). Bloque le report des tags custom entre saisons (transition ne les copie donc pas). À corriger avant de fiabiliser les tags custom · **🟢** |
| `max_days_per_week` coach (calcul dynamique + override checkbox) | ⬜ | v3 §3.4 | **Vérifié le 2026-07-29 : rien n'existe** — aucune occurrence de `maxDaysPerWeek`/`max_days_per_week` dans `backend/src`, `engine/app` ni `frontend/src` (ni colonne, ni contrainte, ni case à cocher). Le 🟡 « à vérifier » précédait la vérification · **🟢** |
| **Réservation salle de convivialité** (self-service coach : réserver une soirée dans une salle non-sportive → notif gestionnaire) | ⬜ | idée club (2026-07-05) | **V2 « club hub » — différé, gardé.** La résa elle-même est **triviale** (salle = `Venue` avec horaires, réservation = date + heure début + durée, **pas de solveur**, juste un check de conflit + notif au gestionnaire). ⚠️ **Le vrai coût n'est pas la résa** : « le coach réserve lui-même » exige des **comptes coach + modèle de rôles/permissions** (aujourd'hui `ClubUser.role` hardcodé `admin`, cf. §5 🔴). Tant que §5 n'est pas fait, ce n'est **pas** gratuit. **Question stratégique** : veut-on que l'appli devienne le *hub du club* (self-service coach) ou rester l'*outil de planning* (piloté gestionnaire) ? Adjacent au cœur (n'exploite pas le solveur — commodité type Doodle). **Déclencheur de réouverture** : construction des comptes coach/rôles (§5), ou demande d'un club pilote — la feature devient alors quasi gratuite · **🟢 la résa / 🔴 le prérequis rôles** |
| App **mobile** (React Native/Expo, consultation → exceptions V2) | ⬜ | v3 §1.4 · FF#14 | P2 · **🔴** (à ne pas faire avant clients payants — web responsive + PDF suffisent) |
| Notifications coach (email PDF → push + lien consultation sans login) | ⬜ | v3 §1.4 · FF#17 | P2 · **🟡** |
| Stats & analytics (taux de remplissage, heures-coach/semaine) | ⬜ | v3 §1.4 · FF#16 | P2 · **🟡** (peu coûteux une fois les occurrences là ; demande d'AG) |
| Dashboard super-admin (multi-clubs) | ⬜ | v3 §14.3 · FF#15 | P2 · **🔴** |
| **Multi-sport** (handball/gym/volley, modèle générique) | ⬜ | v3 §1.4 · FF#18 | V2 · **🔴** (attendre une vraie demande) |
| **Suppression sûre (salle / équipe / coach)** — confirmation + liste d'impact avant hard-delete | ⬜ | idée club (2026-07-07) | Entités **saison-scoped** (partagées par tous les plannings — pas de « suppression pour un seul plan »). **Décisions actées** : **hard delete**, précédé d'une popup listant l'impact en cascade — contraintes liées (`Constraint.scopeTargetId`), liens coach (`TeamCoach`) pour équipe/coach, et **plannings secondaires supprimés** (règle rappelée : modifier le socle purge les overlays de la saison — cohérent avec le 409 `overlays_exist` existant). Coach : la suppression retire le lien équipe **et** liste les contraintes coach avant de purger. Axe *planning lifecycle* → NR requis · **🟡** |
| Doc OpenAPI de `AuthController` (`/api/register`, `/api/me`) | ✅ | BG G4 | **Livré (PR #43, BCK-F)** : `CustomRoutesOpenApiFactory` |
| **Doc OpenAPI des routes custom restantes** (`CustomRoutesOpenApiFactory` couvre aujourd'hui register + verify, `/api/me` et me/password, me/export + club/export, transition de saison, fenêtres de match, conflits de rencontres, manual-edit×3, vacances scolaires + jours fériés et les routes admin — manquent notamment : club appearance/logo, teams/reorder, health, reopen/validate, reset-season, calendar-entries conflicts, memberships pending/approve/reject, constraints/validate, password forgot/reset) | ⬜ | — | Gap constaté 2026-07-06 en régénérant le snapshot : toute route `#[Route]` hors API Platform est invisible de `/api/docs` + du snapshot tant qu'elle n'est pas ajoutée à la factory. **Énumération recalée le 2026-07-29** — la factory a grandi (elle ne se limite plus à register/me/manual-edit/vacances), le gap demeure. Rattrapage en lot + réflexe « nouvelle route custom = entrée factory » · **🟡** |

---

## 10. UX & navigation

Polish frontend discuté, non structurant mais confort d'usage.

| Évolution | Statut | Note |
|-----------|--------|------|
| **Refonte de la navigation** — menu principal **à gauche** (Accueil, Assistant…) ; header **réduit** pour gagner de l'espace au centre ; **burger en haut à droite** pour Profil + déconnexion + thème dark/light | 🟡 | **Burger haut-droite livré** (PR #36) : menu unifié Club · Profil · Thème · Logout (`shared/components/ui/menu.tsx`), header allégé (nav gauche = Accueil · Assistant). **Reste** : menu latéral gauche + sous-menu Assistant (l. suivantes) |
| **Assistant = menu avec ses étapes en sous-menu** (les 6 étapes du wizard sous l'entrée « Assistant » du menu gauche) | ⬜ | Remplace la colonne d'étapes interne du wizard · **🟡** (refonte nav wizard) |
| **Clic sur le nom / logo du club → Accueil** | ⬜ | Raccourci d'accueil · **🟢** (un `<Link>`) |
| **« Demandes » fusionné dans « Gestion du club »** | ✅ | Livré (PR #36) : `/club` devient un hub à sections dépliables (`AccordionSection`) — **Demandes** (admin-only, ouverte par défaut, `PendingMembersSection`) + **Visuel** (identité). Route `/pending-members` supprimée, entrée de nav retirée |
| **Wizard : titre d'étape fixe en haut + Précédent/Suivant fixes en bas** (sticky header/footer) | ✅ | Livré : header (titre + n° d'étape) et footer Précédent/Suivant en `sticky` dans `WizardLayout.tsx`. Affiné (PR #37) : titre **unique** (h2 internes retirés des 6 steps), footer collé au **bas réel** (plus de bar flottant sur étape courte), nav gauche `w-44`, grille Génération raccourcie (`PlanningPage embedded`) pour ne pas passer sous le footer |
| **Wizard : colonne d'étapes repliable / plein écran** | ✅ | Livré : toggle « Plein écran » replie la nav gauche (`navCollapsed`) ; le masquage forcé sur l'étape génération est retiré → l'utilisateur contrôle (résout aussi l'accès menu en régénération) |
| **Menu : entrée « calendrier annuel »** | ✅ | **♻ = l'accueil cockpit** ([`accueil-cockpit-temporel.md`](../courantes/accueil-cockpit-temporel.md), §2/§5) — **livré** (statut corrigé le 2026-07-29) : le cockpit **est** la route `/`, le planning a désormais sa propre entrée `/planning`, et le NavLink « Accueil » (nom + logo du club) y mène. La réserve « vue 12 mois » vit sur la ligne jumelle en §2 |
| **i18n des diagnostics solveur** (fichier de traduction) | ⬜ | Alertes **en français** obligatoire. Aujourd'hui messages FR codés en dur dans l'engine + reste EN à éliminer (`soft_lock_moved`, `unused_slot`). Cible : clés + fichier de traduction (côté backend `DiagnosticMessageBuilder` ou front) plutôt que du texte en dur dans l'engine · **🟢** (éliminer 2-3 clés EN dans `DiagnosticMessageBuilder` — FR obligatoire produit) |
| **DA « ça sent le basket »** — l'app est visuellement neutre alors qu'elle s'adresse à des clubs de basket : ajouter une identité sportive (fond de bandeau évoquant un terrain/parquet, illustrations d'états vides, iconographie) | ⬜ | besoin fondateur 2026-07-17 | **Justification = mono-sport assumé, PAS préparation multi-sport** (la ligne Multi-sport §9 dit « attendre une vraie demande » — la respecter). Un club de basket doit sentir que le produit est fait pour lui : c'est un **argument de démo et de vente**, pas du décor. Complète l'identité **par club** déjà livrée (couleur d'accent + logo, §9) qui, elle, ne dit rien du sport. **Garde-fou de coût** : regrouper les assets sportifs en **un seul endroit** (un thème, pas des images éparpillées dans 20 composants) — pas pour préparer le handball, mais pour qu'un changement de parti pris graphique reste une PR et non une chasse au trésor. Sobriété : un fond de bandeau + les états vides suffisent ; l'excès dessert un outil de gestion. Cadrage graphique à faire (skill `ui-ux-pro-max`) · **🟢/🟡** |
| **Radar — carte « jour férié » à retravailler** (cibler ce qu'on doit voir + texte) | ⬜ | Différé constaté à la livraison des reliquats cockpit (2026-07-06). Aujourd'hui : « 14 juillet · Dans 8 j · jour férié » pour **tout** férié à ≤ 30 j (`PUBLIC_HOLIDAY_HORIZON_DAYS`), carte info sans CTA. À cadrer : **quels fériés méritent le radar** (tous ? seulement ceux qui tombent un jour d'entraînement de la semaine type ?), **le texte** (dire l'impact — ex. « 2 séances ce jour-là » — plutôt que répéter la date) et l'horizon. Décision produit, pas technique · **🟢** |

### Audit UX du wizard (2026-07-04)

> Audit réel (parcours navigateur BCCL 49 équipes + lecture code `frontend/src/features/wizard`). Le socle est bon (6 étapes claires, aide contextuelle, grille gymnases type calendrier, génération inline, 100 % FR, `aria-label` sérieux, faux positifs de diagnostics corrigés → 1 alerte). Les points ci-dessous ciblent la **confiance du gestionnaire non-technique**. Effort estimé à côté.

| Point | Priorité | Effort | Note |
|-------|----------|--------|------|
| **Échecs de mutation silencieux** (zéro `onError`/toast dans l'app) | ✅ | **🟢** | **Livré (FRT-01/02)** : filet global `MutationCache.onError` + `QueryCache.onError` → toasts (`shared/lib/queryClient.ts`), anti-doublon, 401 centralisé |
| **Suppressions immédiates sans confirmation, cascade** | ✅ | **🟢/🟡** | **Livré** : `ConfirmDialog` sur les suppressions cascade (gymnase + créneaux dans `VenuesStep`, équipe, contraintes) |
| **Aucun état de chargement** (vide ≡ en cours) | 🟡 | **🟢** | **✅ Livré** : `useStepValidation` reste neutre tant que les queries chargent (`isLoading`) → plus de flash « Ajoutez au moins une équipe » avant l'arrivée des données. Gardé par `useStepValidation.hook.test.tsx`. Skeletons de contenu restent optionnels |
| **Jargon solveur brut** (`HARD`/`PREFERRED`/`BONUS`/`LOCK` affichés) | ✅ | **🟢** | **Livré** (constaté le 2026-07-29) : libellés FR au dropdown **et** aux badges — Obligatoire / Préféré / Verrouillé (`ConstraintsStep`, `PeriodStructure`), BONUS retiré de l'offre UI (ENG-12) ; **gelé par un test Vitest** qui exige exactement ces trois options. Reste l'i18n des **diagnostics**, ligne dédiée en §10 |
| **Réservations en localStorage jamais revalidées** | 🟡 | **🟡** | Copie l'horaire au moment T ; si le créneau bouge/est supprimé → postée en LOCK HARD sur un horaire mort → solveur INFEASIBLE sans explication. Fix : référencer le slot + revalider au lancement (`useLaunchGeneration`) |
| **Étape Équipes = 1 seul écran de N lignes** (saisie 1 par 1, pas d'import) | 🟡 | **🟡** | 49 équipes tapées à la main. L'endpoint import Excel **existe déjà** (§8) — le brancher au wizard. + corriger le bug React **duplicate keys** de cet écran |
| **Deux classements concurrents non expliqués** : Rang (S/A/B/C/D priorité) vs Niveau de jeu (division FFBB) | ✅ | **🟢** | **Livré** : micro-copy dans l'aide de l'étape Équipes (« le niveau décrit la compétition, pas la priorité de placement ») + regroupement par rang S/A/B/C/D (PR #106) |
| **Piège nouveau gymnase** : après ajout d'un 2ᵉ gymnase, sélection reste sur le 1ᵉʳ → créneaux posés sur le mauvais | 🟢 | **🟢** | **✅ Livré** : le gymnase créé est auto-sélectionné (`VenuesStep`, `onSuccess`) → les créneaux tombent au bon endroit |
| **Polarité des jours invisible** : cocher un jour l'*interdit* (sens visible seulement après enregistrement) | 🟢 | **🟢** | Fix : label « éviter ces jours » |
| **Pas d'indicateur « étape terminée »** dans le rail (navigation libre pour club existant) | 🟢 | **🟢** | Fix : coches d'état par étape |
| **Focus non géré** après ajout (pas de retour au champ) ; **pas de dimanche** dans la grille alors que les contraintes l'autorisent ; colonne coach « a » / « Sans coach » (donnée à trancher) | 🟢 | **🟢** | **Focus ✅ Livré** (retour au champ nom après ajout équipe/gymnase/coach). **Dimanche : tranché — abandonné** (≈95 % des clubs amateurs ne s'entraînent pas le dimanche ; une grille 7 colonnes ne se justifie pas pour les 5 %). À rouvrir seulement si un vrai club pilote le demande. **Reste** : colonne coach « a » |

**Top 3 (2026-07-04) — statut au 2026-07-29** : (1) feedback d'erreur global ✅, (2) confirmation des suppressions ✅, (3) micro-copy Rang/Niveau ✅ — **et les libellés FR des badges de contraintes sont livrés eux aussi** (cf. ligne « jargon solveur » ci-dessus), contrairement à ce que disait cette phrase. **Top 3 soldé.** Reste, hors Top 3, l'**i18n des diagnostics solveur** (§10).

---

## Top des zones à réfléchir en priorité (non priorisées ici — juste les plus structurantes/non-suivies)

> **Chapitre 1 (Contraintes & solveur) : les 13 points d'origine sont clos** (série ENGINE, 2026-07-03) — tranchés + le repos après jour de match livré. (3 lignes ajoutées depuis, hors série : plage horaire coach, trajets/passerelles V2, typage engine — voir fin du tableau §1.) En prime, la série a **rendu effectives** des contraintes qui étaient silencieusement ignorées par le solveur (indispo coach par jour, FACILITY_CAPACITY, LOCK, allowedDays), corrigé les faux positifs de diagnostics, sorti le solve de l'event loop, ajouté un gate perf et PREFERRED TIME. Voir les PRs #26–#31.

1. ~~**Objectif mou « repos après jour de match »**~~ — **livré** (E-feat).
2. **Modèle temporel & périodes d'exception** (templates→occurrences, vacances, plans secondaires) — grosse feature produit.
3. **Bridage plan Découverte** — verrou de conversion, business-critique, rien de fait.
4. **Transition de saison** — nécessaire dès la 2e saison d'un club.

---

## Backlog priorisé — effort × impact (cap commercialisation mi-2027)

> Vue de pilotage (ex-`backlog-priorise.md`, absorbé le 2026-07-11). **Impact** 🔴 bloque la vente /
> l'intégrité · 🟠 fort levier · 🟡 valeur ciblée · ⚪ polish/dette. **Effort** S ≤ 1 PR · M 2-3 PR ·
> L lot phasé · XL recherche + gros lot. Un item livré **quitte** ces tableaux (trace : ligne de la
> carte §1-§10 mise à jour + entrée §Livrés) ; les ids sont **stables, jamais réutilisés** (un trou =
> un livré, pas un oubli).

### P0 — Bloquants GA & intégrité (à solder AVANT de vendre)

**Tous soldés** (P0-1 RGPD 2026-07-11, P0-3/P0-4 2026-07-18, P0-2 config prod + pipeline de déploiement 2026-07-25/26 — cf. §Livrés).

### P1 — Enablers à fort levier

| # | Sujet | Impact | Effort | Débloque | Note |
|---|-------|:---:|:---:|---|---|
| P1-1 | **Rôles non-admin + modèle de permissions** (`ClubUser.role` hardcodé `admin`) | 🟠 | L | self-service coach · collecte vacances (P2-1) · salle convivialité · comptes coach | `isManagementRole` = amorce ; voters à câbler partout |
| P1-3 | **Bridage freemium Découverte** | 🟠 | M | monétisation | doc'd, 0 code ; anti-abus par identité hors v1 |

### P2 — Différenciateurs & complétion

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P2-1 | **Plan de vacances éditable + collecte coach** | 🟠 | L | **le différenciateur commercial** ; collecte = lien tokenisé sans login. Gagne à ce que P1-1 existe, pas bloqué. Première marche livrée (2026-07-25, #10 lot C1) : todo-list « Doléances des coachs » (entité `CoachWish`, 2 vues wizard/cockpit, saisie manuelle, coche « traité »). **C2 livré (2026-07-25, #10)** : collecte tokenisée SANS email — campagne modifiable (« Solliciter les coachs »), lien personnel par coach (`/doleances/{token}`, token en clair, copier-lien WhatsApp), page publique sans login pré-remplie (dépôt borné au périmètre du token, jamais 401, 410 à la deadline), badge radar « X/Y répondu · N à traiter ». **C3 livré (2026-07-25, #10 — feature CLOSE)** : emails (bouton « Envoyer les liens », individuel + global, badge « pas d'email »), **digest quotidien 7h** aux gestionnaires (seulement si nouvelle réponse ; récap final une fois après la deadline), **relance des silencieux** (1×/jour Europe/Paris). **La structure de période ÉDITABLE (P1) est elle aussi livrée** (grille gymnase possédée par la période #8, séances/activation par équipe E4, choix des semaines E1, mutualisation par `Reservation`) — cf. `types-de-planning.md` types 2/3. **P2-1 essentiellement soldé** ; reste optionnel : un flux guidé « reprise progressive » (§6bis, bonus). Trace §Livrés 2026-07-25 — [`types-de-planning.md`](../courantes/types-de-planning.md) E5 (comportement + décisions figées) · [`backend-inventory.md`](../courantes/backend-inventory.md) §2 |
| P2-6 | **Pattern « Plan » (reconstruction ADR-0002)** | 🟠 | L | Entité de premier ordre pour « le planning » ([ADR-0002](../../docs/architecture/adr-0002-pattern-plan.md)) — 4 lots A→D. **Lot A livré (2026-07-12)** : `SchedulePlan` + `Schedule.schedulePlanId`/`versionNumber`, migration RLS + backfill, provisioning auto, API lecture ; billing `Plan`→`SubscriptionPlan`. Additif, zéro legacy retiré. **Lot B1 livré (2026-07-16, ADDITIF)** : le pointeur est *maintenu* (valider/rouvrir/supprimer) + numérotation monotone + **réparation d'un vrai bug du lot A** (le pointeur avait été backfillé depuis la baseline AUTO, qui n'est pas une validation) ; migration non destructive, **zéro comportement changé**. **Leçon (tracée dans l'ADR)** : le legacy n'est pas un miroir passif — il est lu pour décider par ~16 fichiers (radar matchs, routing, mode guidé) ; une demi-bascule crée deux vérités divergentes (~15 défauts confirmés en 4 rounds). **✅ Bascule livrée (2026-07-16)** — back + front + **drop du legacy dans le même lot** : le plan SEASON et sa version pointée SONT le calendrier de la saison ; « validé » se dérive du pointeur (plus de statut) ; valider = pointer + **supprimer** les sœurs, rouvrir = dépointer, **aucun pointage automatique** ; déblocage cockpit = `hasFinishedVersion` ; gate matchs/plans secondaires = pointeur (**409**) ; `Season.{baselineScheduleId,socleValidatedAt,planningName}`, les statuts `VALIDATED`/`ARCHIVED` et `SetBaselineController` **supprimés** ; `/api/me` expose `seasonPlan`. Reste **C** (réglages/génération par plan) |
| P2-9 | **Un LOCK HARD annule silencieusement les contraintes** (§1) — `_extract_hard_locks` pré-place les slots HARD hors solveur et leur variable n'est jamais créée → **aucune** contrainte ne s'y applique. **Volet 1 LIVRÉ (2026-07-28) : le solveur ne se tait plus** — `diagnose_locked_slot_violations` croise les verrous avec les contraintes SAISIES (indispo coach, fenêtres horaires, règles de jours UNIES par équipe dont `forcedDays`, gymnase interdit) et émet un `constraint_not_honored` INFO nommant la VRAIE contrainte, l'équipe, le coach, le gymnase, le jour, l'heure et la durée. Le verrou reste SOUVERAIN (ALIGN-07 non rouvert) : ce qui change est le silence. ⚠ **Le volet capacité a été RETIRÉ de ce lot** (décision fondateur, revue #317 round 3). Trois rounds successifs l'ont pris en défaut sur la même faute — un miroir du payload de génération écrit à la main — et la troisième tentative, qui passait par `buildForClubSeason`, provoquait une **perte de données** : `serializeTeam` déclenche `TeamTagService::syncTeamTags`, qui supprime puis recrée les `TeamTagAssignment` avec un `flush()` intermédiaire, or le contrôleur de validation ne flushe jamais — ouvrir le récap supprimait les tags de la dernière équipe. **Préalable à sa reprise : une source de payload en LECTURE SEULE.** `buildForClubSeason` écrit, et `buildForOverlay` exige un `Schedule` inexistant avant génération. À traiter dans sa PR, avec le câblage frontend des `warnings` (aujourd'hui produits par le backend et jamais affichés) et un garde de forme sur le corps d'erreur — `validateConstraints` transtype tout JSON en `ValidateResult`, donc un 403 fait planter le récap. **RESTE : trois PR, cadrées pour reprise à froid dans [`p2-9-verrous-et-alertes.md`](p2-9-verrous-et-alertes.md)** — (A) volet capacité, bloqué par un préalable (source de payload en LECTURE SEULE : `buildForClubSeason` écrit via `syncTeamTags`, `buildForOverlay` exige un `Schedule` inexistant avant génération), à livrer avec le câblage frontend des `warnings` jamais affichés et un garde de forme sur le corps d'erreur ; (B) **erreur BLOQUANTE** au récap sur impossibilité physique (coach dans deux gymnases à la même heure) ; (C) **prévention au clic** dans la modale. ⚠ B et C : n'interdire que sur les coachs **MAIN**, et une SEULE implémentation partagée de la détection. | 🟠 | M | volet 1 livré 2026-07-28 |
| P2-2 | **Boucle d'ajustement — « corriger sur place »** (glisser une équipe dans un créneau vide) | 🟠 | M | « naviguer » fait (#180) ; même primitive que la grille de réservation |
| P2-3 | **Versions D4 — « Travailler sur cette version »** + savepoint auto | 🟡 | M | moitié manquante de la décision 5 (D1-D3 livrés) |
| P2-4 | **Compte démo** | 🟡 | S/M | spec'd ([`compte-demo.md`](compte-demo.md)), 0 code |

### P3 — Complétude modules

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P3-1 | **Matchs — reste palier A** : volet joueur, `Team.preferredMatchWindow`, envelope HARD | 🟡 | M | paliers B/C plus tard |
| P3-2 | **Overlays — période `custom` générante** (aujourd'hui 422, mitigé bouton désactivé) | 🟡 | M | seul reste des overlays |
| P3-3 | **Modèle « templates → occurrences »** | 🟡 | L | débloque la cascade calendrier de saison → plans secondaires |
| P3-4 | **Enregistrement FFBB** (anti-squatting code club) | 🟡 | M | spec'd (#145) |
| P2-7a | **Périmètre engagé — une équipe qui joue est intouchable** | ✅ | S | **Livré (2026-07-16)** : `TeamEngagementGuard` — une équipe portant **≥1 match, quel qu'en soit le statut** (la correspondance faite par l'import FBI EST l'engagement : la fédération la connaît ; l'import crée tout en `UNPLACED`, donc filtrer sur `PLACED` rendait la garde **inerte** au moment précis où elle doit mordre) ne peut plus être **supprimée** (sinon `purgeChildrenOfTeam` emporte ses matchs, connus de la fédé) ni changer de **`level`** (elle y est inscrite). Rang/tier, `isActive`, nom et créneaux restent libres. Une seule définition, consommée par la garde d'écriture ET par `TeamResource.isEngaged` (batché) dont le front grise les deux gestes. → [`module-matchs.md`](../courantes/module-matchs.md) |
| P2-7 | **« Modifier mon planning de saison » — le geste** | ✅ | M | **Livré (2026-07-30)** — cadrage détaillé : [`reprise-perimetre-engage.md`](reprise-perimetre-engage.md). **Décision fondateur (2026-07-16)**, précisée après cadrage : le planning de saison « ne change quasiment JAMAIS » — il s'ajuste dans de **rares** cas (BCCL récupère un gymnase en construction : le cas est réel). Ce lot ne fluidifie donc pas un flux courant, il fait en sorte que **le cas rare ne casse rien**. (1) ~~**une seule porte** — refuser la création d'une version de saison concurrente tant que le plan en pointe une (aujourd'hui `POST /api/schedules` rend **201** dans cet état)~~ **livré** : `ScheduleStateProcessor::processPost` refuse en **409** (« le planning de la saison est en vigueur — rouvrez-le avant d'en préparer un autre ») tout POST « de saison » (sans `schedulePlanId`, ou avec le plan SEASON explicite) tant que `chosenOfSeasonPlan` répond non-null — posé sous le verrou de plan-scope de la saison, dans la transaction de création (même patron que valider/rouvrir). « La seule manière de modifier le planning de saison est Rouvrir » devient vrai **par construction** ; NR `SeasonVersionUniquenessTest` (phase1). (2) ~~**confirmation forte** — popup + validation explicite~~ **livré, dans la forme retenue** : la popup existante de réouverture (409 `overlays_exist`, affichée seulement s'il y a des plannings secondaires à détruire) exige de taper « modifier mon planning de saison » avant que le bouton s'active (`ConfirmDialog.confirmPhrase`, prop additive) — **pas** sur la modale de validation : décision fondateur, valider ne supprime que des versions de travail sœurs, jamais un plan secondaire. (3) **Volet matchs abandonné (décision fondateur, 2026-07-30)** : ~~seuls les matchs `UNPLACED` sont supprimés (→ ré-import FBI)~~ — le module matchs est déjà gaté sur le pointeur (`SocleGuard::assertSeasonPlanChosen`), donc rouvrir le rend inaccessible **sans rien détruire** ; les supprimer obligerait à refaire l'import FBI pour un résultat déjà acquis. Le module matchs fera l'objet d'une prochaine évolution (pas re-ouvert ici). (4) ~~**rien du passé ni des périodes EN COURS** n'est détruit — aucun filtre de date aujourd'hui~~ **livré le 2026-07-24 avec #8** : `findWithPlanNotStarted` filtre sur `startDate > aujourd'hui` (pivot = date de début), et la portée s'est élargie aux périodes dont le plan n'est pas validé — une période « Adaptée » jamais générée possède déjà sa grille copiée. |
| P3-8 | **Binômes de partage définis par le gestionnaire** (§1) — sur un créneau à capacité > 1 le solveur associe n'importe quelles équipes ; pré-définir les paires autorisées. **Rouvre la décision « `allow_shared_court` : abandonné »** (§1). Forme à trancher AVANT de coder — tag de compatibilité plutôt que saisie de paires (N² = inutilisable à 40 équipes). Besoin à valider ; axe *constraint semantics* → NR | 🟡 | L | besoin fondateur 2026-07-17 |
| P3-5 | **Versions — diff/comparaison entre versions** | 🟡 | L | hors périmètre D assumé. *« Restaurer une version ARCHIVED » retiré : caduc depuis la bascule ADR-0002 (valider **supprime** les sœurs, il n'y a plus d'archive).* |
| P3-6 | **`solver_metrics` — persistance + partition + purge 6 mois** | ✅ | M | persistance livrée SA1 ; partition/purge **abandonnées** (append-only, rétention ≥ 13 mois — SA2-stats 2026-07-18, cf. §9) |
| P2-9bis | **Le socle en vigueur : un invariant unique pour les QUATRE portes** | 🟠 | M | **Constaté par la revue adverse de la PR #326 (round 2, 2026-07-30).** P2-7 a fermé la **création** d'une version de saison (`POST /api/schedules` → 409 tant que le plan SEASON pointe). Mais l'invariant réel est plus large que celui qu'on a codé : **« tant que le plan de saison est en vigueur, rien ne touche au calendrier de saison ni à ses entrées »**. Trois chemins l'ignorent encore, tous sur des **données héritées** (une version pointée PLUS une sœur — l'état que le trou d'avant P2-7 produisait, qui ne naît plus) : (1) `RegenerateController` et (2) `GenerateScheduleController` ne refusent que si la version **source** est celle qui est pointée, jamais si le plan pointe une AUTRE version ; (3) `RegenerateFromVersionController` restaure une structure ancienne (`StructureRestorer::apply`, destructif) **sans consulter le pointeur du tout**. ⚠️ **Ne PAS traiter porte par porte** : la tentative de fermer `regenerate` seul a été implémentée puis **retirée** parce qu'elle transformait « Charger cette version » en **cul-de-sac destructif** — le restore écrase la structure vivante du club, puis « Régénérer » refuse, et la seule issue restante détruit tous les plannings de période à venir. **Forme attendue** : une assertion unique dans `SocleGuard` (`assertSeasonPlanNotChosen`, miroir de l'existante — aujourd'hui le prédicat et son message sont écrits à la main), prise **sous le verrou de plan-scope et dans la transaction** (la garde du POST le fait ; une garde de contrôleur qui lit hors verrou est un simple TOCTOU qu'une validation concurrente traverse), appliquée aux quatre portes, **plus les affordances frontend** (`regenerateDisabled`/`canRegenerateFrom` ne regardent que `snapshotHash` et la version choisie : ils offrent des actions que le serveur refuse). NR à prévoir : un cas par porte, plus la couverture de discrimination entre plans perdue au passage (elle s'exerce aujourd'hui sur `ensureSeasonPlanId` et non plus sur le POST). |
| SEC-12 | **RLS : le test ne garde pas la PORTÉE des policies** (défense en profondeur réduite sur 3 tables) | 🟠 | S | **Constaté le 2026-07-29, non exploitable en l'état — décision fondateur : documenter et tracer, ne pas durcir en urgence.** Trois tables s'écartent du motif `tenant_isolation FOR ALL` : `club_user` et **`coach_wish_token`** ont un `SELECT` **ouvert** (`USING (true)`, donc cross-tenant au niveau base), `audit_log` a des policies scindées. Pour `coach_wish_token` c'est **structurellement nécessaire** : la page publique `/api/coach-wishes/public/{token}` n'a pas de JWT et doit lire le token pour découvrir le `club_id` qui posera le GUC. **Pourquoi ce n'est pas une faille aujourd'hui** : le filtre Doctrine `TenantFilter` s'applique à toute entité portant un `club_id` (donc à celle-ci) sur tout chemin authentifié ; aucun SQL brut ne touche la table ; la campagne qui expose les tokens est management-only en lecture ET écriture (SEC-07) sous RLS complète ; le token est un secret de 32 octets sans endpoint de listing, avec 404 byte-identique. **Ce qui reste à corriger** : (1) l'isolation de ces tables ne repose plus que sur **une** couche au lieu de deux — une requête native ou un `createQuery` filtre désactivé y fuiterait en silence ; (2) `RlsIsolationTest` vérifie `rls_enabled`/`rls_forced`/`policies > 0` mais **jamais la portée** — un `USING (true)` copié-collé depuis `Version20260726100000` sur une future table tenant passerait le test sans rougir. **Correctif proposé** : policy `SELECT` scopée au GUC *quand il est posé* (`current_setting('app.club_id', true) IS NULL OR '' OR club_id::text = …`) — le lookup pré-GUC continue de marcher, l'étanchéité revient dès qu'un contexte tenant existe — **plus** un test de portée sur TOUTES les tables tenant. Documenté : [`docs/security/rls.md`](../../docs/security/rls.md) §Exceptions. |
| P2-8 | **Le front re-dérive des règles de refus du serveur** | 🟠 | M | `PlanningToolbar` recalcule côté client `isLastFinishedSeasonVersion`, `hasInFlightSibling` et le décompte des sœurs supprimées à la validation. **Chaque miroir est un risque de dérive** — c'est ce motif exact (deux endroits qui répondent à la même question) qui a produit **40 défauts en 4 rounds** sur la bascule ADR-0002. Les supprimer suppose que le serveur expose ce qui est **permis** (capacités par ressource, patron de `Schedule.isChosen` / `Team.isEngaged`) : chantier de conception, pas correctif. |
| P3-10 | **Libellés de versions : utiliser `Schedule.versionNumber`** | 🟡 | S | Le serveur porte un `versionNumber` **stable** (compteur monotone du plan) ; le front dérive ses libellés « V3 » de l'ordre de création. Depuis la bascule, valider **supprime** les sœurs — la version choisie se retrouve donc libellée « V1 ». Sans conséquence tant que le plan n'a qu'une version, mais la numérotation ment dès qu'on en supprime une. |
| P3-11 | **Radar : aucun état de chargement** | 🟡 | XS | `RadarPanel` n'affiche ni cartes ni « Rien à l'horizon » tant que les impacts des fermetures arrivent (quelques centaines de ms) — un cadre « À traiter » nu. Le masquage pendant le vol est **voulu** (ne pas faire clignoter une carte qui va disparaître) ; il manque juste un squelette. |
| DOC-3 | **`PUT /api/teams` : `level` absent ≠ `level` effacé** | 🟡 | S | `TeamInput::$level` est `?string` : un PUT qui OMET le niveau est indiscernable d'un PUT qui l'efface. Deux sémantiques cohabitent et se contredisent — le commentaire de `TeamInput` documente le PUT partiel (« partial PUTs that omit it still work »), et `TeamApiTest::testLevelIsClearedWhenNullOnUpdate` atteste que `null` EFFACE, délibérément. Conséquence sur une équipe engagée : un renommage par l'API qui n'envoie pas `level` prend un **409 nommant un champ qu'il n'a pas envoyé**, et aucune forme de payload ne dit « renomme, ne touche pas au niveau ». Le wizard y échappe : il ré-écho `level` à chaque PUT. Sur une équipe NON engagée, le même PUT efface le niveau en silence — sémantique **antérieure** dont la garde hérite. Les 4 autres champs non envoyés (`forcedVenueId`, `matchDay`, `minSessionsOverride`, `parentTeamId`) sont passés en « absent = on garde » (P2-7a), car aucun client ne les envoie ; `level`, lui, a un client ET un test qui l'efface — **à trancher par le fondateur**. |
| DOC-2 | **Un match déposé à la fédération peut perdre sa salle sans avertissement** | 🟠 | S | Le périmètre engagé protège les ÉQUIPES (P2-7a) ; rien ne protège la **salle d'un match**. Supprimer un gymnase (`EntityCascadeDeleter::purgeChildrenOfVenue`) ou charger une version antérieure à ce gymnase (`StructureRestorer`) **dépointe** `Fixture.venueId` de n'importe quel match — y compris `SUBMITTED`/`VALIDATED`, c'est-à-dire déjà déclaré à la ligue à CETTE salle. Le match redevient visiblement « à placer » (donc récupérable, contrairement à une équipe balayée : c'est pourquoi l'un vaut un NULL et l'autre un 409), mais le gestionnaire n'est prévenu de rien et devra re-soumettre. **À trancher par le fondateur** : faut-il un avertissement, un refus, ou est-ce acceptable ? Comportement **antérieur** au périmètre engagé. |
| DOC-1 | **Divergence doc↔code : le module matchs est-il découplé du socle ?** | 🟠 | XS | `gestion-matchs-ffbb.md` (l.315/376) et `module-matchs.md` (l.5) affirment que le gating des matchs est **découplé** de la validation du socle. Le code dit l'inverse : `SocleGuard::assertSeasonPlanChosen` gate `FixtureStateProcessor` et `ImportFixturesController`. Divergence **antérieure** à la bascule (intention produit vs code livré). **À trancher par le fondateur** : c'est la doc qui a tort, ou le code ? |
| P3-7 | **Import équipes Excel — UI wizard** | ⚪ | S | backend existe ; l'API FFBB remplacera à terme. ⚠ **Lire P4-35 AVANT de câbler l'écran** : la déduplication actuelle de `FfbbExcelImporter` perd des équipes en silence (200, `created:0`), et le correctif décidé est une **correspondance explicite** faite par le gestionnaire — ça change la maquette, pas seulement le backend |

### P4 — Dette & polish (avant GA, par lots opportunistes)

| # | Sujet | Impact | Effort | Réf |
|---|-------|:---:|:---:|---|
| P4-2 | **ENG-17 + ENG-24** — `coachId` de sortie limité aux slotTemplates → diagnostics coach inertes ; `coach_overload` confond les unités | 🟡 | M | audit |
| P4-3 | **FE1 — 3 composants wizard > 400 lignes** (TeamsStep 552 · ConstraintsStep 498 · VenuesStep 413) — extraire éditeurs/modales en composants frères, la step reste liste + orchestration ; PR dédiée + `/code-review` | ⚪ | M | dette FE1 / UXS-03 |
| P4-4 | **FE2 — registre tu/vous incohérent** (wizard tutoie ~29/15, cockpit vouvoie) — trancher un registre app-wide + sweep | ⚪ | S | dette FE2 |
| P4-7 | **B4 — publish Mercure dupliqué** (`GenerateScheduleHandler`/`ExportPdfHandler::publishProgress`) — extraire un `MercureScheduleNotifier` **seulement si un 3e publisher apparaît** (defer délibéré) | ⚪ | S | dette B4 |
| P4-9 | **Radar « jour férié » à retravailler** (quels fériés + texte d'impact) | ⚪ | S | décision produit |
| P4-10 | **#3b — désactiver « Régénérer » si rien n'a changé** | ⚪ | M | détection de changement fiable |
| P4-12 | **`*PeriodOverride` — parité miroir à durcir (les 2 jumeaux)** : (a) processor `createEntityFromInput` = check-then-insert non-atomique → POST concurrents dupliqués → 500 au lieu de 422 (guard front supprime le trigger réaliste) ; (b) `#[ApiFilter(SearchFilter)]` inerte (provider custom lit les params à la main — ne sert que le snapshot OpenAPI) ; (c) provider ré-implémente `provideCollection` au lieu du hook `applyRequestFilters` de la base. ~~(d) `PeriodConstraints` : override `isActive=true` rendait la case cochable→create→422~~ **✅ résolu** (toggle = upsert-or-delete-to-default, `useUpdatePeriodConstraintOverride`, PR reprise 2026-07-12). (e) la règle de défaut reprise (CLUB/COACH/TEAM gardées, FACILITY droppée) est **dupliquée** PHP `activePermanentForReprise` + TS `defaultKept` sans source partagée (comme le contrat engine, sync manuelle) — une édition unilatérale ferait diverger checklist et payload ; test de parité ou schéma partagé à envisager. Toucher `TeamPeriodOverride` **et** `ConstraintPeriodOverride` ensemble pour (a/b/c) | ⚪ | S | code-review PR #211 (2026-07-12) |
| P4-15 | **`PeriodConstraints` — fidélité checklist vs payload (affichage seul, plan toujours correct)** : le front ne réplique pas toute la logique backend d'`buildForOverlay`. (a) une contrainte **CLUB+targetTag** dont toutes les équipes taguées sont en pause : le backend droppe ses lignes expansées, le front la montre cochée (le front ne résout pas tag→équipes — backend-only) ; (b) sous **erreur de fetch teamOverrides**, l'applicabilité TEAM est inconnue → lock conservateur des toggles TEAM (retire une capacité que `main` avait en closure) — l'inverse (ne pas locker) affiche un strike faux : tradeoff inhérent d'un edge de panne transitoire. Backend post-filtre **autoritaire** dans tous les cas → planning correct. Relève de la dette query-error UX **P4-1** ; à traiter avec une gestion d'erreur systématique des queries wizard | ✅ | S | résolu dans PR1 |
| P4-14 | **`expandClosedVenues` gated closure-only** — une contrainte datée FACILITY `config.type=venue_closed` attachée à une période **holiday/reprise** ne serait PAS expansée en `forbiddenVenueId` (l'engine ignore `venue_closed` brut → NO-OP silencieux). Inatteignable via l'UI aujourd'hui (`venue_closed` n'est créé que par le `ClosureForm` du cockpit sur une période closure) ; à couvrir si la reprise gagne un jour un raccourci « gym fermé ». Étendre l'expansion aux deux types ou verrouiller l'invariant | ✅ | S | résolu dans PR2 |
| P4-13 | **Validation pré-solve des overlays incomplète** — `ValidateConstraintsController` en mode période ne valide que les contraintes **datées** ; or `buildForOverlay` **hérite les permanentes** (closure depuis B3+F2, reprise depuis ce lot). Une permanente HARD héritée insatisfaisable → `valid=true` au récap puis INFEASIBLE au solve (statut `failed`, diagnostics posteriori) au lieu d'un diagnostic amont. Faire valider au controller **le même jeu** que shippe `buildForOverlay` (permanentes filtrées par défaut/override + datées). Advisory (le solve reste correct), pré-existant closure | ✅ | S | résolu dans PR1 |

| P4-16 | **Horloge simulée — le « jour courant » de l'UI ne la suit pas** (§7) : la date du simulateur ne déplace ni le repère « aujourd'hui » du planning ni les CTA/radar qui en dérivent → impossible de rejouer une date pour une démo ou une recette | 🟡 | S/M | constaté 2026-07-17 |
| P4-18 | **DA « ça sent le basket »** (§10) — identité sportive (fond de bandeau, états vides, iconographie) ; assets regroupés en un thème unique | ⚪ | S/M | besoin fondateur 2026-07-17 |
| P4-19 | **Popup de suppression d'une période : l'avertissement rate un cas** — la règle fondateur est « supprimer un événement qui a un plan demande une popup pour que le gestionnaire se rende compte de la PORTÉE de son action ». Le back tient sa part (`deleteEntryAndCascade` emporte le plan et ses versions) ; c'est le front qui sous-avertit. `DayDialog.tsx` conditionne son texte et son `destructive` sur **`toDelete?.overlayScheduleId`** — or ce pointeur ne nomme que la version **ACTIVE**, et `OverlayManager::newestOtherOverlayId` le remet à **null** dès que la seule sœur survivante n'est pas COMPLETED. **Cas réel** : une période a V1 (COMPLETED) et V2 (PENDING) → on supprime V1 → `overlayScheduleId` tombe à null → supprimer la période affiche « Cette entrée sera retirée du calendrier » (bénin, `destructive=false`) alors qu'elle emporte le plan — son **nom**, son **compteur monotone** — et V2. Le gestionnaire ne voit pas la portée : exactement ce que la règle veut empêcher. **Même racine** que les défauts du lot C1 (rounds 1-2) : `overlayScheduleId` ≠ « cette période porte un planning ». **Fix** : conditionner sur « la période porte des VERSIONS », pas sur le pointeur actif — attention, pas sur « porte un plan » : depuis C1 toute closure/holiday en a un, y compris vide, et on sur-avertirait pour une suppression qui ne perd rien. Le signal n'existe pas encore au contrat (ni `lastVersionNumber`, monotone et donc >0 même après suppression de toutes les versions) : un champ dérivé batché est à exposer, sur le motif de `Schedule.isChosen`. Préexistant, hors périmètre C1. **Résolu par PR4 (lot D-b, 2026-07-18)** : `overlayScheduleId` **supprimé** (racine du défaut) ; **décision fondateur (2026-07-18)** — supprimer une période supprime son PLAN, donc TOUTES ses versions, et le gestionnaire doit en valider la PORTÉE. `DayDialog` arme désormais son avertissement destructif sur « le plan de la période porte ≥ 1 version » (brouillon inclus : la cascade les emporte), dérivé côté front des `useSchedules()` filtrées par `schedulePlanId` (pas de nouveau champ API) ; un plan vide (aucune version) garde le message bénin | ✅ | S | résolu par PR4 (lot D-b, 2026-07-18) |
| P4-23 | **`DayDialog` charge toute la collection des schedules pour un booléen** — depuis PR4 (lot D-b), la garde destructive de suppression d'une période dérive « le plan porte ≥ 1 version » de `useSchedules()` filtrées par `schedulePlanId`. Fonctionnellement correct (fail-closed pendant le chargement), mais tire **toute** la collection saison/période côté client pour répondre à une existence par plan. Mitigé en pratique (cache react-query partagé avec le cockpit/planning), mais sur un club à nombreuses versions c'est un transfert inutile. **Fix propre** : exposer un champ dérivé **batché** `hasVersions` sur `SchedulePlanResource` (sur le motif de `Schedule.isChosen` — enrichissement dans `SchedulePlanStateProvider`, une requête `COUNT` groupée), puis `DayDialog` lit `plan.hasVersions` (une seule query, plus de `useSchedules`). Même champ utile à d'autres consommateurs « cette période a-t-elle du travail ? » | ⚪ | S | constaté 2026-07-18 (code-review PR4) |
| P4-25 | **Écritures API Platform sans verrou optimiste (transverse)** — le PUT générique (`AbstractStateProcessor::processPut`) remplace TOUS les champs éditables sans vérifier la colonne `version` : deux éditeurs concurrents de la MÊME ligne (deux onglets, cache react-query périmé 30s) → l’écriture la plus tardive écrase silencieusement l’autre (lost update). Constaté sur `CoachWish` (revue #10 C1 round 2 : cocher « traité » depuis un cache périmé annule une édition concurrente), mais **universel** — `TeamPeriodOverride`, `VenuePeriodOverride`, etc. partagent la sémantique. Non spécifique à C1, hors scope d’un lot feature. **Fix propre** : If-Match/ETag ou garde de `version` dans `processPut` (409 sur conflit), à câbler une fois pour toutes les ressources. Fenêtre étroite (édition concurrente de la même ligne < 30s) → ⚪. | ⚪ | M | constaté 2026-07-25 (code-review #10 C1 round 2) |
| P4-26 | **Deux coachs d'une même équipe partagent la doléance de la semaine** — `CoachWish` est clé sur `(calendarEntryId, teamId, weekStart)` **sans dimension coach** (design C1) : une équipe à MAIN + ASSISTANT n'a qu'UNE doléance par semaine. Sur la collecte C2, si le coach A saisit puis le co-coach B rouvre son lien (pré-rempli de l'état courant) et saisit une autre valeur, `CoachWishUpserter::upsert` écrase la ligne de A et re-stampe `coachId=B`. **Atténué** — le pré-remplissage montre à B la valeur courante (B édite en connaissance, pas d'écrasement aveugle) et « un souhait, jamais une contrainte ». Mais la todo-list ne garde qu'un avis par équipe/semaine. Décision fondateur explicite (« un souhait par équipe × semaine ») → laissé tel quel en V0 ; si un club veut distinguer les avis par coach, ajouter `coachId` à la clé (impact C1). **Même famille** que P4-25 (lost update). | ⚪ | M | constaté 2026-07-25 (code-review #10 C2) |
| P4-8 | **`resolveClubId` choisit arbitrairement pour un gestionnaire multi-club** (ex-BCK-10, reformulée 2026-07-26) — `MembershipController` consomme désormais le club de la REQUÊTE via `ResolvesCurrentClubTrait` (plus de second tirage indépendant), mais le tirage lui-même reste arbitraire : le front n'envoie pas `X-Club-Id`, donc `TenantFilterListener::resolveClubId` retombe sur `findOneBy(['userId','isActive'])` **sans ORDER BY**. Pour un humain gérant deux clubs, « quel club est courant ? » n'a pas de réponse définie — et la policy RLS `club_user_read` est `USING (true)`, donc c'est le filtre Doctrine (pas RLS) qui borne la lecture. **Fix = choix produit** (sélecteur de club explicite, ou club « par défaut » sur l'adhésion) → rattaché à **P1-1**. | ⚪ | M | rouverte 2026-07-26 (code-review #301) |
| P4-27 | **Presenter de campagne exécuté deux fois par écriture** — `CoachWishCampaignStateProcessor` : `parent::process*` projette déjà via `CoachWishCampaignPresenter`, le résultat est jeté, puis le presenter re-tourne après `tokenSync->sync()` (indispensable : les tokens n'existent pas au 1er passage). **Tentative 2026-07-26 abandonnée** : déplacer la sync dans `afterPersist` la place dans un hook que le processor de base réserve explicitement aux effets de bord FACULTATIFS et post-commit — or créer les tokens est obligatoire (sans token, aucun lien de collecte). Le vrai fix demande au processor de base une porte « écrire sans projeter » (ex. `projectAfterWrite()` surchargeable), donc une modification du socle partagé — pas un quick win. Coût réel : un presenter en trop sur une écriture rare. | ⚪ | S/M | constaté 2026-07-25, re-cadré 2026-07-26 |
| P4-28 | **`GET /api/admin/health` : sondes réseau séquentielles + scan Redis intégral** (console admin) — `AdminHealthService` lance ~6 sondes HTTP/TCP + 3 dépendances externes **une à une** (une dépendance lente stalle toute la réponse plusieurs secondes, poll 30 s) ; `AdminMessengerFailedController::fromRedisStream` **bufferise tout le stream** en mémoire à chaque requête pour paginer (coût proportionnel au nb total d'échecs, pas à la page). Non bloquant : le transport failed est normalement minuscule et le health reste fonctionnel. **Fix** : sondes concurrentes (HttpClient multiplexé — borne la latence à la sonde la plus lente) ; pagination Redis native (curseur). Constaté 2026-07-25 (code-review console admin, findings [5][6] ; le double-probe [7] déjà corrigé). | ⚪ | M | constaté 2026-07-25 (code-review console admin) |
| P4-35 | **Import d'équipes FFBB : la déduplication ne regarde aucune identité d'équipe** — `FfbbExcelImporter.php:84` demande `findOneBy(['clubId', 'seasonId'])`, soit « cette saison a-t-elle UNE équipe ? » au lieu de « cette équipe-ci existe-t-elle ? ». **Mesuré sur la base de test** (aucun test n'existait pour cet importeur) : saison vierge + 4 lignes de 4 catégories → `created=2, skipped=2` ; 1 équipe créée à la main puis import de 3 lignes → `created=0, skipped=3` ; dans tous les cas l'API répond **200**. Le cas « saison vierge » vient d'un effet de bord : `findOrCreateSportCategory` appelle `flush()` quand il CRÉE une catégorie, ce qui rend visible l'équipe de la ligne précédente à la requête de dédup suivante — le nombre d'équipes importées dépend donc de l'ordre et de la diversité des catégories du fichier. ⚠ **Sévérité corrigée de 🔴 à ⚪ (2026-07-27)** : le scénario est **inatteignable par l'UI** — `POST /api/clubs/{id}/import-teams` n'est appelé nulle part dans le frontend (seul l'import de RENCONTRES l'est). Le classement initial en 🔴 venait d'une analyse du code sans vérification du chemin utilisateur. Redevient bloquant le jour où **P3-7** câble l'écran. **Correctif décidé (fondateur, 2026-07-27) : une correspondance EXPLICITE, pas une clé naturelle.** Le gestionnaire apparie les lignes du fichier avec ses équipes ; les lignes non appariées créent des équipes qu'il adapte ensuite. La devinette disparaît, donc mes deux questions d'arbitrage (`Nom` ou `Numéro` ? mettre à jour ou ignorer ?) tombent. La correspondance doit être **persistée** pour que le ré-import n'oblige pas à la refaire — exactement le rôle que `Fixture.externalRef` joue déjà côté rencontres. À noter : **l'import de rencontres applique DÉJÀ cette règle** (`ImportFixturesController` : « the target team is chosen at upload, the parser never guesses it »), à un cran d'ergonomie près (un fichier par équipe plutôt qu'un fichier global mappé ligne à ligne). Reste purement technique quel que soit le design : le `flush()` en plein milieu de la boucle laisse des écritures partielles si l'import casse en cours de route. | ⚪ | M | constaté 2026-07-27 (code-review PR #311), recadré le même jour |
| P4-34 | **Quatre ancres `schedulePlanId` restent sans FK ni garde d'existence** — P4-30 a couvert `Reservation` et `VenueTrainingSlot` (les deux nommées par la ligne d'origine). Mais `VenuePeriodOverride`, `TeamPeriodOverride`, `ConstraintPeriodOverride` et `Schedule` acceptent eux aussi un `schedulePlanId` en écriture, sans FK et sans `AssertsSchedulePlanExistsTrait` : une ancre inventée y crée toujours une ligne orpheline silencieuse. Même correctif, même migration en deux temps, même NR ; l'asymétrie actuelle est assumée pour borner la PR, pas parce que ces tables seraient différentes. Preuve : `backend/src/Dto/{VenuePeriodOverrideInput,TeamPeriodOverrideInput,ConstraintPeriodOverrideInput,ScheduleInput}.php`. | ⚪ | S | constaté 2026-07-27 (PR P4-5+P4-30) |
| P4-33 | **L'e2e Playwright n'est pas dockerisé** — c'est le SEUL maillon qui exige du Node sur l'hôte, ce qui contredit le contrat « l'hôte n'a besoin que de Docker, Docker Compose et Make » (CLAUDE.md §10.1). La CI l'exécute sur le runner GitHub ; en local il faut `npm ci` + `npx playwright install`, et le parcours casse de deux façons observées le 2026-07-27 : le postinstall de **puppeteer** (dépendance du pdf-worker, sans rapport avec l'e2e) échoue sur un `~/.cache/puppeteer` contenant des dossiers de navigateur vides d'un téléchargement avorté, et `playwright install --with-deps` réclame `sudo`. Contournement connu : `rm -rf ~/.cache/puppeteer && PUPPETEER_SKIP_DOWNLOAD=1 npm ci && npx playwright install chromium`. Conséquence : impossible de reproduire un e2e rouge en CI sans refaire ce parcours. **Fix** : service compose sur l'image officielle `mcr.microsoft.com/playwright` **épinglée à la version du `@playwright/test` du projet** + cible `make -C frontend e2e`. ⚠ L'épinglage FAIT la valeur : les tests de contraste mesurent un rendu, donc un chromium de version différente déplacerait le problème au lieu de le régler (on aurait trois navigateurs en circulation : hôte, conteneur, runner). | ⚪ | S/M | constaté 2026-07-27 (PR #309) |

### Parking (idées gardées, non cadrées)

- **Reverse-engineering des contraintes** (idée club #92) — fort attrait, effort XL, aucun cadrage.
- **Réservation salle de convivialité** (V2 « club hub ») — triviale en soi, bloquée sur P1-1.
- **Gymnase — paramètre « utilisé en match »** (besoin fondateur 2026-07-20) : chaque `Venue` porte un booléen (précédent : `isExternal`/`canSplit` déjà des flags simples sur l'entité) ; l'écran matchs (placement domicile, [`gestion-matchs-ffbb.md`](gestion-matchs-ffbb.md) §Temps 1 — heure + salle) ne propose que les gymnases où ce flag est vrai. Non cadré : où l'admin bascule le flag (annuaire des gymnases ?), défaut à la création (`false` ou `true` ?), impact sur les gymnases déjà utilisés dans des matchs existants si on le décoche après coup.

### Ordre d'attaque conseillé

1. ~~**P0 d'abord**~~ — **tous soldés** (RGPD, prod, backups, observabilité, déploiement).
2. **P2-9 (lock HARD qui annule les contraintes)** — seul 🔴 ouvert, et il touche la promesse centrale du produit.
3. **P1-1 (rôles)** — le verrou qui débloque le plus de valeur aval (et solde P4-8 au passage).
4. **P1-3 (bridage freemium)** — pas de monétisation sans lui.
5. Puis **P2** selon l'appétit commercial, **P3/P4** par lots opportunistes.

---

## Dette technique — état vivant

> Ex-`docs/technical-debt.md` (absorbé le 2026-07-11). Règle d'origine conservée : **un item de
> dette n'existe qu'avec une preuve** (`fichier:ligne`). Les dettes actionnables vivent comme
> lignes **P4-x** ci-dessus (même pipeline que tout le reste) ; cette section ne garde que les
> **keeps délibérés** (décisions « ne pas corriger » qu'il faut pouvoir retrouver) et le solde.

| Item | Décision | Preuve / raison |
|------|----------|-----------------|
| **FE3 — ambre codé en dur dans `MonthCalendar.tsx`** (pas le token `--warning`) | 🟩 **keep délibéré** | `--warning` clair = 2.9:1 sur fond (A11Y-06) → l'utiliser sur le label vacances **échouerait WCAG AA** ; `amber-700` passe. Migrer = régression a11y contre un résidu de cohérence |
| **B4 — publish Mercure dupliqué** | 🟩 defer (ligne P4-7) | 2 handlers, payloads distincts — extraire au 3e publisher |
| **DP1 — contacts FFBB sur `club`** | ✅ soldé avec P0-1 (2026-07-11) | base légale intérêt légitime actée, survivent à la purge (annuaire adverse), export inclus — `docs/security/rgpd.md` §2 |
| **Dette `TeamTagService::syncTeamTags`** (efface les tags custom à chaque édition) | 🟧 à corriger avant de fiabiliser les tags custom | ligne `team_tags` §9 ; bloque le report des tags custom entre saisons |

**Soldé** (détail dans l'historique git de `docs/technical-debt.md`, supprimé le 2026-07-11) :
B1 (Rector 8.4) · B2 (PHPUnit unifié) · B3 (TenantCacheIsolationTest réel) · B6 (attributs PHPUnit 11) ·
B7 (tag LOISIR fixture) · E1-E6 (aliases morts, helpers dédupliqués, ADR-0001 single-pass, doc timeout,
TODOs PREFERRED TIME) — tous résolus le 2026-07-01.

---

## Livrés — traces datées (depuis la fusion du 2026-07-11)

> Quand un item du backlog est livré : sa ligne P*x-y* est **supprimée** des tableaux ci-dessus,
> la ligne correspondante de la carte (§1-§10) passe ✅ avec pointeur, et une ligne s'ajoute ici
> (date · id · sujet · où c'est documenté). C'est la trace que `documentation-update` maintient.

| Date | Id | Sujet | Documenté dans |
|------|----|-------|----------------|
| 2026-07-27 | P4-32 | **Le gate Rector bloque enfin** — le job `Rector (style gate)` tournait et pouvait rougir depuis son introduction (P4-24) sans jamais empêcher un merge : il n'était pas dans les *required status checks* de `main`. La CI affichait donc un garde-fou que le dépôt n'appliquait pas. Contexte ajouté par le fondateur (10 contextes requis désormais). À noter pour la suite : le dépôt porte **deux systèmes de protection en parallèle** — un ruleset « Basic » (actif, mais ne contenant que `deletion` et `non_fast_forward`) ET la protection de branche classique, qui est celle qui porte les checks. Les deux écrans se ressemblent et ne pilotent pas la même chose. Au passage : les 5 labels référencés par `.github/dependabot.yml` (`dependencies`, `engine`, `frontend`, `backend`, `ci`) n'existaient pas — Dependabot le signalait sur chacune de ses PR. Créés. | réglage dépôt · CLAUDE.md §4 |
| 2026-07-27 | P4-5 + P4-30 | **Deux dettes backend soldées.** **P4-5** — les contrôleurs d'import relayaient `$e->getMessage()` en se fiant à la CLASSE de l'exception. Or `PhpOffice\PhpSpreadsheet\Reader\Exception` **étend `RuntimeException`** : la chaîne d'une librairie tombait dans le même `catch` et partait en 422. Reproduit — `File "/var/www/html/var/tmp/php7Zx9Qa" does not exist or is not readable.` dans le toast du gestionnaire. Désormais seul `ImportRejectedException` (type dédié, porté par les jets MÉTIER des importeurs) est relayé ; tout le reste est journalisé et remplacé par un message générique. NR `ImportErrorMessageLeakTest` mutation-testé **sur les deux bords** : le second compte autant que le premier, car tout masquer fermerait la fuite en rendant l'import muet sur ce qu'il faut corriger. **P4-30** — `Reservation.schedulePlanId` et `VenueTrainingSlot.schedulePlanId` gagnent une FK `ON DELETE CASCADE` (migration en deux temps : purge des orphelins PUIS pose de la contrainte — PostgreSQL refuse une FK que des lignes violent). `SET NULL` écarté : `NULL` = « ligne de base partagée par la saison », donc il aurait transformé les créneaux d'une période supprimée en créneaux permanents, pris au sérieux par le solveur. **Ce que la FK a révélé** : l'API acceptait un `schedulePlanId` ne désignant AUCUN plan — orphelin silencieux avant, **500** après. Corrigé par `AssertsSchedulePlanExistsTrait` → **422**. Quatre tests ancraient sur des identifiants inventés (`3333…`) : ils créaient donc la dette qu'ils étaient censés ignorer, et passent par un vrai plan (`CreatesPeriodPlanTrait`). ⚠ **Piège tracé** : Doctrine ignore ces FK (colonne `guid` nue, aucun `ManyToOne` — même forme que `coach_wish_token`), donc `migration-diff` propose de les DROPper ; `SchedulePlanAnchorCascadeTest` (phase1, mutation-testé) échoue si quelqu'un committe ce DROP. Vérifié : PHPStan, CS-Fixer, Rector, **948 tests**, smoke-solver COMPLETED. | code + tests · migration `Version20260727120000` |
| 2026-07-27 | P4-31 | **Stack Symfony réaligné sur la LTS 7.4** — 19 paquets `symfony/*` tournaient en **8.0.x** (`http-kernel`, `http-foundation`, `dependency-injection`, `config`, `routing`, `cache`, `error-handler`, `security-core`, `password-hasher` + 10 utilitaires) sous des bundles 7.4.x. **Cause (corrigée en revue — la première analyse était fausse)** : `extra.symfony.require` couvre bien TOUS les splits Symfony, transitifs compris — `PackageFilter::removeLegacyPackages()` ne teste jamais si le paquet est en require directe, et 9 splits jamais nommés (`event-dispatcher`, `security-http`, `http-client`, `doctrine-bridge`…) étaient d'ailleurs restés en 7.4. La vraie échappatoire est l'**exemption par le lock** : Flex laisse passer tout paquet dont la version candidate est déjà celle du lock, donc une fois `http-kernel` verrouillé en 8.0.14, aucune mise à jour partielle ne l'en sort. **Correctif** : `composer update` sur les 19 — `composer.json` n'est PAS touché. Une première version de cette PR les épinglait en `7.4.*` ; les pins étaient inutiles (vérifié : sur le `composer.json` de `main`, sans aucun pin, un `composer update` fait redescendre les 19 et Flex l'annonce — « Restricting packages listed in symfony/symfony to 7.4.* »). **Enjeu** : 8.0 sort de support en **juillet 2026** — le projet tournait déjà sur une branche en fin de vie, alors que 7.4 **est la LTS**, au-delà de la commercialisation visée mi-2027. La 8.1 n'est PAS une LTS (fin jan. 2027) ; la prochaine est la 8.4. **Le `withSkip` de `rector.php` disparaît** : sans set Symfony 8.0 chargé, `RemoveEraseCredentialsRector` n'est plus enregistrée — Rector le signalait lui-même. NR `SymfonyStackAlignmentTest` : lit les versions **INSTALLÉES** (`Composer\InstalledVersions`), pas le lock — c'est l'installé que lisent Rector, PHPStan et le conteneur, et un lock propre au-dessus d'un `vendor/` périmé est exactement là où la dérive frappe sans se voir. Hors du gate bloquant **à dessein** (une chaîne de version ne doit pas gater `build-docker` ni précéder les tests d'isolation tenant — même raison que Rector en P4-24 b). Vérifié : PHPStan, CS-Fixer, Rector, `composer audit`, 942 tests, smoke-solver COMPLETED, e2e 19 tests. | `backend/composer.lock` · `backend/rector.php` · CLAUDE.md §5 |
| 2026-07-26 | P4-24 (correctifs) | **Retombées de l'adoption Rector, corrigées** — (a) `RemoveEraseCredentialsRector` avait supprimé `User::eraseCredentials()` : la règle lit la version INSTALLÉE de `security-core` (transitivement **8.0.14**) alors que le projet cible 7.4 (`extra.symfony.require`, tout le reste du stack en 7.4.x). Le code compilait, mais un réalignement de la dépendance rendait `User` non instanciable — fatal au boot du conteneur, **toute l'authentification tombait**. Risque **vérifié empiriquement** le 2026-07-27 : épingler réellement `symfony/security-core` en `7.4.*` fait échouer `cache:clear` (`contains 1 abstract method`) tant que la méthode manque — d'où l'ordre imposé, cette PR **avant** le réalignement 7.4 (P4-31). Méthode restaurée **sur `User` ET `SuperAdmin`** (l'oubli initial ne visait que `User` : `SuperAdmin` est chargée par le firewall admin, donc l'omission aurait tué le conteneur entier, pas seulement l'auth club), règle en `withSkip` (seule exception au « on adopte le standard »), NR `UserInterfaceContractTest` mutation-testé. (b) Le gate Rector quitte le job `phpstan` pour un **job dédié sans `needs`** (⚠ à ajouter aux required status checks de `main` pour qu'il bloque réellement le merge — réglage repo) — même forme que `dependency-audit` : dans `phpstan` il faisait sauter `blocking-tests` (isolation tenant/RLS) et `build-docker` pour de la dérive de STYLE, un bump dependabot prenant le déploiement en otage. (c) Périmètre élargi à `tests/` (10 fichiers encore hors convention : le split-brain que P4-24 visait). (d) Job CI renommé. | `.github/workflows/ci.yml` · `backend/rector.php` · CLAUDE.md §4/§5 |
| 2026-07-26 | P4-17 | **Le spécifique-basket rangé, couche par couche** — 13 classes déplacées dans un sous-dossier `Basketball/` LOCAL à chaque couche (`Entity/`, `Repository/`, `Service/`, `Controller/`, `Message/`, `MessageHandler/`), jamais un dossier transverse : chaque couche garde sa place, on isole le basket à l'intérieur. `App\Sport\BasketballCategoryCatalog` → `App\Service\Basketball\CategoryCatalog` (le dossier porte le sport, la classe perd le préfixe redondant) ; le dossier `Sport/` disparaît. Le préfixe `Ffbb*` RESTE : il désigne la fédération, pas le sport (`Basketball/League` serait ambigu avec `Competition`). Aucune migration (tables explicites `#[ORM\Table]`), mapping Doctrine récursif intact. **Le vrai risque n'était pas les `use`** mais le routage Messenger par FQCN (`messenger.yaml` + `test/messenger.yaml`) : un oubli y aurait dé-routé l'autofill FFBB du register en silence — vérifié par `debug:messenger` (toujours `async`, handler associé). 86 fichiers référençaient ces classes. | code · specs `frontend-wizard` / `backend-inventory` / `gestion-matchs-ffbb` (références au catalogue renommées) |
| 2026-07-26 | P4-24 | **Style Rector adopté et rendu BLOQUANT** — les trois décisions fondateur du 2026-07-20 appliquées telles quelles : (1) le standard Rector devient la convention (`!$x instanceof FQCN` pour les checks `?FQCN`) plutôt qu'un `withSkip` qui l'aurait figé — **54 fichiers** convergés (la dérive avait grossi depuis les 45 constatés) ; (2) CS-Fixer aligné : `fully_qualified_strict_types` passe à `['import_symbols' => true]`, sans quoi il ne raccourcissait les FQCN inline écrits par Rector que dans les fichiers où la classe était DÉJÀ importée — deux styles dans un même fichier ; (3) `composer rector -- --dry-run` ajouté au job bloquant `phpstan`. Convergence vérifiée dans les deux sens : Rector « is done », CS-Fixer « 0 of 662 files ». La cause racine était l'absence de gate — un bump `rector/rector` (groupe composer, dependabot) élargit une règle et personne ne le voit avant merge. | code + `.github/workflows/ci.yml` |
| 2026-07-26 | P4-6 + P4-11 | **Bundle découpé par route** : un chunk unique de **834 kB** (241 kB gzip) partait à chaque première visite — console superadmin et wizard compris, y compris pour un coach n'ouvrant qu'une page publique de doléances. Les routes passent en `lazy` de react-router : entrée à **302 kB** (95 kB gzip), soit **−64 %**, et le wizard (149 kB), la console admin (57 kB), le cockpit (50 kB) ne partent qu'à leur navigation. Un split `manualChunks` vendor a été essayé puis **écarté** : il faisait GROSSIR l'entrée (383 kB) — rolldown extrait déjà mieux seul ; le gain de cache ne payait pas le coût au premier chargement. **P4-11 : husky + lint-staged RETIRÉS** — le hook `pre-commit` n'avait jamais tourné (`core.hooksPath` jamais positionné : `prepare: husky` s'exécute dans l'image Docker, pas sur l'hôte), et un hook Node contredirait le contrat « l'hôte n'a besoin que de Docker, Docker Compose et Make » (CLAUDE.md §10.1). La CI porte déjà le filet : lint + `tsc -b` + build + vitest. | code (aucune doc de comportement impactée) |
| 2026-07-26 | SEC-08 résiduel (partie 5xx) | **Un 5xx ne parle plus au nom du serveur** : `errorMessage` ne reprend la chaîne fournie par le serveur que pour les **4xx** (messages métier écrits pour être lus) ; au-delà, elle est remplacée par « Erreur serveur. Réessayez plus tard. » — un 5xx n'est pas actionnable et son corps peut porter un message de driver ou d'upstream. NR mutation-testé. **La ligne P4-5 reste OUVERTE** : plusieurs contrôleurs renvoient encore `$e->getMessage()` avec un statut 4xx (`ImportController`, `ImportFixturesController`), donc le résidu vit maintenant côté backend. | code + tests |
| 2026-07-26 | P4-20 + P4-1 | **L'ancre de période dit toujours son état** — union discriminée `PeriodAnchor = loading \| failed \| base \| period(planId)` (le booléen `ready` recouvrait trois situations et se laissait oublier : 4 fois en 4 revues, dont une en tentant de le corriger par un `isError` ajouté au tuple). Le compilateur a imposé les **8 call sites**. Les écrans période passent par `PeriodAnchorGate`, une PORTE qui ne livre le `planId` qu'après avoir traité `loading` et `failed` — oublier l'échec ne compile plus. Décisions fondateur : **Générer** grisé + message + Réessayer ; **récap BLOQUANT et explicite** quand l'ancre a échoué (jamais un vert sur une période cassée, jamais un `pending` qui ressemble à « ça charge »). **P4-1 traité avec** : les listes de période (`usePeriodSlots`, `useVenuePeriodOverrides`, `useTeamPeriodOverrides`) ne sont plus coercées en `[]` — un GET raté rendait une grille **vide et crédible** qui poussait à re-saisir (doublons de créneaux) ou à valider une période crue sans entraînement. Subtilité gardée par NR : une ancre **en cache** survit à un refetch raté (react-query garde `data` et bascule `status` — basculer l'écran en erreur détruirait une vue qui marche). 5 NR, tous mutation-testés. | code + tests |
| 2026-07-26 | Quick wins backend (P4-21, P4-22a, P4-29) | **Passe de dette backend** : le NR C3 `testPeriodLayersHangOffThePlanAndNullStillMeansShared` passe par le **chemin de prod** (`ScheduleConstraintBuilder::buildForClubSeason`, avec purge de `cache.schedule` pour que l'assertion ne dépende pas de l'adaptateur array de l'env de test) au lieu de l'EntityManager — il testait le mapping Doctrine, il garde désormais le filtre d'ancre (mutation-testé) · **UUID validés en écriture ET en lecture** : `#[Assert\Uuid]` sur les identifiants des DTO (`ReservationInput.teamId/venueId/schedulePlanId`, `ScheduleInput.schedulePlanId`) **+ `#[Assert\NotBlank(allowNull: true)]`** — le validateur Uuid de Symfony laisse passer la chaîne vide — et `ReadsUuidQueryParamTrait` sur les **12 providers de collection** qui lient un paramètre à une colonne uuid (`schedulePlanId`, `venueId`, `teamId`, `seasonId`, `constraintId`, `calendarEntryId`, `scheduleId`) : **400** au lieu d'un 500 PostgreSQL `invalid input syntax for type uuid` (qui, sous le harnais, avortait la transaction DAMA et empoisonnait la suite du test). ABSENT ⇒ défaut du provider, PRÉSENT-MAIS-VIDE ⇒ 400 (les confondre ferait répondre au filtre d'une période par les données du socle). Gardé par `UuidQueryParamGuardTest` (23 cas, mutation-testé) · sonde mailpit **fail-closed** (jamais en prod, et seulement si le DSN pointe mailpit), 2 NR mutation-testés ; la carte mailpit n'entrait PAS dans le statut global (l'affirmation initiale était fausse, corrigée partout). **P4-8 et P4-27 restent OUVERTES** (voir backlog) : leurs correctifs se sont révélés plus étroits que la ligne. | code + tests · [`console-superadmin.md`](console-superadmin.md) |
| 2026-07-26 | P0-2 (suite) — deploy | **Déploiement une commande** : workflow `deploy.yml` (tag `v*` OU `make deploy`/`gh workflow run`) → build+push des 6 images prod sur **ghcr.io** (cache gha ; **court-circuit registre** : une version déjà publiée est redéployée telle quelle, jamais rebuildée/écrasée) → job SSH dormant tant que `DEPLOY_ENABLED≠true` (mergeable avant toute VM) → scp du compose+script versionnés puis `remote-deploy.sh` **fail-closed** : dump pré-migration OBLIGATOIRE (`run --rm --no-deps`, détection première install par volume postgres) → pull → **migrate via la nouvelle image avant la bascule** → `up -d --wait` → sondes → pin VERSION en dernier. Rollback = redéployer le tag précédent (images d'origine réutilisées). **Runbook fondateur pas-à-pas** + convention migrations rétro-compatibles. | [`docs/ops/deploy.md`](../../docs/ops/deploy.md) |
| 2026-07-25 | P0-2 + INF-03 | **Stack de production** : `docker-compose.prod.yml` autonome (images immuables ghcr.io, zéro bind-mount, un seul port publié), stage `prod` php (COPY + `--no-dev` + opcache + rclone), edge prod **sans `/engine/`**, init postgres paramétré (`02-users.sh`, plus de mot de passe en dur), `mem_limit` v3 §2.2 + rotation logs + `restart` (INF-03 fermé), `.env.prod.dist`, hook off-site opérationnel (rclone dans l'image, doc pas-à-pas). Répétition locale : 10/10 healthy, génération COMPLETED, hook exécuté. CI build les targets prod (anti-rot). | [`docs/ops/prod-stack.md`](../../docs/ops/prod-stack.md) · [`docs/ops/backup-restore.md`](../../docs/ops/backup-restore.md) §4bis |
| 2026-07-25 | ALIGN-07 (audit 2026-07-19) | **Verrou HARD sur créneau divisible = comportement assumé** (pas un bug) : une réservation HARD prend le **créneau entier** même si `capacity>1` (`blocked_venue_slots`, model.py) — l'équipe épinglée est seule, décision gestionnaire ; partager = co-épingler explicitement les N équipes (picker borné à `capacity`, aucun diagnostic tant que N ≤ capacity). Aucun changement solveur ; figé par 3 NR sémantiques + mutation-testés. | [`constraint-coverage.md`](../../backend/docs/constraint-coverage.md) · [`engine-inventory.md`](../courantes/engine-inventory.md) · `engine/tests/semantic/test_hard_lock_divisible_slot.py` |
| 2026-07-25 | P2-1 (collecte coach, #10) | **Doléances coachs C1/C2/C3** : todo-list (`CoachWish`, 2 vues) · collecte tokenisée SANS login (`CoachWishCampaign`/`CoachWishToken`, page publique `/doleances/{token}`, badge radar) · emails (envoi liens, digest quotidien 7h conditionnel, récap final, relance 1×/jour). Sécurité durcie (token en clair assumé, saison read-only calendaire, périmètre borné, anti-énumération). | [`types-de-planning.md`](../courantes/types-de-planning.md) E5 · [`backend-inventory.md`](../courantes/backend-inventory.md) §2 |
| 2026-07-25 | SA (console onglets + monitoring) | **Console super-admin en 6 onglets** (a11y tablist, deep-link `?tab`, back-button) + **monitoring** : `/api/admin/health` étendu append-only (`containers[]` 11 services, `externalDependencies[]`) + heartbeats cron & pdf-worker + 3 journaux read-only (audit / échecs async / erreurs système). Restart conteneur **retiré** (`docker.sock` non transposable prod). | [`superadmin-auth.md`](../courantes/superadmin-auth.md) · [`console-superadmin.md`](console-superadmin.md) |
| 2026-07-25 | FF#19 (autofill FFBB, lot C) | **Autofill club/ligue/comité + logos** à la création (`FfbbClubPopulator` async au verifyEmail + `POST /api/club/ffbb-import` manuel) : `FfbbLeague`/`FfbbCommittee` = tables de référence partagées cache-first, logos ré-hébergés, 3 blocs contact sur la page club. SSRF-safe (hosts const, redirects off). Fichier `import-ffbb-autofill.md` supprimé (gradué). | [`backend/docs/ffbb-api.md`](../../backend/docs/ffbb-api.md) · [`backend-inventory.md`](../courantes/backend-inventory.md) |
| 2026-07-25 | P2-3 (versions de planning) | **Versions de l'espace de travail** (savepoints) : D1 (UX versions) → D2 (`ScheduleStructureSnapshot`) → D3 (`regenerate-from`) → D3bis (`regenerate` linéaire) → D3ter (versions d'overlay + `app:overlays:purge`) → D3quater (ids de créneau scopés par schedule, P0-5). Rebâti par la bascule ADR-0002 (pointeur du plan, plus de statut). Fichier `planning-versions.md` supprimé (gradué). | [`adr-0002-pattern-plan.md`](../../docs/architecture/adr-0002-pattern-plan.md) · [`planning-lifecycle-validated.md`](../courantes/planning-lifecycle-validated.md) |
| 2026-07-19 | P2-5 (E3+E4+E6) | **Clôture des écarts types-de-planning** (dernière ligne P2-5 soldée). **E3** : défaut d'équipes conscient du type — reprise = Fanion + importantes (2 premiers rangs), fermeture = tout le club actif (structure verrouillée, loisir décochable) ; seed de `PeriodTeams`. **E4** : séances/équipe déjà exposées dans l'UI période (champ 1–7 + toggle = 0 séance, `TeamPeriodOverride`, closure comme reprise) — arrivé avec #262, tracé ici. **E6** : noms de plan par défaut conformes côté serveur (`SchedulePlanProvisioner`, source unique, ADR-0002 inv. 12) — CLOSURE `Ajustement {gymnase} du … au …`, HOLIDAY `Planning de vacances de … du … au …`, distincts du `CalendarEntry.title`. NR : `SchedulePlanProvisionerTest` (noms) + `PeriodStructure.test.tsx` (seed par type). E5 (modale coachs) suit sous P2-1 | [`types-de-planning.md`](../courantes/types-de-planning.md) (E3/E4/E6) |
| 2026-07-18 | P2-5 (5b) | **Granularité JOUR des fermetures** : un gymnase fermé une partie de la semaine n'est indispo QUE ses jours réellement fermés (incident ∩ fenêtre) — `App\Service\VenueClosureDays` retire ses créneaux du payload ces jours-là dans `buildForOverlay`, conflits day-précis (`CalendarEntryConflictsController`), `expandClosedVenues` (forbid tous-jours, day-blind) supprimé. Zéro engine, zéro migration. NR sémantiques réécrits + smoke COMPLETED | [`types-de-planning.md`](../courantes/types-de-planning.md) (E1) |
| 2026-07-18 | P2-5 (E1+E2) | **Plans de période à la semaine** (version fondateur) : période > 7 j → choix des semaines (lun→dim ∩ saison), une entrée enfant + un plan par semaine (`CalendarEntry.parentEntryId`, rail 1 entrée = 1 plan intact), exclusivité bloc/semaines (422/409), couverture par chips, datées héritées de la mère ; été adaptable. Reste E3/E4/E6 (ligne P2-5) | [`types-de-planning.md`](../courantes/types-de-planning.md) (E1/E2) |
| 2026-07-18 | P0-3 | **Backups PostgreSQL** — `pg_dump` piloté par l'activité (skip sans activité, bootstrap si base à données sans signal, rétention 14, `.part` atomique + purge orphelins), preuve `app:db:restore-check`, board fraîcheur + alerte `freshness:db-backup` | [`docs/ops/backup-restore.md`](../../docs/ops/backup-restore.md) |
| 2026-07-18 | P0-4 | **Observabilité** — Sentry câblé dans les 3 zones (backend/engine/frontend), DSN-vide-inactif (une erreur d'init ne tue jamais l'app), `traces_sample_rate=0` (erreurs seulement, pas d'APM) | [`docs/ops/backup-restore.md`](../../docs/ops/backup-restore.md) §5 |
| 2026-07-17 | P3-12 | **Doc ops : tunnel Cloudflare pour les démos** (PR #242). Trois corrections par rapport au cadrage de la ligne : le port à exposer est **8081** (conteneur `frontend`, qui sert le SPA *et* proxifie `/api`) et non 8080, qui ne rend que du JSON ; le tunnel **ne dispense pas d'avoir la machine allumée** (il supprime le besoin d'un réseau commun, pas la machine) ; l'URL publique n'a **aucune auth** devant elle et `/engine/` y est proxifié, donc le solveur devient joignable sans auth le temps du tunnel. Retenu du cadrage : les garde-fous (ne jamais laisser tourner, aucune donnée réelle, vérifier que le CTRL+C a coupé). Écrit dans `docs/technique/` (à côté de `DEVELOPMENT.md`, qui le référence) plutôt que dans le `docs/ops/` proposé — ce dossier n'existe pas | [`demo-tunnel-cloudflare.md`](../../docs/technique/demo-tunnel-cloudflare.md) |
| 2026-07-16 | P2-6 (lot B, **bascule**) | **Pattern « Plan » — bascule** : le plan SEASON et sa version pointée SONT le calendrier de la saison ; « validé » se dérive du pointeur (plus de statut) ; valider = pointer + supprimer les sœurs, rouvrir = dépointer, aucun pointage automatique ; déblocage cockpit = `hasFinishedVersion` ; gate matchs/plans secondaires = pointeur (409) ; legacy droppé (`Season.{baselineScheduleId,socleValidatedAt,planningName}`, statuts `VALIDATED`/`ARCHIVED`, `SetBaselineController`). *(P2-6 reste au backlog : lot C à venir.)* | [ADR-0002](../../docs/architecture/adr-0002-pattern-plan.md) · [`planning-lifecycle-validated.md`](../courantes/planning-lifecycle-validated.md) |
| 2026-07-11 | P0-5 | Ids de créneau par-schedule (vol de créneau inter-version) | [`adr-0002-pattern-plan.md`](../../docs/architecture/adr-0002-pattern-plan.md) · `ScheduleResultImporterCrossVersionTest` |
| 2026-07-11 | P0-1 | **RGPD socle** (5 PRs #199-#203) : effacement (anonymisation + purge club différée 30 j, identité FFBB épargnée, win-back), portabilité (exports JSON compte/club), rétention auto (inactifs 24 mois avec préavis 1 mois, saisons N-1 au cron), audit trail append-only (12 mois), consentement au register + page confidentialité (placeholders juridiques) | [`../../docs/security/rgpd.md`](../../docs/security/rgpd.md) (registre des traitements) |
