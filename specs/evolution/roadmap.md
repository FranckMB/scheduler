# Roadmap — ce qui reste à faire

> **Ce fichier ne tient QUE l'ouvert.** Bugs, évolutions, dettes techniques : tout ce qu'on trace pour ne pas
> l'oublier un jour. Rien de livré n'y figure — un item livré **quitte** ce fichier et laisse sa trace dans
> [`../courantes/etat-des-lieux.md`](../courantes/etat-des-lieux.md), avec le comportement documenté dans la
> spec courante qui le reçoit.
>
> **Corollaire à ne pas contourner** : si vous cherchez « est-ce que X est fait ? », ce fichier ne répond pas —
> [`etat-des-lieux.md`](../courantes/etat-des-lieux.md) répond. Et si un sujet a été **tranché contre** une option
> évidente (« abandonné », « assumé »), c'est le §2 de l'état des lieux qui l'a, pas ici : ne le re-posez pas.
>
> **Index unique de l'ouvert.** Toute évolution, tout gap, toute idée future y laisse une ligne. Un fichier de
> détail à côté n'existe que pour un besoin trop gros pour une ligne, et doit être **référencé d'ici**.
>
> **Ids stables, jamais réutilisés** — un trou dans la numérotation = un item livré, jamais un oubli.
> **Impact** : 🔴 bloque la vente / l'intégrité · 🟠 fort levier · 🟡 valeur ciblée · ⚪ polish/dette.
> **Effort** : XS/S ≤ 1 PR · M 2-3 PR · L lot phasé · XL recherche + gros lot.
> Cap de commercialisation : **mi-2027**.
>
> **Fichiers de détail actifs** : [`gestion-matchs-ffbb.md`](gestion-matchs-ffbb.md) (P1-4) ·
> [`p2-9-verrous-et-alertes.md`](p2-9-verrous-et-alertes.md) (P2-9) ·
> [`bridage-freemium-decouverte.md`](bridage-freemium-decouverte.md) (P1-3) ·
> [`enregistrement-ffbb.md`](enregistrement-ffbb.md) (P3-4) · [`compte-demo.md`](compte-demo.md) (P2-4) ·
> [`console-superadmin.md`](console-superadmin.md) (P4-54) ·
> [`infrastructure-hebergement.md`](infrastructure-hebergement.md) (étude) ·
> [`reprise-perimetre-engage.md`](reprise-perimetre-engage.md) (mémoire produit du planning de saison).
>
> **Réf historiques** : `FF#n` / `BG G#n` = identifiants des anciens `features-futures.md` / `backend-gaps.md`,
> absorbés le 2026-07-05. `v3 §x` / `contraintes-v2` = specs figées de `specs/initiales/`.

---

## Ordre d'attaque conseillé

1. **P2-9 PR A** — dernier volet du seul 🔴 ouvert, et son prérequis technique est tombé le 2026-07-31.
2. **P2-15** — correctif terrain : le récap d'une période affiche le club entier au lieu de la période. *(P2-20, le renommage qui visait le mauvais plan, est livré.)*
3. **P1-4 (module matchs)** — la feature qui décide de la mise en prod, selon le fondateur. Gros lot, à phaser.
4. **P1-1 (rôles)** — le verrou qui débloque le plus de valeur aval (et solde P4-8 au passage).
5. **P1-3 (bridage freemium)** — pas de monétisation sans lui.
6. Puis **P2/P3** selon l'appétit commercial, **P4** par lots opportunistes.

---

## P1 — Enablers à fort levier

| # | Sujet | Impact | Effort | Débloque | Note |
|---|-------|:---:|:---:|---|---|
| P1-4 | **Module matchs — le lot qui décide la mise en prod** (FF#19 · FF#21 · v3 §1.4) | 🔴 | XL | la vente | **Besoin fondateur 2026-07-31 : « la dernière grosse feature qui me permettra de partir en prod ou non ».** Le socle existe (voir état des lieux §1.5) ; ce qui manque est le **parcours du gestionnaire**. (1) **Le vrai format FBI est connu** — l'export « Saisie des résultats pour tout le club » est un fichier **GLOBAL**, colonnes `Division · N° de match · Equipe 1 · Equipe 2 · Date de rencontre · Heure · Salle · e-Marque V2 · Score 1 · Forfait 1 · Score 2 · Forfait 2`. L'import livré suppose **un fichier par équipe** avec l'équipe choisie à l'upload : il est à reprendre sur le format réel, et la ligne « format supposé » se ferme enfin. (2) **Correspondance Division ↔ équipes de l'app**, explicite et **persistée** (le ré-import ne doit pas la redemander) — même décision que P4-35, même rôle que `Fixture.externalRef`. (3) **Seuls les matchs à domicile sont traités** ; l'extérieur reste informatif (il porte quand même l'empreinte trajet, qui est ce qui crée les conflits coach). (4) **Le solveur place l'heure et la salle des matchs à domicile**, sous contraintes — un gymnase n'accueille pas forcément des matchs (cf. Parking « gymnase utilisé en match »). (5) **Géométrie variable** : certains clubs ont leur calendrier entier dès maintenant, d'autres par phases (brassage → poule 1 → poule 2), d'autres 2 phases seulement — le parcours doit tenir le partiel et le ré-import sans que le gestionnaire se perde. (6) **Le but est de voir les problèmes TÔT** (« Emerick coache le dimanche après-midi mais les SM2 jouent à Grenoble le même après-midi ») pour demander les dérogations à temps — le radar de conflits existe déjà, c'est son **exploitation en amont** qui manque. Forme pressentie : un **wizard matchs**. ⚠ **Besoin à valider (§7 CLAUDE.md) et à re-spécifier AVANT tout plan** : [`gestion-matchs-ffbb.md`](gestion-matchs-ffbb.md) décrit un placement **manuel** ; « le solveur donne un horaire satisfaisant » est un axe **constraint semantics** → NR obligatoire. Absorbe P3-1. Dépend de P2-19 (reconnaissance API) pour ce qui peut être récupéré plutôt que saisi |
| P1-1 | **Rôles non-admin + modèle de permissions** | 🟠 | L | self-service coach · comptes coach · salle convivialité · P4-8 | `ClubUser.role` est hardcodé `admin` au register ; aucune différenciation de droits. `isManagementRole` = amorce, voters à câbler partout. Inclut l'écran **gestion des membres** (inviter / changer rôle / désactiver — le membership existe, pas l'UI) |
| P1-3 | **Bridage freemium Découverte** | 🟠 | M | monétisation | Spécifié, zéro code → [`bridage-freemium-decouverte.md`](bridage-freemium-decouverte.md). Club complet, **aucun cap d'entité** (sinon le solveur paraît nul) ; gate = `POST /generate` plafonné (~4) ; générer décompte, ajuster gratuit ; compteur **total non rechargeable** (⚠ `generation_count_season` se remet à zéro par saison — il faut un compteur qui ne recharge jamais) ; PDF off ; read-only à l'épuisement. Enforcement = **3 gardes, pas transversal**. Reste aussi : le **seed des 4 offres** `SubscriptionPlan` (le modèle est livré, les offres n'existent pas) et la logique `billing_cycle`/`plan_expires_at` |

---

## P2 — Différenciateurs & correctifs terrain

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P2-9 | **Un LOCK HARD annule silencieusement les contraintes — volet capacité (PR A)** | 🔴 | M | **Seul 🔴 ouvert. DÉBLOQUÉE le 2026-07-31** : le prérequis (source de payload en lecture seule) est livré avec P2-9ter, et le rail d'affichage des avertissements au récap avec #337. Reste à livrer : **demande** = somme des `sessionsPerWeek` du payload, **offre** = somme des capacités des créneaux du payload, alerte si `offre < demande` (`>=` et non `>`), message en « **au moins X** » — la condition est nécessaire, jamais suffisante (les créneaux sont situés). ⚠ **Ne PAS recalculer à la main ce que le builder calcule** : trois tentatives ont échoué sur exactement cette faute, dont une qui a causé une **perte de données** (les tags de la dernière équipe disparaissaient à l'ouverture du récap). Consommer `buildForPeriodPlan`. **+ demande fondateur 2026-07-31** : le sens **inverse** est voulu aussi — avertir quand l'offre de créneaux **dépasse** largement le besoin (capacité de gymnase inutilisée). Même calcul, second message ; à cadrer avec PR A plutôt que dans une PR séparée. Détail : [`p2-9-verrous-et-alertes.md`](p2-9-verrous-et-alertes.md) |
| P2-15 | **Les réglages d'une période ne sont respectés QUE par le solveur** | 🟠 | M | **Constaté 2026-07-31 (usage club), vérifié.** Le backend filtre correctement (depuis P2-14, la sélection vit dans `PeriodConstraintSelector` — source unique gate+payload — et `buildForPeriodPlan` la sérialise). **L'UI, elle, ignore tout** : (a) `RecapStep.tsx:37-44` lit `useWizardTeams()`/`useWizardVenues()`/`useVenueSlots()` **bruts** — d'où « 49 équipes » affichées quand 6 sont cochées, et des compteurs (`:108-111`) qui ne décrivent pas ce qui sera généré ; (b) l'écran de génération montre **tous** les gymnases alors qu'un seul sert — du bruit pur ; (c) les **sélecteurs de contraintes et de réservations** proposent encore un gymnase désactivé (le backend le sort du payload et avertit, mais offrir le geste est une confusion gratuite : « ça n'a pas de sens que le gymnase Mateo soit désactivé mais que je puisse encore y relier des contraintes »). **Même racine que P2-8 et que feu P2-14 (soldée)** : deux endroits répondent à la même question. Le bon correctif consomme le payload que le builder calcule déjà (`buildForPeriodPlan`, rendu lisible-seul par P2-9ter) plutôt que d'ajouter un quatrième miroir. **À faire AVEC ou APRÈS P2-9 PR A**, qui ouvre le même rail |
| P2-16 | **Contraintes de base à la création d'un club** | 🟡 | M | Besoin fondateur 2026-07-31 : aider le gestionnaire à réussir son **premier** planning. `AuthController::seedNewClub` (`:645`) sème aujourd'hui la saison, le plan SEASON, le sport et les 12 catégories — **zéro contrainte**. Un club neuf part donc d'une page blanche sur la partie la plus difficile à comprendre. **Non cadré, et c'est là le travail** : *lesquelles* ? (candidates évidentes : « les jeunes ne s'entraînent pas après 19h30 » via le tag JEUNE — le mécanisme existe déjà, cf. `TIME` + `TeamTagService` ; « pas d'entraînement le dimanche »). ⚠ Deux pièges : des contraintes HARD semées d'office peuvent rendre INFEASIBLE un club atypique, et des contraintes que le gestionnaire n'a pas écrites doivent être **visibles et supprimables** (sinon on recrée le silence que la série ENGINE a soldé). Trancher HARD vs PREFERRED et la liste **avant** de coder. Jumeau utile : P4-55 (montrer les règles implicites) |
| P2-17 | **Mutualisation lisible : « SM1/SM2 » sur un créneau partagé** | 🟡 | M | Besoin fondateur 2026-07-31, sur les périodes de vacances. La **mécanique** est livrée (réservation de 2 équipes sur un créneau à capacité 2, zéro engine — état des lieux §1.2), mais rien ne la **donne à lire** : le gestionnaire veut voir **« SM1/SM2 »** comme une seule ligne de planning, avec **le coach des SM1** considéré comme le coach de la séance. Et il veut pouvoir **proposer la mutualisation depuis les doléances** (« ces deux équipes peuvent s'entraîner ensemble cette semaine-là »). Trois volets distincts : affichage fusionné (grille + exports), règle de coach porteur (⚠ elle a un effet **solveur** : aujourd'hui les deux coachs sont contraints, pas un seul — l'affirmer sans le câbler serait un placebo, axe *constraint semantics* → NR), et le geste dans la modale doléances. **Cadrer les trois séparément** : seul le premier est gratuit |
| P2-18 | **Resynchronisation FFBB à la demande** (nom du club, logos, équipes) | 🟠 | M | Besoin fondateur 2026-07-31 : « mon club est en train de changer de nom, le logo change pour certaines équipes ». Aujourd'hui l'autofill FFBB est **one-shot à la création** (`FfbbClubPopulator` dispatché au `verifyEmail`) + un import manuel management-gaté (`POST /api/club/ffbb-import`) ; rien ne rafraîchit après coup **côté UI**, et **aucune synchro n'existe à la création d'une équipe**. À livrer : (a) bouton **« Resynchroniser depuis la FFBB »** dans l'onglet Club **et** dans la console superadmin (le catalogue d'actions support `AdminActionCatalog` est le bon rail — fermé, tracé, confirmé) ; (b) synchro **à la création d'une équipe** (logo/identité d'équipe) ; (c) **ne jamais écraser en silence** ce que le gestionnaire a édité à la main — proposer le delta, ou tracer ce qui a changé. Dépend de P2-19 pour savoir ce que l'API rend vraiment au niveau **équipe**. ⚠ Confinement SSRF à conserver tel quel (hosts en dur, jamais dérivés de l'input, redirections coupées) |
| P2-19 | **Reconnaissance `api.ffbb.app` — l'API officielle** | 🟠 | S | **Intel fondateur 2026-07-31 (source : collègue qui scrape la FFBB pour un site de club).** Il existe une **API officielle documentée** : `https://api.ffbb.app/`, spec OAS publique sur `/server/specs/oas`. Deux conditions d'accès : en-tête **`User-Agent: okhttp/4.12.0`** (sans lui le WAF répond 403) et un **bearer token** récupéré sur un endpoint public (`GET /items/configuration` → `data.key_dh`), à mettre en cache ~12 h et **jamais codé en dur**. L'app parle aujourd'hui à deux autres hosts (`api.ffbb.com` + `meilisearch-prod.ffbb.app`). **Travail = lire l'OAS et inventorier** ce qui est disponible sur les **rencontres**, les **équipes**, les **salles** et les **engagements** — puis dire, fait par fait, ce que ça retire de saisie au gestionnaire. Le livrable est un **cadrage écrit** qui alimente P1-4 et P2-18, pas du code. ⚠ Le token étant obtenu dynamiquement, sa gestion (cache, rotation, échec) est un vrai sujet de conception, pas un détail |
| P2-6 | **Pattern « Plan » — lot C** (réglages & génération par plan) | 🟠 | M | Dernier lot de la reconstruction ADR-0002. Lots A et B livrés (état des lieux §1.3) → [ADR-0002](../../docs/architecture/adr-0002-pattern-plan.md) |
| P2-2 | **Boucle d'ajustement — « corriger sur place »** | 🟠 | M | « Naviguer » est livré (#180) : les diagnostics surlignent les créneaux vides du gymnase concerné et la vue bascule dessus. **Reste** : glisser une équipe dans un créneau vide pour réparer. **⭐ Même primitive que la grille de réservation pré-génération** (grille + créneaux vides + clic = affecter) : une seule brique, deux moments (avant génération = LOCK HARD, après = réparer). À spécifier ensemble le jour venu. Jumeau : rendre les **diagnostics actionnables** (actions + liens entités dans le jsonb) plutôt que du texte |
| P2-8 | **Le front re-dérive des règles de refus du serveur** | 🟠 | M | `PlanningToolbar` recalcule côté client `isLastFinishedSeasonVersion`, `hasInFlightSibling` et le décompte des sœurs supprimées à la validation. **Chaque miroir est un risque de dérive** — c'est ce motif exact qui a produit 40 défauts en 4 rounds sur la bascule ADR-0002. Les supprimer suppose que le serveur expose ce qui est **permis** (capacités par ressource, patron de `Schedule.isChosen` / `Team.isEngaged`) : chantier de conception, pas correctif |
| P2-11 | **Le worker Messenger ne purge jamais le cache qu'il invalide** | 🟠 | S | **Constaté 2026-07-31.** `CacheInvalidationListener` collecte les clés sur `postPersist`/`postUpdate`/`postRemove` (`:37-50`) mais ne les vide que sur **`kernel.terminate`** (`:52-56`) — or **un worker Messenger n'émet pas de `TerminateEvent`**. Les invalidations collectées pendant `GenerateScheduleHandler` ne partent jamais : le payload `schedule_input` reste **périmé** après une génération, et `$pendingInvalidations` croît sur un process long. Fix : écouter `WorkerMessageHandledEvent`/`WorkerMessageFailedEvent`. ⚠ Indépendant de P2-9ter (qui a changé la clé et le mode de purge, **pas** le déclencheur) — ne pas confondre les deux |
| P2-12 | **Le pool `cache.tenant` n'a AUCUN writer** | 🟡 | S | **Constaté 2026-07-31.** `CacheInvalidationListener` purge `club.{id}.tenant_data` et `club.{id}.schedule_snapshot` (`:94-95`) : ce sont les **seules occurrences de ces chaînes dans tout `backend/src` et `backend/config`**. Plus large : le pool `cache.tenant` lui-même n'a **aucun writer** — un pool Redis déclaré (TTL 1 h, tag-aware en prod) et une purge à chaque `kernel.terminate`, pour zéro donnée. ⚠ **Un gate CI bloquant garde ce vide** : `TenantCacheIsolationTest` sème lui-même les clés qu'il vérifie — il atteste l'isolation d'un cache qui n'existe pas en production. **À trancher** : besoin à re-poser, ou vestige à retirer (pool + clés + injection + test). **Ne PAS supprimer le test sans l'avoir remplacé** — c'est le seul garde-fou d'isolation cross-club côté cache (l'invariant porte en réalité sur `cache.schedule`, couvert depuis P2-9ter) |
| P2-13 | **`TeamTagService::getOrCreateSystemTags` flushe à chaque appel** | 🟡 | S | **Constaté 2026-07-31.** Le `flush()` de `TeamTagService.php:128` est **inconditionnel** : il part même quand aucun tag système n'a été créé, c'est-à-dire presque toujours (les 21 tags d'un club sont créés une fois). Or `syncTeamTags` est appelé **par équipe** — un import FFBB ou une transition de saison qui crée N équipes déclenche N flushes. Correctif : ne flusher que si `persist()` a eu lieu, ou sortir la création des tags système du chemin par équipe (elle est idempotente par club) |
| P2-3 | **Versions — « Travailler sur cette version » + savepoint auto (D4)** | 🟡 | M | Moitié manquante de la décision 5 ; D1→D3quater livrés (état des lieux) |
| P2-4 | **Compte / mode démo** | 🟡 | S/M | Spécifié, zéro code → [`compte-demo.md`](compte-demo.md). **Fort levier de vente** : club fictif pré-rempli, génération en 30 s avant toute saisie. Options vendeur vs prospect self-service, anonymisation RGPD, flag `isDemo`. ⚠ Dépend en pratique de P4-16 (l'horloge simulée ne déplace pas le « aujourd'hui » de l'UI — sans quoi on démontre un cockpit à la mauvaise date) |

---

## P3 — Complétude modules

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P3-13 | **Radar — ne montrer que l'avenir actionnable** | 🟠 | M | **Besoin fondateur 2026-07-31, trois symptômes d'une même cause.** (a) **Toutes** les vacances scolaires à venir sont affichées : en été, Noël et la Toussaint apparaissent alors que c'est « TROP loin pour que je m'en occupe de suite ». Les jours fériés ont un horizon (`PUBLIC_HOLIDAY_HORIZON_DAYS = 30`, `RadarPanel.tsx:22`) ; **les vacances n'en ont aucun** — attendu : les **prochaines** vacances + tout événement portant un planning inachevé. (b) La **couverture d'une période découpée** compte les semaines **passées et en cours** : « 0/7 semaines couvertes » alors que 3 des 7 sont derrière — « on gère l'avenir, pas le présent ». Les semaines révolues ne doivent ni compter ni proposer un plan. (c) Le radar **devient très long** : il faut un **repli/dépliage** par carte. ⚠ Le filtrage par date doit passer par l'horloge simulée en dev (P4-16), sinon la démo ne peut plus rejouer une situation datée |
| P3-14 | **Doléances — tri par importance et cohérence coach ↔ équipe** | 🟡 | S | **Constaté 2026-07-31.** (a) La liste de coachs de la todo-list n'est **pas triée** (`CoachWishesModal.tsx:51` construit un groupe unique dans l'ordre brut de l'API) alors que le regroupement salariés / coachs-joueurs / bénévoles **existe déjà** (`groupedCoaches`, utilisé au récap) — à réutiliser plutôt qu'à réinventer. (b) **Incohérence à la saisie** : choisir une équipe pré-sélectionne son coach (« c'est TOP »), mais le champ coach reste ensuite **libre** — on peut enregistrer « U18F1 — Emerick » alors qu'Emerick ne coache que les SF1 et U15F1. Le choix doit être borné aux coachs **liés à l'équipe** (`TeamCoach`). ⚠ Garder le cas « coach dé-attribué » (`coachId` null), déjà géré et affiché |
| P3-15 | **Modale « Solliciter les coachs » — trop longue, et sollicite le passé** | 🟡 | M | **Constaté 2026-07-31.** (a) `CampaignDialog.tsx` empile semaines + équipes + deadline + liste des coachs dans une seule modale : « BEAUCOUP TROP longue, c'est pas jouable ». Piste fondateur : **onglets**. (b) La liste des équipes sélectionnées n'est **pas triée par importance** (le rang existe, `groupTeamsByTier` est déjà utilisé ailleurs). (c) **Les semaines passées et la semaine en cours sont proposées ET cochées par défaut** (`:47` `periodAdjustWeeks` sans filtre de date, `:59` défaut = toutes) — on ne sollicite un coach que pour les semaines **à venir**. Même règle que P3-13 (b), à traiter avec |
| P3-1 | **Matchs — reste du palier A** (FF#21) : volet joueur (`CoachPlayerMembership` côté matchs), `Team.preferredMatchWindow`, envelope-ligue HARD | 🟡 | M | **Absorbé par P1-4** — à traiter dans son cadrage plutôt qu'isolément |
| P3-2 | **Overlays — période `custom` générante** | 🟡 | M | `periodType=custom` n'est pas générant : créer un overlay dessus rend 422 = impasse UX. Mitigation livrée (bouton désactivé + tooltip, gardé par test). Reste à activer quand `custom` devient générant |
| P3-3 | **Modèle « templates → occurrences »** (v3 §3.5, §8.1 · FF#8) | 🟡 | L | Débloque la cascade calendrier de saison → plans secondaires, et le grain fin (exceptions, annulations, déplacements, remplacements coach/salle). ⚠ **La matérialisation glissante J+14 de la vision d'origine est abandonnée** au profit d'une projection + occurrences éparses (deltas) : `schedule_slot_occurrences` ne stocke, si elle existe, **que les overrides**. Le grain fin est déjà partiellement couvert par `ManualEditController` (`/manual-edit/one-time` + `temporaryLock`) — n'ajouter la table **que si** un besoin réel le justifie |
| P3-4 | **Refonte enregistrement FFBB** (anti-squatting du code club — v3 §9.1 · FF#19) | 🟡 | M | Spécifié → [`enregistrement-ffbb.md`](enregistrement-ffbb.md). Vérification du gestionnaire par code/document FFBB + approbation par email si le club existe déjà. Croise le gate `/api/club_users` et P2-19 |
| P3-8 | **Binômes de partage définis par le gestionnaire** | 🟡 | L | Besoin fondateur 2026-07-17. Sur un créneau à capacité > 1, le solveur associe **n'importe quelles** équipes ; le gestionnaire veut pré-définir les paires autorisées (deux catégories jeunes compatibles oui, SM1 + U9 non). ⚠ **Rouvre une décision tranchée** (`allow_shared_court`, abandonné) — le terrain montre la conséquence du choix : la capacité dit *combien*, jamais *avec qui*. **Trancher la FORME avant de coder** : N² paires à saisir pour 40 équipes = inutilisable, donc **un tag de compatibilité** (le gestionnaire tague, le moteur dérive) est probablement la bonne forme. Axe *constraint semantics* → NR |
| P3-16 | **Suppression sûre (salle / équipe / coach)** | 🟡 | M | Ex-ligne §9. Entités **saison-scoped**, partagées par tous les plannings — il n'y a pas de « suppression pour un seul plan ». Décisions actées : **hard delete**, précédé d'une popup listant l'impact en cascade (contraintes liées, liens coach, **plannings secondaires supprimés**). Recoupe DOC-2 (les matchs déjà déclarés perdent leur salle) — les traiter ensemble. Axe *planning lifecycle* → NR |
| P3-17 | **`COACH_FORBIDDEN_TIME_RANGE`** (plage horaire interdite, au-delà du jour entier) | 🟡 | M | `COACH_AVAILABILITY` ne gère aujourd'hui que les **jours** (`unavailableDays`) — l'engine sait déjà lire une fenêtre horaire (`_coach_window_minutes`), c'est la **config + l'UI** qui manquent. Même grille que le questionnaire coach si celui-ci retient jours × plages |
| P3-5 | **Versions — diff / comparaison** (`ScheduleDiffService` — v3 §6.2 · FF#11) | 🟡 | L | Hors périmètre D assumé ; les snapshots existent déjà (`snapshot_data`) |
| P3-7 | **Import d'équipes Excel — UI wizard** | ⚪ | S | Le backend existe (`POST /clubs/{id}/import-teams`) ; l'écran manque, et l'étape Équipes se tape aujourd'hui **une ligne à la fois** (49 équipes chez BCCL). ⚠ **Lire P4-35 AVANT de câbler l'écran** : la déduplication actuelle perd des équipes en silence, et le correctif décidé est une **correspondance explicite** faite par le gestionnaire — ça change la maquette, pas seulement le backend. À terme remplacé par l'API FFBB (P2-19) |
| P3-18 | **Deux façons de nommer un planning dans les dialogues serveur** | ⚪ | XS | **Constaté 2026-07-31 (revue #339 round 3).** Depuis P4-41 le nom d'un planning vient de son PLAN (`SchedulePlanProvisioner::displayNameOf`, consommé par les exports PDF/XLSX). Mais `OverlayManager::periodLabelOf` (`backend/src/Service/OverlayManager.php:199`) nomme les plannings secondaires par le **titre de la `CalendarEntry`** — le FAIT déclencheur (« Gymnase A — fermé »), que l'ADR-0002 distingue explicitement de la RÉPONSE qu'est le plan. Ce libellé part dans le corps du 409 `overlays_exist`, donc dans la popup qui liste les plannings qu'une réouverture va détruire. **Pas un défaut de la PR** (elle ne fait qu'améliorer son repli) et **défendable en l'état** : dans une popup de suppression, le gestionnaire reconnaît peut-être mieux son incident que le nom du plan. **À trancher** : uniformiser sur le nom du plan, ou assumer l'écart et l'écrire dans l'ADR. |
| P3-10 | **Libellés de versions : utiliser `Schedule.versionNumber`** | 🟡 | S | Le serveur porte un `versionNumber` **stable** (compteur monotone du plan) ; le front dérive ses libellés « V3 » de l'ordre de création. Depuis la bascule, valider **supprime** les sœurs — la version choisie se retrouve donc libellée « V1 ». Sans conséquence tant que le plan n'a qu'une version, mais la numérotation ment dès qu'on en supprime une |
| P3-11 | **Radar : aucun état de chargement** | 🟡 | XS | `RadarPanel` n'affiche ni cartes ni « Rien à l'horizon » tant que les impacts des fermetures arrivent — un cadre « À traiter » nu pendant quelques centaines de ms. Le masquage pendant le vol est **voulu** (ne pas faire clignoter une carte qui va disparaître) ; il manque juste un squelette. À traiter avec P3-13 |
| SEC-12 | **RLS : scoper le SELECT sur 3 tables** (résiduel) | 🟠 | S | Volet « test de portée » **livré** (état des lieux). **Non fait, décision fondateur : documenter et tracer, ne pas durcir en urgence.** `club_user` et `coach_wish_token` ont un `SELECT` **ouvert** (`USING (true)`), `audit_log` des policies scindées : sur ces trois tables l'isolation repose sur **une** couche (le filtre Doctrine) au lieu de deux — une requête native ou un `createQuery` filtre désactivé y fuiterait. **Pourquoi ce n'est pas une faille aujourd'hui** : le filtre Doctrine couvre tout chemin authentifié, aucun SQL brut ne touche ces tables, la campagne est management-only sous RLS complète, le token est un secret de 32 octets sans endpoint de listing. **Correctif proposé** : policy `SELECT` scopée au GUC *quand il est posé* (`current_setting('app.club_id', true) IS NULL OR …`) — le lookup pré-GUC continue de marcher, l'étanchéité revient dès qu'un contexte tenant existe. → [`rls.md`](../../docs/security/rls.md) §Exceptions |
| DOC-3 | **`PUT /api/teams` : `level` absent ≠ `level` effacé** | 🟡 | S | **Arbitré 2026-07-31 : absent = on garde.** `TeamInput::$level` est un `?string` — un PUT qui **omet** le niveau est indiscernable d'un PUT qui l'**efface**, et deux sémantiques se contredisent dans le dépôt (le commentaire documente le PUT partiel, `TeamApiTest::testLevelIsClearedWhenNullOnUpdate` atteste que `null` efface). Conséquence sur une équipe engagée : un renommage par l'API prend un **409 nommant un champ qu'il n'a pas envoyé**. Le wizard y échappe (il ré-écho `level`), donc aucun écran n'est cassé. À livrer : aligner `level` sur les 4 champs déjà passés en « absent = inchangé » (`forcedVenueId`, `matchDay`, `minSessionsOverride`, `parentTeamId`). Effacer un niveau devient impossible par l'API — assumé ; le test change de sens et doit être réécrit. Axe *périmètre engagé* → NR |
| DOC-2 | **Un match déposé à la fédération peut perdre sa salle sans avertissement** | 🟠 | S | **Arbitré 2026-07-31 : avertir avant, laisser passer.** Le périmètre engagé protège les ÉQUIPES ; rien ne protège la **salle d'un match**. Supprimer un gymnase (`EntityCascadeDeleter::purgeChildrenOfVenue:104`) ou charger une version antérieure (`StructureRestorer:173`) **dépointe** `Fixture.venueId` de n'importe quel match, y compris `SUBMITTED`/`VALIDATED` — déjà déclaré à la ligue à CETTE salle. Le match redevient visiblement « à placer », donc récupérable — c'est pourquoi l'un vaut un NULL et l'autre un 409. **Ne PAS refuser le geste** (un gymnase qui ferme, ça arrive). À livrer : la confirmation annonce « N match(s) déjà déclaré(s) perdront leur salle et devront être re-soumis à la fédération ». À traiter avec P3-16. Axe *périmètre engagé* → NR |
| DOC-1 | **Divergence doc↔code : le module matchs est-il découplé du socle ?** | 🟠 | XS | `gestion-matchs-ffbb.md` (l.315/376) et `module-matchs.md` (l.5) affirment que le gating des matchs est **découplé** de la validation du socle. Le code dit l'inverse : `SocleGuard::assertSeasonPlanChosen` gate `FixtureStateProcessor` et `ImportFixturesController`. Divergence **antérieure** à la bascule (intention produit vs code livré). **À trancher par le fondateur** : c'est la doc qui a tort, ou le code ? À poser dans le cadrage de P1-4 |

---

## P4 — Dette & polish (avant GA, par lots opportunistes)

### Retour terrain du 2026-07-31 (wizard, overlay, général)

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P4-36 | **Étape Équipes — en-tête, rang par ligne, tri par colonne** | ⚪ | M | Un lot, quatre demandes liées. (a) **L'en-tête des colonnes n'apparaît qu'une fois une équipe créée** : il est rendu dans la branche `teams.length > 0` (`TeamsStep.tsx:554-562`), alors que le formulaire d'ajout (`:486-535`) est **au-dessus** et n'a que des placeholders — on saisit à l'aveugle. (b) **Le rang n'existe pas sur la ligne** : il n'est que le titre de la section (`tierGroupLabel`, `:567`) ; `TeamRow` affiche `#`, nom, catégorie, genre, niveau, séances — pas le rang. Décision fondateur : **un badge S/A/B/C/D sur chaque ligne + les flèches ↑↓ visibles en permanence** (déplacer sans entrer en mode Trier) ; le drag & drop du bouton « Trier » reste comme seconde manière. Nom d'équipe plus étroit : assumé. (c) **Tri par colonne** : rang par défaut, mais aussi catégorie, genre, niveau de jeu, nombre de séances. ⚠ **Conflit de forme à trancher** : la liste est aujourd'hui **groupée par rang** (sections) — trier par une autre colonne exige un mode liste **plate**, sinon le tri ne s'applique qu'à l'intérieur de chaque section. (d) **Ordre des catégories inversé** : le catalogue va du plus jeune au plus vieux (`CategoryCatalog.php:28-39`, `sortOrder` 0→11), le fondateur veut **Vétéran → Senior → U21 → … → U5**. ⚠ Les catégories sont **copiées en base par club** au register : changer le catalogue ne suffit pas, il faut décider du sort des clubs existants (re-numérotation, ou tri à l'affichage) |
| P4-37 | **Étape Gymnases — grille 8h→23h, durées 45 min→2h30, indice de clic** | ⚪ | S | (a) **La grille s'arrête à 22h** (`weekGrid.ts:8`, `END_MIN = 22*60`) : un créneau de 22h-23h existe en base et reste **invisible**. Source unique partagée avec la grille de réservation — un seul changement couvre les deux. (b) **Les durées proposées vont de 1h à 2h** (`days.ts:27`, `[60,75,90,105,120]`) ; attendu **45 min → 2h30 par pas de 15 min**. (c) **Rien ne dit comment poser un créneau** : la barre « À poser : [durée] » (`VenuesStep.tsx:359-368`) n'est suivie d'aucun indice — ajouter « cliquer sur la grille pour ajouter un nouveau créneau ». ⚠ Élargir la grille à 15 lignes d'heure de plus la rallonge d'autant : vérifier qu'elle tient encore à l'écran (`ROW_H = 11px`) |
| P4-38 | **Bandeau de période — réorganisation en deux lignes** | ⚪ | XS | Forme demandée par le fondateur (`WizardLayout.tsx:285-311`). **Ligne 1** : `Mode période — Vacances d'Été — semaine du 17 août 2026`, et **à droite** l'icône Supprimer + le bouton « Quitter » renommé **« Retour à l'accueil »**. **Ligne 2** : `du 17-08-2026 au 23-08-2026`, et **à droite** le bouton Doléances |
| P4-39 | **Le score du solveur ne doit plus apparaître nulle part** | ⚪ | XS | Décision fondateur : « ça ne sert à rien pour le gestionnaire, on l'affiche nulle part, c'est plus simple, ça épure ». Deux sites : `PlanningToolbar.tsx:160` et `SeasonPlanBanner.tsx:73`. ⚠ Retirer **l'affichage**, pas la donnée : `Schedule.score` et `solver_metrics` restent (télémétrie superadmin, comparaison de versions) |
| P4-40 | **Diagnostics ouverts par défaut après une génération dans le wizard** | ⚪ | XS | Décision fondateur : ouverts **une fois la génération lancée depuis le wizard** — « sinon on risque de ne pas le voir si on n'est pas familier avec l'écran génération » — et repliés en boucle de travail. ⚠ **Contredit un réglage antérieur** : `PlanningPage.tsx:144-146` dit « collapsed by default (**user request**) ». Le nouveau comportement est **contextuel**, pas un simple inversement. ⚠ Deuxième repli à traiter : même panneau ouvert, les **groupes par sévérité** sont refermés (`DiagnosticsPanel.tsx:34`, `openSeverity` initialisé à `null`) — un diagnostic INFO reste donc à deux clics |
| P4-42 | **Groupe d'âge BABY (U5-U7), distinct d'EMB** | ⚪ | S | Besoin fondateur. Aujourd'hui `TeamTagService::determineTagNames` (`:148`) pose **EMB** pour tout `ageMax ≤ 12`, ce qui agrège Baby basket, U5, U7, U9 et U11 sous un seul tag. Attendu : **BABY** pour U5-U7, EMB pour le reste. ⚠ Deux effets de bord : les contraintes ciblant le tag EMB changent de portée (à annoncer), et **les tags des équipes existantes ne se resynchronisent pas tout seuls** — cf. P4-50, le listener n'écoute que `Team`, pas `SportCategory` |
| P4-43 | **Overlay — régler l'heure des créneaux depuis l'onglet Gymnases, et voir le sélecteur de ressources** | ⚪ | S | (a) « Il faut que je puisse facilement définir l'heure des créneaux dans l'onglet des gymnases » d'une période — aujourd'hui la période possède sa grille mais l'édition d'horaire n'y est pas au même endroit que dans le wizard de saison. (b) Le **bouton de sélection coachs / gymnases / équipes** de l'écran de génération « n'est pas très visible ». (c) Le **barré des gymnases désactivés existe déjà** dans la structure de période (`PeriodStructure.tsx:513`) — la demande porte sur les **autres** sélecteurs, traitée en P2-15 |

### Dette technique et polish antérieurs

| # | Sujet | Impact | Effort | Note |
|---|-------|:---:|:---:|---|
| P4-16 | **L'horloge simulée ne déplace pas le « aujourd'hui » de l'UI** | 🟡 | S/M | La bascule de saison et les crons sont routés sur `ClockInterface` (PR #114), mais **tout ce qui dérive « aujourd'hui » côté front** (`new Date()`) et le code serveur qui construit les cartes du radar lisent l'heure réelle. Symptôme : impossible de rejouer une situation datée pour une démo ou une recette — bonne saison, mauvais jour. **Impact direct sur P2-4 (démo) et P3-13 (filtrage du radar par date)** |
| P4-35 | **L'import d'équipes FFBB ne regarde aucune identité d'équipe** | ⚪ | M | `FfbbExcelImporter.php:84` demande `findOneBy(['clubId','seasonId'])` — soit « cette saison a-t-elle **une** équipe ? » au lieu de « **cette** équipe existe-t-elle ? ». Mesuré : saison vierge + 4 lignes → `created=2, skipped=2` ; 1 équipe existante + 3 lignes → `created=0, skipped=3` ; réponse **200** dans tous les cas. Le nombre d'équipes importées dépend de l'ordre et de la diversité des catégories du fichier (un `flush()` de `findOrCreateSportCategory` rend visible l'équipe précédente). **Inatteignable par l'UI aujourd'hui** (aucun écran n'appelle la route) → redevient bloquant avec P3-7. **Correctif décidé : une correspondance EXPLICITE, pas une clé naturelle** — le gestionnaire apparie, la correspondance est **persistée** pour le ré-import. Reste technique quel que soit le design : le `flush()` en plein milieu de la boucle laisse des écritures partielles si l'import casse |
| P4-25 | **Écritures API Platform sans verrou optimiste (transverse)** | ⚪ | M | `AbstractStateProcessor::processPut` remplace tous les champs éditables sans vérifier la colonne `version` : deux éditeurs de la même ligne (deux onglets, cache react-query périmé 30 s) → lost update silencieux. Constaté sur `CoachWish`, mais **universel**. Fix : If-Match/ETag ou garde de `version` (409 sur conflit), câblé une fois pour toutes. Fenêtre étroite → ⚪ |
| P4-26 | **Deux coachs d'une même équipe partagent la doléance de la semaine** | ⚪ | M | `CoachWish` est clé sur `(calendarEntryId, teamId, weekStart)` **sans dimension coach** : une équipe à MAIN + ASSISTANT n'a qu'**une** doléance par semaine, et le second à saisir écrase le premier. **Atténué** (le pré-remplissage montre la valeur courante, et « un souhait, jamais une contrainte »). Décision fondateur explicite (« un souhait par équipe × semaine ») → laissé tel quel en V0. Même famille que P4-25. À relire au moment de P3-14 |
| P4-8 | **`resolveClubId` choisit arbitrairement pour un gestionnaire multi-club** | ⚪ | M | Le front n'envoie pas `X-Club-Id`, donc `TenantFilterListener::resolveClubId` retombe sur `findOneBy(['userId','isActive'])` **sans ORDER BY** : pour un humain gérant deux clubs, « quel club est courant ? » n'a pas de réponse définie. **Fix = choix produit** (sélecteur de club explicite, ou club par défaut sur l'adhésion) → rattaché à **P1-1** |
| P4-27 | **Presenter de campagne exécuté deux fois par écriture** | ⚪ | S/M | `CoachWishCampaignStateProcessor` : `parent::process*` projette, le résultat est jeté, puis le presenter re-tourne après `tokenSync->sync()` (indispensable : les tokens n'existent pas au 1er passage). Le déplacer dans `afterPersist` a été **essayé et abandonné** — ce hook est réservé aux effets de bord facultatifs et post-commit, or créer les tokens est obligatoire. Le vrai fix demande une porte « écrire sans projeter » au processor de base : modification du socle partagé, pas un quick win |
| P4-28 | **`GET /api/admin/health` : sondes séquentielles + scan Redis intégral** | ⚪ | M | `AdminHealthService` lance ~6 sondes HTTP/TCP + 3 dépendances externes **une à une** (une dépendance lente stalle toute la réponse, poll 30 s) ; `AdminMessengerFailedController::fromRedisStream` **bufferise tout le stream** pour paginer. Fix : sondes concurrentes (HttpClient multiplexé), pagination Redis native |
| P4-34 | **Quatre ancres `schedulePlanId` sans FK ni garde d'existence** | ⚪ | S | P4-30 a couvert `Reservation` et `VenueTrainingSlot`. `VenuePeriodOverride`, `TeamPeriodOverride`, `ConstraintPeriodOverride` et `Schedule` acceptent aussi un `schedulePlanId` en écriture, sans FK ni `AssertsSchedulePlanExistsTrait` : une ancre inventée crée une ligne orpheline silencieuse. Même correctif, même migration en deux temps, même NR. L'asymétrie est assumée pour borner la PR, pas parce que ces tables seraient différentes |
| P4-33 | **L'e2e Playwright n'est pas dockerisé** | ⚪ | S/M | Seul maillon qui exige du Node sur l'hôte — contredit « l'hôte n'a besoin que de Docker, Docker Compose et Make ». En local, deux échecs observés : le postinstall de **puppeteer** casse sur un `~/.cache/puppeteer` pollué, et `playwright install --with-deps` réclame `sudo`. Contournement : `rm -rf ~/.cache/puppeteer && PUPPETEER_SKIP_DOWNLOAD=1 npm ci && npx playwright install chromium`. **Fix** : service compose sur l'image officielle `mcr.microsoft.com/playwright` **épinglée à la version du `@playwright/test` du projet** — ⚠ l'épinglage FAIT la valeur (les tests de contraste mesurent un rendu ; trois navigateurs en circulation déplaceraient le problème) |
| P4-2 | **ENG-17 + ENG-24** — `coachId` de sortie limité aux slotTemplates → diagnostics coach inertes ; `coach_overload` confond les unités | 🟡 | M | audit |
| P4-3 | **Trois composants wizard > 400 lignes** (TeamsStep · ConstraintsStep · VenuesStep) | ⚪ | M | Extraire éditeurs/modales en composants frères, la step reste liste + orchestration. ⚠ P4-36 et P4-37 vont y toucher : bon moment pour le faire, mauvais moment pour le faire **en même temps** |
| P4-4 | **Registre tu/vous incohérent** (le wizard tutoie, le cockpit vouvoie) | ⚪ | S | Trancher un registre app-wide + sweep |
| P4-7 | **Publish Mercure dupliqué** (`GenerateScheduleHandler` / `ExportPdfHandler`) | ⚪ | S | Extraire un notifier **seulement si un 3ᵉ publisher apparaît** (defer délibéré) |
| P4-9 | **Radar — carte « jour férié » à retravailler** | ⚪ | S | Aujourd'hui « 14 juillet · Dans 8 j · jour férié » pour **tout** férié à ≤ 30 j, sans CTA. À cadrer : **quels** fériés méritent le radar (tous ? seulement ceux qui tombent un jour d'entraînement de la semaine type ?) et **le texte** (dire l'impact — « 2 séances ce jour-là » — plutôt que répéter la date). Décision produit. À traiter avec P3-13 |
| P4-10 | **Désactiver « Régénérer » si rien n'a changé** | ⚪ | M | Demande une détection de changement fiable |
| P4-12 | **`*PeriodOverride` — parité miroir à durcir (les 2 jumeaux)** | ⚪ | S | (a) `createEntityFromInput` = check-then-insert non atomique → POST concurrents → 500 au lieu de 422 ; (b) `#[ApiFilter(SearchFilter)]` inerte (le provider custom lit les params à la main — ne sert que le snapshot OpenAPI) ; (c) le provider ré-implémente `provideCollection` au lieu du hook `applyRequestFilters` ; (e) la règle de défaut reprise (CLUB/COACH/TEAM gardées, FACILITY droppée) est **dupliquée** PHP `activePermanentForReprise` + TS `defaultKept` sans source partagée — une édition unilatérale ferait diverger checklist et payload. Toucher `TeamPeriodOverride` **et** `ConstraintPeriodOverride` ensemble |
| P4-18 | **DA « ça sent le basket »** | ⚪ | S/M | L'app est visuellement neutre alors qu'elle s'adresse à des clubs de basket : fond de bandeau évoquant un parquet, illustrations d'états vides, iconographie. **Justification = mono-sport assumé, PAS préparation multi-sport.** Argument de démo et de vente. **Garde-fou de coût** : regrouper les assets en **un seul thème** — pour qu'un changement de parti pris reste une PR et non une chasse au trésor. Sobriété : un fond + les états vides suffisent |
| P4-23 | **`DayDialog` charge toute la collection des schedules pour un booléen** | ⚪ | S | La garde destructive dérive « le plan porte ≥ 1 version » de `useSchedules()` filtrées. Correct (fail-closed pendant le chargement) mais tire toute la collection pour une existence. **Fix propre** : champ dérivé **batché** `hasVersions` sur `SchedulePlanResource`, sur le motif de `Schedule.isChosen` |
| P4-44 | **Réservations en localStorage jamais revalidées** | 🟡 | S | Le wizard copie l'horaire au moment T ; si le créneau bouge ou disparaît, la réservation part en **LOCK HARD sur un horaire mort** → INFEASIBLE sans explication pour le gestionnaire. Fix : référencer le slot et revalider au lancement (`useLaunchGeneration`) |
| P4-45 | **Rate limiting — les deux caps métier et la clé par club** (v3 §10.2) | ⚪ | S | Le throttle `api` **par utilisateur** est livré (SEC-11) et gardé en CI. **Restent** les caps métier annoncés en v3 §10.2 : `POST /generate` **5/h** et export PDF **10/h**, et surtout la clé **par CLUB** là où l'existant borne par utilisateur (un club à 3 gestionnaires triple le budget). Croise P1-3 (le bridage freemium plafonne le **total**, pas le débit) |
| P4-46 | **Fuseau horaire du club** (`ClubTimeService`, UTC ↔ fuseau — v3 §6.2 · FF#10) | ⚪ | M | Transverse — touche tout affichage horaire. La **zone** scolaire est dérivée du code club ; le fuseau ne l'est pas. Sans objet tant que le produit est métropolitain ; devient réel avec les DOM/TOM (dont les calendriers de vacances sont **déjà** supportés) |
| P4-47 | **Doc OpenAPI des routes custom restantes** | ⚪ | M | Toute route `#[Route]` hors API Platform est **invisible** de `/api/docs` et du snapshot tant qu'elle n'est pas déclarée dans `CustomRoutesOpenApiFactory`. La factory a grandi ; manquent notamment club appearance/logo, teams/reorder, health, reopen/validate, reset-season, calendar-entries conflicts, memberships, constraints/validate, password forgot/reset. Rattrapage en lot + réflexe « nouvelle route custom = entrée factory » |
| P4-48 | **i18n des diagnostics solveur** | ⚪ | S | Les alertes en français sont **obligatoires** côté produit ; aujourd'hui les messages FR sont codés en dur dans l'engine et il reste des clés EN (`soft_lock_moved`, `unused_slot`). Cible : clés + fichier de traduction côté backend (`DiagnosticMessageBuilder`) plutôt que du texte en dur dans l'engine |
| P4-49 | **Navigation — menu latéral gauche, sous-menu Assistant, clic sur le logo** | ⚪ | M | Le burger haut-droite est livré (Club · Profil · Thème · Logout) et le header allégé. **Restent** : le menu principal **à gauche**, les 6 étapes du wizard en **sous-menu** de l'entrée « Assistant » (remplace la colonne d'étapes interne), et **clic sur le nom/logo du club → Accueil** (un `<Link>`) |
| P4-50 | **`team_tags` — deux dettes qui se cumulent** | ⚪ | S | (a) `TeamTagService::syncTeamTags` **efface tous** les tags (custom inclus) et ne re-dérive que les tags **système** à chaque édition d'équipe → les tags **custom** sont **éphémères** et ne survivent pas à une édition. C'est ce qui bloque leur report entre saisons (dernier reliquat de la transition P2). (b) Depuis P2-9ter, les tags ne sont plus resynchronisés à la génération, seulement au write-path par `TeamTagSyncListener` — **qui n'écoute que `Team`**. Éditer une **`SportCategory`** laisse donc les tags des équipes **périmés**. Sans effet en V0 (les fixtures repartent de zéro) ; le correctif est d'étendre le listener. ⚠ **Prérequis de P4-42** (créer le tag BABY implique de re-dériver l'existant) |
| P4-51 | **`max_days_per_week` coach** (calcul dynamique + override) | ⚪ | S | **Vérifié : rien n'existe** — aucune occurrence dans `backend/src`, `engine/app` ni `frontend/src` (ni colonne, ni contrainte, ni case à cocher) |
| P4-52 | **Export — rétention des rendus + logo du club au PDF** | ⚪ | S | Les exports PDF/PNG/Excel sont livrés ; restent la **rétention** (base/period/manual/`is_pinned`) et le **logo du club dans l'en-tête PDF** |
| P4-53 | **Couverture de tests à compléter** (v3 §13.5 · FF#12) | ⚪ | M | Golden datasets `vacation_week` et `partial_regen` à vérifier (le premier dépend de P3-3) · invariants Hypothesis partiels (no double-booking, coach unique, coach-joueur cohérent, HARD préservé, tier-S jamais sacrifié si D placé) · couverture Vitest à étoffer |
| P4-54 | **Console super-admin — SA4 v2 puis SA5** | ⚪ | M | SA0→SA4 v1 livrés + monitoring + alerting (état des lieux §1.8). Suite au signal → [`console-superadmin.md`](console-superadmin.md). ⚠ Suspension de club et approbation fallback **délibérément différées** au premier cas réel |
| P4-55 | **Contraintes implicites du solveur — visibilité en lecture seule** | 🟡 | M | Besoin fondateur 2026-07-20. Le solveur applique des règles structurelles **jamais saisies** (`add_room_at_most_one`, `add_coach_at_most_one`, `add_coach_player_non_overlap`, `add_coach_rest_day_constraints`, `add_one_session_per_day_constraints`…) en plus des règles implicites documentées. Aucune n'est visible côté UI — le gestionnaire ne sait pas ce qui s'applique « gratuitement ». Demande : un panneau **lecture seule**, non éditable. **Non cadré** : où l'afficher, et surtout **quelles règles méritent d'être montrées** — certaines sont des détails d'implémentation sans valeur pour un non-technicien (`add_age_ascending_constraints`), d'autres directement utiles (non-chevauchement coach, repos après match). Trier avant tout cadrage. Jumeau de P2-16 |
| P4-56 | **Vue calendrier — 12 mois d'un coup** | ⚪ | S | Le cockpit rend la couche d'exceptions, les vacances de la zone et les fériés, mais **mois par mois** (navigable). Un panorama annuel n'existe pas. À rouvrir **seulement si** « voir l'année entière » est réellement demandé |
| P4-57 | **Validation temps réel — compléter la liste de warnings** | ⚪ | S | Le wizard valide par étape ; les warnings de réservation (créneau surchargé vs capacité, quota séances/semaine, 2 séances le même jour) et le warning « équipe compétitive classée D » sont livrés, non bloquants — le solveur reste l'autorité. La liste se complète au fil de l'eau. Recoupe le rail d'affichage de P2-9 PR A |
| P4-58 | **Wizard — polish restant** | ⚪ | S | (a) **Polarité des jours invisible** : cocher un jour l'*interdit*, et le sens n'apparaît qu'après enregistrement → label « éviter ces jours ». (b) **Pas d'indicateur « étape terminée »** dans le rail (la navigation est libre pour un club existant) → coches d'état par étape. (c) **Colonne coach « a » / « Sans coach »** : donnée à trancher |
| P4-59 | **Dialogue post-modification — l'endpoint existe, l'UI ne l'appelle jamais** (v3 §11.4 · FF#4) | 🟡 | M | `ManualEditService::applyPermanentConstraint` existe et `POST /api/schedule-slots/{id}/manual-edit/constraint` répond « Permanent constraint created. » — mais **aucun appelant frontend** (`features/planning/api.ts` n'appelle que `/manual-edit/lock` et `/one-time`) : l'endpoint est **mort côté produit**. Reste le **dialogue** lui-même (« convertir ce déplacement en contrainte permanente ? ») et le `source_occurrence_id` (qui, lui, dépend de P3-3) |

---

## Vision — non priorisé (V2 et au-delà)

> Gardé pour ne pas ré-inventer, mais hors cap mi-2027. Une ligne remonte au backlog quand un club le demande.

| Sujet | Effort | Note |
|---|:---:|---|
| **Matrice de temps de trajet + passerelles d'équipes** (U15→U18 — v3 §3.4, §4.1 · FF#5) | 🔴 | Tables absentes, contraintes engine en stub (`travel_feasibility`, `required_bridge`). **Comment obtenir la matrice** : le gymnase porte déjà une adresse → géocodage → distances. Deux paliers : (a) **vol d'oiseau** (haversine, zéro dépendance, suffit pour « ces deux gymnases sont-ils enchaînables ? ») puis (b) temps de trajet réel (API d'itinéraires, dépendance + quota). **(a) d'abord.** Géocodage : l'**API Adresse (BAN)** est gratuite, publique et 🇫🇷 (cohérent RGPD), à confiner comme `FfbbApiClient`. Stocker lat/long **sur le gymnase** (géocoder à la saisie, pas au solve) : le payload reste des nombres et l'engine ne parle à personne |
| **Reverse-engineering des contraintes** (dériver des PREFERRED du planning existant) | XL | Fort attrait, aucun cadrage. **Décisions déjà actées** : suggestions **PREFERRED uniquement** (des HARD dérivées figeraient le plan et neutraliseraient le solveur), **agrégation obligatoire** (« 4/4 séances mardi → 1 PREFERRED mardi » + score de confiance), analyse = **service backend pur, engine intouché**. Réutilise le rail `pendingConstraintSuggestion` déjà câblé |
| **Régénération partielle guidée** (`PartialRegenService` — v3 §6.2, §14.2 · FF#1) | 🔴 | Partiellement couvert par les overlays (une période génère un plan borné sans toucher au socle) ; reste la regen **ciblée du plan de base** hors période. À requalifier quand un besoin réel se présente |
| **Déterminisme exact du plan sur gros clubs** | 🟡 | Les 8 workers rendent l'assignation non déterministe (la **valeur** d'objectif reste stable). `interleave_search` seul ne suffit pas → refonte du budget timeout. À ne faire **que si** un club demande la repro exacte — aujourd'hui jugé inutile (le gestionnaire ajuste). Alternative : un mode « repro exacte » optionnel (1 worker + budget élargi) |
| **Connecteurs API municipales** (`venues.source`, `external_ref` — v3 §1.4 · FF#20) | 🔴 | V2 |
| **App mobile** (React Native/Expo — v3 §1.4 · FF#14) | 🔴 | **À ne pas faire avant d'avoir des clients payants** — web responsive + PDF suffisent |
| **Notifications coach** (push + lien de consultation sans login — v3 §1.4 · FF#17) | 🟡 | Le rail tokenisé sans login existe déjà pour les doléances |
| **Stats & analytics club** (taux de remplissage, heures-coach/semaine — v3 §1.4 · FF#16) | 🟡 | Peu coûteux une fois les occurrences là (P3-3) ; demande d'AG |
| **Dashboard multi-clubs** (au-delà de la console superadmin — v3 §14.3 · FF#15) | 🔴 | V2 |
| **Multi-sport** (handball, gym, volley — v3 §1.4 · FF#18) | 🔴 | **Attendre une vraie demande.** Conséquence assumée : P4-18 (DA basket) part du mono-sport et ne le prépare pas |

---

## Parking — idées gardées, non cadrées

- **Réservation de salle de convivialité** (self-service coach : réserver une soirée dans une salle non sportive → notif gestionnaire). La résa est **triviale** (salle = `Venue`, pas de solveur, juste un check de conflit). ⚠ **Le vrai coût n'est pas la résa** : « le coach réserve lui-même » exige des **comptes coach + un modèle de rôles** (P1-1). **Question stratégique** : veut-on que l'appli devienne le *hub du club* (self-service coach) ou reste l'*outil de planning* (piloté gestionnaire) ? Déclencheur de réouverture : P1-1 livré, ou demande d'un club pilote — la feature devient alors quasi gratuite.
- **Gymnase — paramètre « utilisé en match »** (besoin fondateur 2026-07-20) : un booléen sur `Venue` (précédents : `isExternal`, `canSplit`) ; l'écran matchs ne proposerait que les gymnases où il est vrai. Non cadré : où l'admin bascule le flag, défaut à la création, effet sur les matchs déjà placés si on le décoche. **Devient un prérequis de P1-4** (« certains gymnases peuvent accueillir des matchs, d'autres non ») — à cadrer dans son lot.
- **Reset du club : la route s'appelle encore `DELETE /api/reset-season`** alors que le geste rendu est « reset club ». Reliquat cosmétique, sans conséquence fonctionnelle.

---

## Dette — keeps délibérés

> Règle d'origine conservée : **un item de dette n'existe qu'avec une preuve** (`fichier:ligne`). Les dettes
> **actionnables** vivent comme lignes P4-x ci-dessus, dans le même pipeline que tout le reste. Cette section ne
> garde que les décisions « **ne pas corriger** » qu'il faut pouvoir retrouver.

| Item | Décision | Preuve / raison |
|------|----------|-----------------|
| **Ambre codé en dur dans `MonthCalendar.tsx`** (pas le token `--warning`) | 🟩 **keep délibéré** | `--warning` clair = 2.9:1 sur fond (A11Y-06) → l'utiliser sur le label vacances **échouerait WCAG AA** ; `amber-700` passe. Migrer = régression a11y contre un résidu de cohérence |
| **Publish Mercure dupliqué** | 🟩 defer (ligne P4-7) | 2 handlers, payloads distincts — extraire au 3ᵉ publisher |
| **`buildForOverlay` gardé comme adaptateur de `buildForPeriodPlan`** | 🟩 keep | Le court-circuit `scheduleId === null ⇒ []` est une économie d'intention, pas une branche de comportement (`scheduleId` est NOT NULL sur l'entité) — aucun test ne peut le faire tomber, ne pas en écrire un qui prétendrait le contraire |
