# --- OS / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# --- Local tooling / workspace artifacts ---
/.code-review-graph/
/.omo/
/.opencode/
/.serena/
/doc/
# dossier business perso (stratégie, pricing, pilote) — local only, jamais poussé
/business/
# Claude Code : config perso par machine (allowlist permissions, MCP) — jamais poussé
/.claude/settings.local.json
# RTK : stats de tracking locales — on garde uniquement filters.toml (config projet)
/.rtk/*
!/.rtk/filters.toml

# --- PHP / Symfony ---
/vendor/
/var/
/.phpunit.result.cache
/.php-cs-fixer.cache
/.phpstan-cache/
/.psalm-cache/
/.phpunit.cache/
/.env.local
/.env.*.local
!/.env.example

# --- Python ---
__pycache__/
*.egg-info/
*.py[cod]
*.pyo
*.pyd
.python-version
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.tox/
.coverage
coverage.xml

# --- React / Node ---
node_modules/
dist/
build/
.next/
.turbo/
coverage/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Docker / local runtime ---
/.env
/volumes/
*.pid
*.sock
# Local dev Compose override (binds docker.sock + ADMIN_RESTART_ALLOWED for the
# DEV-only container restart feature — see plan admin-console-tabs-and-containers
# todo 12, docker-compose.override.yml at the project root).
/docker-compose.override.yml
# Added by code-review-graph
.code-review-graph/
/.installed

# code-review-graph local artifact
/backend/.codegraph
/backend/.omo/

# Prod runtime files (VM-side; only the .dist template is tracked)
/.env.prod
/jwt/
