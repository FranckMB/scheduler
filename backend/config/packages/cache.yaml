framework:
    cache:
        # Default app cache (filesystem in dev, Redis in prod)
        app: cache.adapter.redis_tag_aware
        default_redis_provider: '%env(REDIS_URL)%'

        pools:
            # P2-12 (2026-08-04) : le pool cache.tenant a été SUPPRIMÉ — déclaré en
            # phase 1, jamais aucun writer. Le seul cache métier est cache.schedule.
            # Schedule snapshots and solver inputs (longer TTL)
            cache.schedule:
                adapter: cache.adapter.redis_tag_aware
                provider: '%env(REDIS_URL)%'
                default_lifetime: 14400  # 4 hours
                tags: true
