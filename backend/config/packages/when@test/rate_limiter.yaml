framework:
    rate_limiter:
        # Effectively disable throttling in tests (state lives in cache, not rolled back).
        auth_register:
            policy: 'sliding_window'
            limit: 100000
            interval: '15 minutes'
        auth_password_forgot:
            policy: 'sliding_window'
            limit: 100000
            interval: '15 minutes'
