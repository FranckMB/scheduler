framework:
    messenger:
        transports:
            async:
                dsn: 'sync://'
            # In-memory in tests: never touch Redis for the failure transport.
            failed: 'in-memory://'
